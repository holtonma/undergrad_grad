fZ6
g=%
h%V
!*Q
'N"
!LS
001
)5/
000
001
)9_
002
Top Level Commands
"0X
Help Index
!"G
001
)F6
001
Help on Help
!!N
001
)RR
006
Solvers
"'R
Examine Command
$UX
Message Functions
!DF
Too Many Guesses
#X]
Help on Help
!!N
Help Index
!"G
001
*IT
001
Troubleshooting
!#)
001
*RN
003
Status Field
%ZL
Status Line
!]U
Troubleshooting
!#)
002
*X%
+(1
001
Name Field
%]R
001
+(1
001
Name Syntax
!&^
001
+JV
003
Expression
!M.
Logical Expression
!P=
Statement Syntax
!)4
001
+R]
006
Procedures
!/I
Macros
$.C
Rule Syntax
!($
Function Syntax
!-7
Local Variables
!+1
Name Syntax
!&^
001
,H/
005
Parameter Variables
&,]
Argument Variables
&/5
Result Variables
&1N
Input Variables
&4)
Output Variables
&6E
001
,QT
006
Built-in Functions
!7&
List Function
!1=
Rule Function
!0C
Procedure Function
!/I
Statement Syntax
!)4
Rule Syntax
!($
001
-+M
002
Statement Syntax
!)4
Rule Function
!0C
001
-J\
002
Rule Syntax
!($
Procedure Function
!/I
001
."?
005
Table Mapping
!3>
Step Mapping
!44
Linear Interpolation
!52
Cubic Interpolation
!6/
Function Syntax
!-7
001
.).
002
Step Mapping
!44
List Function
!1=
001
.2J
002
Linear Interpolation
!52
List Function
!1=
001
.<A
002
Cubic Interpolation
!6/
List Function
!1=
001
.C8
002
List Function
!1=
Table Mapping
!3>
016
.J]
.M<
.PU
.XM
.[-
._^
/6'
/GK
/TS
0;&
0_T
1=V
1QY
2**
2F[
2P&
001
Function Syntax
!-7
001
.J]
001
Built-in Functions
!7&
002
.J]
.I\
001
Values
!L)
001
.PU
001
Built-in Functions
!7&
001
.M<
001
Built-in Functions
!7&
001
.XM
001
Built-in Functions
!7&
004
.PU
.M<
.XM
.[-
002
Non-invertible Math Functions
!=2
Built-in Functions
!7&
001
._^
002
Invertible Math Functions
!;W
Built-in Functions
!7&
001
/*K
005
Non-invertible Math Functions
!=2
Invertible Math Functions
!;W
Complex Functions
!@[
Built-in Functions
!7&
User-defined Functions
$*7
001
/6'
002
Built-in Functions
!7&
Complex Arithmetic
!KB
001
/GK
002
Built-in Functions
!7&
Logical Expression
!P=
001
/TS
001
Built-in Functions
!7&
006
0;&
0_T
1=V
1QY
2**
2F[
001
Built-in Functions
!7&
001
0_T
002
Troubleshooting
!#)
Built-in Functions
!7&
001
1=V
002
Character Strings
":B
Built-in Functions
!7&
001
1QY
002
Keystroke Macros
$.C
Built-in Functions
!7&
001
2**
002
Keystroke Macros
$.C
Built-in Functions
!7&
001
240
002
Dialog Functions
!GC
Keystroke Macros
$.C
001
2F[
003
TK-Specific Functions
!CE
Keystroke Macros
$.C
Built-in Functions
!7&
001
2P&
001
Built-in Functions
!7&
001
2TQ
001
Complex Functions
!@[
001
3'4
002
Name Syntax
!&^
Generalized Character Strings
":B
001
3:L
006
Arithmetic Expression
!RK
Relational Expression
!QP
Logical Expression
!P=
Symbolic Expression
!O]
List Reference
"%0
Indeterminate Expressions
!TM
001
3A/
001
Expression
!M.
001
3EE
003
Relational Expression
!QP
Boolean Function
!A\
Expression
!M.
001
3I5
002
Expression
!M.
Arithmetic Operators
!SF
001
3P=
002
Expression
!M.
Arithmetic Operators
!SF
001
3UT
002
Arithmetic Expression
!RK
Relational Expression
!QP
001
4$Y
002
Zero-by-Zero Division
!UI
Expression
!M.
001
41,
002
Indeterminate Expressions
!TM
Expression
!M.
001
47"
003
TK Sheets
!W^
Presentation View
'@X
Problem Solving with TK
"'-
001
4HI
014
TK Model
!VI
Variable
$(R
Rule
$)2
Function
$*7
Unit
$C$
Keystroke Macro
$+]
List
$D!
Plot
$D<
Table
$GK
Comment
$K'
Numeric Format
$HO
Global
$K;
PV
'@X
Select Sheet, =
$(!
001
4R+
006
Status Line, SV
!]U
Status Line, PV
!_"
Prompt/Error Line
!`(
Window Command
$%,
Configuration Editor
%:9
Select Sheet Command
$(!
001
5)V
002
Screen Composition
![7
Status Line, Presentation View
!_"
001
57,
002
Screen Composition
![7
Status Line, Sheet View
!]U
001
5>\
003
Screen Composition
![7
Iteration Count
'6J
Convergence Factor
"/W
001
5DD
003
Unit Sheet
$C$
Unit Conversion
""I
Comment Field
&#^
002
5T+
\=4
002
Unit Sheet
$C$
Unit Field
%_$
001
6'Q
001
Unit Conversion
""I
002
6/?
\=4
003
Square Brackets
"%0
List Solve
%C@
List Sheet
$D!
001
7&!
002
Lists
"$$
Statement Syntax
!)4
001
7@"
003
Direct Solver
")G
Iterative Solver
"*Z
List Solve
%C@
001
7H]
001
Model Structure
!VI
001
7OC
005
Direct Solver
")G
Iterative Solver
"*Z
Backsolving
"-C
Goal Seeking
".K
Troubleshooting
!#)
001
8?H
002
Solvers
"'R
Solve Command
"&"
001
8JJ
001
Inconsistent
#_C
001
8R2
007
Solvers
"'R
Solve Command
"&"
Direct Solver
")G
Comparison Tolerance
'41
Iteration Count
'6J
Convergence Factor
"/W
Automatic Iteration
'3+
001
9%B
003
Solvers
"'R
Direct Solver
")G
Iterative Solver
"*Z
001
92"
003
Backsolving
"-C
Direct Solver
")G
Iterative Solver
"*Z
001
9MF
002
Iterative Solver
"*Z
Comparison Tolerance
'41
001
9U:
003
Function Keys
"2*
Slash Commands
$L@
Navigation Commands
"4<
001
9[4
011
Top Level Commands
"0X
F1
!!N
F2
"4-
F3
$YN
F4
$\+
F5
"7<
F6
$%,
F7
$D<
F8
$GK
F9
"&"
F10
%C@
001
:,\
000
001
:8M
004
Top Level Commands
"0X
Goto Command
#`?
Sheet View
!W^
Presentation View
'@X
001
:V1
006
Edit Field
"7<
Blank
$O:
Delete
$PF
Insert
$QJ
Copy
$O`
Move
$RN
001
;!*
001
Editing Commands
"5`
001
;/K
003
Sheet Edit
"5`
Field Edit
"7<
Navigation Commands
"4<
001
;C*
002
Storage Command
$V_
Wildcards
":"
001
;_G
001
Disk Index
"9-
001
KD?
005
String Functions
!ED
TK Names
!'C
Comments
&#^
Symbolic Values
!L)
Symbolic Value Format
'/3
003
f6?
J;?
fFR
003
Disk Index
"9-
Function Save
%"Y
Merge and Include
$[B
001
<4P
000
001
<<+
003
Expression
!M.
Rule Syntax
!($
Statement Syntax
!)4
001
<A&
003
Expression
!M.
Rule Syntax
!($
Statement Syntax
!)4
001
<E9
002
Built-in List Functions
!B]
Function Syntax
!-7
001
<M*
001
Function Syntax
!-7
001
<Q2
001
Function Syntax
!-7
001
<U$
001
Statement Syntax
!)4
001
<X1
001
Statement Syntax
!)4
001
<\V
002
Function Type
&(6
Function Syntax
!-7
001
=%6
001
Expression
!M.
001
=(O
002
Built-in Functions
!7&
Function Syntax
!-7
001
=/'
003
Built-in Functions
!7&
Name Syntax
!&^
Function Syntax
!-7
001
=2+
002
Built-in Functions
!7&
Function Syntax
!-7
001
=75
002
Built-in Functions
!7&
Function Syntax
!-7
001
=<?
001
Values
!L)
001
=@H
002
Built-in Functions
!7&
Function Syntax
!-7
001
=EK
001
Expression
!M.
001
=L@
001
Complex Arithmetic
!KB
001
=P?
001
Expression
!M.
001
=T7
002
Rule Syntax
!($
Complex Arithmetic
!KB
001
=ZV
001
Function Syntax
!-7
001
=_U
001
Statement Syntax
!)4
001
>'S
001
Parameter Variables
&,]
001
>+=
001
Statement Syntax
!)4
001
>/J
002
Function Syntax
!-7
Expression
!M.
001
>4G
001
Expression
!M.
001
>7T
000
001
><&
002
Function Type
&(6
Function Syntax
!-7
001
>J=
002
List Solve
%C@
Status Field
%ZL
001
>S>
002
List Solve
%C@
Status Field
%ZL
001
>EC
000
001
>YE
001
Configuration
%:9
001
>\]
001
ASCII Load
%J.
001
?$1
001
Plot Sheet
$D<
001
?*9
001
Editing Commands
"5`
001
?0E
001
Blank Command
$O:
001
?4!
001
Editing Commands
"5`
001
?9Y
001
Save Command
$\+
001
?F)
001
Load Command
$YN
002
?NJ
?Y5
003
Disk Index
"9-
Add Step
%=6
Multiply By Step
%?(
001
?]0
000
001
?`\
001
Table Sheet
$GK
001
@)M
001
Title
&V&
001
@3E
001
Table Sheet
$GK
001
@9H
001
Load Command
$YN
001
@?$
001
List Function
!1=
001
@K>
000
001
@R!
000
001
@W7
000
001
@ZA
000
001
@`G
000
001
A%A
000
001
A(:
000
001
A,?
001
List Fill Command
%;T
001
AE0
000
001
AI3
001
Expression
!M.
001
AMQ
005
Parameter Variables
&,]
Argument Variables
&/5
Result Variables
&1N
Input Variables
&4)
Output Variables
&6E
001
AQF
005
Parameter Variables
&,]
Argument Variables
&/5
Result Variables
&1N
Input Variables
&4)
Output Variables
&6E
001
AX8
001
Disk Index
"9-
001
A]P
004
Unit Conversion
""I
Display Unit
%`^
Calculation Unit
&"@
DIF Load
$'/
001
B#/
004
Unit Conversion
""I
Display Unit
%`^
Calculation Unit
&"@
DIF Save
$&=
001
B);
000
001
B-2
000
001
B14
001
Line Chart Scaling
&?-
001
B7G
001
Status Field
%ZL
001
B=6
000
001
BFY
001
Unit Conversion
""I
001
BJV
001
Plot Sheet
$D<
001
BNI
001
WKS Load
%FX
001
BV[
000
001
BYN
000
001
B`E
000
001
C+,
000
001
C.T
000
001
B`E
000
001
C6@
000
001
C=I
000
001
CB>
001
Keystroke Macro Commands
$=`
001
CF+
001
Include Files
$[B
001
CQB
000
001
CU2
001
Configuration of TK
%:9
001
C^+
001
Disk Index
"9-
001
D#?
001
Disk Index
"9-
001
D(F
001
Disk Index
"9-
001
D/9
000
001
D3-
001
Disk Index
"9-
002
D82
<17
000
001
D8T
000
001
D>G
000
001
DCM
000
001
DG_
001
Wildcards
":"
002
DK9
<17
000
002
DMZ
<+.
000
001
DQK
000
001
DU]
000
002
DX1
<+.
001
Invertible Math Functions
!;W
002
DYT
<+.
001
Logarithmic Functions
!:G
002
D[6
<+.
000
002
D_8
<+.
001
Trigonometric Functions
!9_
002
E!Y
<+.
001
Trigonometric Functions
!9_
002
E$<
<+.
001
Hyperbolic Functions
!;/
002
E'.
<+.
001
Hyperbolic Functions
!;/
002
E*0
<+.
001
Trigonometric Functions
!9_
002
E,H
<+.
001
TK-Specific Functions
!CE
002
E57
<+.
001
TK-Specific Functions
!CE
002
E89
<+.
001
List and Multi-Argument Functions
!B]
002
E:[
<+.
001
List and Multi-Argument Functions
!B]
002
E>K
<+.
001
List and Multi-Argument Functions
!B]
002
EC?
<+.
001
List and Multi-Argument Functions
!B]
002
EG\
<+.
001
List and Multi-Argument Functions
!B]
002
EL9
<+.
001
List and Multi-Argument Functions
!B]
002
EO+
<+.
001
List and Multi-Argument Functions
!B]
002
EQ^
<+.
001
List and Multi-Argument Functions
!B]
002
EV0
<+.
000
002
EXR
<+.
000
002
EZ7
<+.
001
Trigonometric Functions
!9_
002
E\F
<+.
001
Hyperbolic Functions
!;/
002
E_"
<+.
001
Hyperbolic Functions
!;/
002
F!/
<+.
001
Hyperbolic Functions
!;/
002
F#^
<+.
001
Logarithmic Functions
!:G
001
F%?
001
Complex Functions
!@[
002
F0O
F2#
001
TK-Specific Functions
!CE
002
F(G
<+.
001
Invertible Math Functions
!;W
002
F*M
<+.
001
Invertible Math Functions
!;W
002
F-Y
<+.
001
List and Multi-Argument Functions
!B]
002
F1+
F2#
001
TK-Specific Functions
!CE
002
F1G
F2#
001
TK-Specific Functions
!CE
002
F5S
<+.
001
List and Multi-Argument Functions
!B]
001
F8V
001
List and Multi-Argument Functions
!B]
001
F<-
001
List and Multi-Argument Functions
!B]
002
F?L
<+.
000
002
FD5
<+.
001
List and Multi-Argument Functions
!B]
001
FHD
001
TK-Specific Functions
!CE
001
FM`
000
001
FS*
000
001
FVB
001
Rule Function
!0C
001
F]C
001
Statement Syntax
!)4
001
G"H
001
Statement Syntax
!)4
001
G&3
001
Statement Syntax
!)4
001
G,3
001
Statement Syntax
!)4
001
G0G
001
Statement Syntax
!)4
001
G3H
002
Relational Expression
!QP
Logical Expression
!P=
001
G6,
002
Expression
!M.
Symbolic Value
!L)
001
G81
001
Procedure Function
!/I
001
G>V
002
Built-in Functions
!7&
Function Syntax
!-7
002
GF0
GY;
002
Troubleshooting
!#)
Iterative Solver
"*Z
001
GM=
000
001
H%^
000
002
GT!
GY;
002
Troubleshooting
!#)
Iterative Solver
"*Z
001
H-M
001
TK-Specific Functions
!CE
001
H2W
002
Built-in Functions
!7&
Function Syntax
!-7
001
H7-
004
Statement Syntax
!)4
Brackets
"%0
Apostrophe
'LT
Indeterminate Expressions
!TM
001
HHK
000
001
HU>
000
001
HY2
000
001
HZ@
002
Message Functions
!DF
TK Name Syntax
!&^
001
I%H
000
001
I.#
000
001
I10
000
001
IFK
002
Overdefined
"*8
Comparison Tolerance
'41
001
INB
001
Navigation Commands
"4<
001
J.3
001
Plot Sheet
$D<
001
J2!
004
Save Model
$\+
Save 1 Sheet
$]O
Save Lists
%1.
Storage Command
$V_
003
J8,
J;?
JB9
004
How to Use Help
!!N
Top Level Commands
"0X
Configuration
%:9
Load File
$YN
001
JI4
000
001
JN5
000
001
JTU
004
Select Sheet Command
$(!
Slash Commands
$L@
SV
!W^
PV
'@X
001
Y&=
002
DIF storage
%3F
Disk Index
"9-
001
Y3D
002
DIF Storage
%3F
Disk Index
"9-
001
K$`
002
Sheets
!W^
Window Command
$%,
001
K-/
001
Rule Sheet
$)2
001
K6'
003
Status Field
%[B
Rule Field
&''
Rule Syntax
!($
001
K:?
004
Function Syntax
!-7
List Function
!1=
Rule Function
!0C
Procedure Function
!/I
001
KX$
007
Keystroke Macros
$.C
Macro Names
$1.
Running Macros
$4V
Recording Macros
$7<
Macro Statements
$:'
Macro Commands
$=`
Key Designators
$@M
001
L!(
007
Macro Names
$1.
Running Macros
$4V
Recording Macros
$7<
Macro Statements
$:'
Macro Commands
$=`
Key Designators
$@M
Keystroke Macro Sheet
$+]
001
L;:
008
Running
$4V
StartUp Macro
$3`
Recording
$7<
Macro Statements
$:'
Macro Commands
$=`
Key Dsgntrs
$@M
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
001
LB]
002
Running Macros
$4V
Macro Names
$1.
001
LL4
007
Recording
$7<
Macro Statements
$:'
Macro Commands
$=`
Key Designators
$@M
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
Macro Names
$1.
001
LT=
007
Macro Statements
$:'
Macro Commands
$=`
Key Designators
$@M
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
Macro Names
$1.
Running Macros
$4V
001
M$(
008
Macro Commands
$=`
Statement Syntax
$=%
Key Dsgntrs
$@M
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
Macro Names
$1.
Running Macros
$4V
Recording Macros
$7<
001
+R]
002
Macro Commands
$=`
Macro Statements
$:'
001
MI$
007
Key Designators
$@M
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
Macro Names
$1.
Running Macros
$4V
Recording Macros
$7<
Macro Statements
$:'
001
N8W
006
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
Macro Names
$1.
Running Macros
$4V
Recording Macros
$7<
Macro Statements
$:'
001
OR\
002
Unit Sheet Fields
"!?
Unit Conversion
""I
001
OW,
001
Lists
"$$
001
P""
003
Line Chart Subsheet
$EW
Bar Chart Subsheet
$F?
Pie Chart Subsheet
$G'
002
P<H
PB)
001
Display Scale
&<D
002
P>?
PB)
001
Display Scale
&J[
002
P@4
PB)
001
Data List
&Q5
001
PC+
003
Table Subsheet
&U3
Table Box
'F_
Field Box
'C&
001
Q%F
005
Numeric Notation
&`V
Global Numeric Format
'8@
N Format, List Sheet
&9@
N Format, Table Sheet
&[_
N Format, Variable Sheet
&&!
002
Q.^
Q3=
000
001
Q@K
002
Display Intermediate Values
'18
Configuration
%:9
002
QH3
QWG
009
Editing Commands
"5`
Window
$%,
Storage
$V_
Print
$SR
Examine
$UX
Reset
$U:
Quit
$V'
Top Level Commands
"0X
Slash Commands in Presentation View
%K:
001
Q].
001
Editing Commands
"5`
001
R"H
001
Editing Commands
"5`
001
R+H
002
Editing Commands
"5`
/D in Presentation View
%R#
001
R1!
002
Editing Commands
"5`
/I in Presentation View
%X6
001
R95
002
Editing Commands
"5`
/M in Presentation View
%MH
001
RRE
002
Slash Commands
$L@
/P in Presentation View
$TT
001
S&8
001
/P in Sheet View
$SR
001
S12
000
001
S;M
000
001
X-)
000
001
SFZ
001
Slash Commands
$L@
002
f!$
QWG
000
001
RG'
011
Load Model
$YN
Save Model
$\+
Save 1 Sheet
$]O
DIF
%3F
WKS
%5/
ASCII
%6X
Text
%8[
Include
%9T
Configuration
%:9
Delete
%:^
Disk Index
"9-
001
SN3
006
Storage Command
$V_
Disk Index
"9-
DIF
$'/
WKS
%FX
ASCII
%J.
Merge and Include Files
$[B
001
S[^
002
Load File
$YN
/SI
%9T
003
T0]
T2C
T8>
004
Save 1 Sheet
$]O
Save Lists
%1.
Storage Command
$V_
Disk Index
"9-
001
T@D
012
Variable
%!"
Function
%"Y
Keystroke Macro
%$T
Unit
%'&
Numeric Format
%)!
Plot
%*\
Table
%,W
Comment
%.R
Lists
%1.
Save Entire Model
$\+
Storage Command
$V_
Disk Index
"9-
002
TKS
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command
$V_
Disk Index
"9-
002
TP0
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
002
TTM
T2C
006
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
Keystroke Macros
$.C
002
TYM
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
002
U(F
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
002
T]<
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
002
U"^
T2C
005
Save 1 Sheet
$]O
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
002
U-;
T2C
006
Save 1 Sheet
$]O
Save Comments as Text File
%8[
Save Lists
%1.
Save Entire Model
$\+
Storage Command, /S
$V_
Disk Index
"9-
001
T:?
008
DIF Save
$&=
DIF Load
$'/
WKS Save
%F&
WKS Load
%FX
ASCII Save
%GJ
ASCII Load
%J.
Save Entire Model
$\+
Save 1 Sheet
$]O
001
U2O
005
DIF Save
$&=
DIF Load
$'/
ASCII Storage
%6X
WKS Storage
%5/
Storage Command
$V_
001
U;!
005
WKS Load
%FX
WKS Save
%F&
ASCII Storage
%6X
DIF Storage
%3F
Storage Command
$V_
001
UC4
006
ASCII Save
%GJ
ASCII Load
%J.
WKS Storage
%5/
DIF Storage
%3F
Storage Command
$V_
Built-In File Fns
!I?
001
UKF
002
Storage Command
$V_
Comment Sheet
'05
001
UV_
001
Storage Command
$V_
001
V%K
001
Storage Command
$V_
001
V\`
002
Storage Command
$V_
Disk Index
"9-
001
Y>U
004
Add Step
%=6
Linear Spacing
%>2
Multiply By Step
%?(
Geometric Spacing
%@'
001
YFL
002
Linear Spacing
%>2
List Fill Command
%;T
001
YOK
002
Add Step
%=6
List Fill Command
%;T
001
YUK
002
Geometric Spacing
%@'
List Fill Command
%;T
001
Y_J
002
Multiply By Step
%?(
List Fill Command
%;T
001
W#W
004
List Solve
%C@
Block Solve
%D;
Get Element
%ED
Put Element
%E"
001
W/\
003
List Command
%A%
Plot Sheet
$D<
Table Sheet
$GK
002
W8/
\=4
002
List Command
%A%
Solve Command
"&"
002
WJ_
\=4
001
List Command
%A%
001
WX7
001
List Command
%A%
001
X"&
001
List Command
%A%
001
X>=
002
WKS Storage
%5/
Disk Index
"9-
001
XG@
002
WKS Storage
%5/
Disk Index
"9-
001
XO^
004
ASCII Storage
%6X
Built-In File Fns
!I?
Global Numeric Format
'8@
Disk Index
"9-
001
ZSC
002
ASCII Save
%GJ
Global Numeric Format
'8@
001
X^L
003
ASCII Storage
%6X
Built-In File Fns
!I?
Disk Index
"9-
001
Z&/
007
Presentation View
'@X
Move Box
%MH
Grow Box
%P!
Delete Box
%R#
Untangle Boxes
%T:
Freeze Box
%V6
Insert Rows
%X6
001
Z17
007
Grow Box
%P!
Delete Box
%R#
Untangle Boxes
%T:
Freeze Box
%V6
Insert Rows
%X6
Slash Commands in PV
%K:
/M in Sheet View
$RN
001
Z6[
006
Delete Box
%R#
Untangle Boxes
%T:
Freeze Box
%V6
Insert Rows
%X6
Slash Commands in PV
%K:
Move Box
%MH
001
Z;`
007
Untangle Boxes
%T:
Freeze Box
%V6
Insert Rows
%X6
Slash Commands in PV
%K:
Move Box
%MH
Grow Box
%P!
/D in Sheet View
$PF
001
Z@&
006
Freeze Box
%V6
Insert Rows
%X6
Slash Commands in PV
%K:
Move Box
%MH
Grow Box
%P!
Delete Box
%R#
001
ZD:
006
Insert Rows
%X6
Slash Commands in PV
%K:
Move Box
%MH
Grow Box
%P!
Delete Box
%R#
Untangle Boxes
%T:
001
ZK]
007
Slash Commands in PV
%K:
Move Box
%MH
Grow Box
%P!
Delete Box
%R#
Untangle Boxes
%T:
Freeze Box
%V6
/I in Sheet View
$QJ
001
ZY3
002
Overdefined
"*8
Variable Sheet
$(R
001
[-A
001
Rule Sheet
$)2
001
[?O
002
Procedures
!/I
Macros
$.C
001
[L@
003
Variable Sheet
$(R
Values
!L)
Expressions
!M.
001
[SD
002
Name Syntax
!&^
Sheets
!W^
001
[X@
001
Variable Sheet
$(R
002
[`Q
\=4
005
Unit Conversion
""I
Display Unit
%`^
Calculation Unit
&"@
Variable Sheet
$(R
List Sheet
$D!
002
\.Z
\=4
004
Unit Field
%_$
Calculation Unit
&"@
Unit Name
!&^
Unit Conversion
""I
002
\61
\=4
004
Unit Field
%_$
Display Unit
%`^
Unit Name
!&^
Unit Conversion
""I
001
\C[
001
Comment Sheet
$K'
002
\M:
\TA
001
Iterative Solver
"*Z
001
\W9
002
Variable Sheet
$(R
List Sheet
$D!
001
]#,
002
Numeric Format Sheet
$HO
Global Numeric Format
'8@
001
]<T
003
Rule Syntax
!($
Rule Sheet
$)2
Rule Function Subsheet
!0C
001
a!,
002
Function Syntax
!-7
Function Sheet
$*7
001
a(8
001
Function Syntax
!-7
001
aD+
003
Keystroke Macros
$+]
Procedure Functions
!/I
Rule Functions
!0C
001
a1G
001
List Function
!1=
002
a7M
a;?
001
List Function
!1=
002
a@>
a;?
001
List Function
!1=
001
aD+
006
Argument Variables
&/5
Result Variables
&1N
Input Variables
&4)
Output Variables
&6E
Local Variables
!+1
Function Syntax
!-7
001
aNI
006
Result Variables
&1N
Input Variables
&4)
Output Variables
&6E
Parameter Variables
&,]
Local Variables
!+1
Function Syntax
!-7
001
aV3
006
Argument Variables
&/5
Input Variables
&4)
Output Variables
&6E
Parameter Variables
&,]
Local Variables
!+1
Function Syntax
!-7
001
b!#
006
Output Variables
&6E
Argument Variables
&/5
Result Variables
&1N
Parameter Variables
&,]
Local Variables
!+1
Function Syntax
!-7
001
b)A
006
Input Variables
&4)
Argument Variables
&/5
Result Variables
&1N
Parameter Variables
&,]
Local Variables
!+1
Function Syntax
!-7
001
`V&
001
List Sheet
$D!
001
](H
002
Numeric Format Sheet
$HO
Global Numeric Format
'8@
001
`Y>
001
List Sheet
$D!
001
b2@
001
Plot Sheet
$D<
002
b<)
b?O
002
Plot Sheet
$D<
Configuration File
%:9
002
bBW
bE!
002
Display Zero Axes
&=A
Plot Sheet
$D<
001
bG4
002
Display Grid
&>4
Plot Sheet
$D<
001
bK3
002
Line Chart Scaling
&?-
Plot Sheet
$D<
001
bM?
002
Title Field
&?_
Plot Sheet
$D<
001
b82
002
X-Axis Label
&@R
Plot Sheet
$D<
001
b`8
002
Y-Axis Label
&AE
Plot Sheet
$D<
001
c$A
002
X-Axis MinMax
&B9
Plot Sheet
$D<
002
c(C
c7,
002
Y-Axis MinMax
&C2
Plot Sheet
$D<
002
c,%
c7,
002
X-Axis List
&DN
Plot Sheet
$D<
002
c1Z
c7,
001
Plot Sheet
$D<
001
c>;
002
Y-Axis Lists
&EA
Plot Sheet
$D<
001
cCH
002
Style Field
&F3
Plot Sheet
$D<
001
c\@
003
Character Field
&G<
Symbol Count
&H/
Plot Sheet
$D<
001
d)=
002
Symbol Count
&H/
Plot Sheet
$D<
001
bW_
003
First Element
&I4
Plot Style
&F3
Plot Sheet
$D<
001
cSO
002
Last Element
&J'
Plot Sheet
$D<
001
cW%
002
Display Scale
&<D
Plot Sheet
$D<
002
bBW
bE!
002
Title Field
&KR
Plot Sheet
$D<
001
b82
002
X-Axis Label
&LE
Plot Sheet
$D<
001
b`8
002
Y-Axis Label
&M8
Plot Sheet
$D<
001
c$A
002
X-Axis List
&N*
Plot Sheet
$D<
001
d5-
002
Y-Axis List
&N\
Plot Sheet
$D<
001
d8?
002
Bar Chart Type
&OQ
Plot Sheet
$D<
001
d<"
002
Fill Type
&PA
Plot Sheet
$D<
001
d>E
002
Display Scale
&J[
Plot Sheet
$D<
002
d?L
bE!
002
Label List
&R+
Plot Sheet
$D<
001
dCD
002
Display Percent
&S!
Plot Sheet
$D<
001
dF5
002
Title Field
&SS
Plot Sheet
$D<
001
b82
002
Fill Type
&TC
Plot Sheet
$D<
001
d>E
002
Data List
&Q5
Plot Sheet
$D<
001
dHC
002
Title Field
&V&
Table Sheet
$GK
001
dL<
002
Vertical or Horizontal
&W$
Table Sheet
$GK
001
dM^
002
Row Separator
&WY
Table Sheet
$GK
001
dPW
002
Column Separator
&XQ
Table Sheet
$GK
001
dW'
002
First Element
&YF
Table Sheet
$GK
001
d]P
002
Last Element
&Z:
Table Sheet
$GK
001
d`I
002
List Field
&[,
Table Sheet
$GK
001
e$J
002
Width Field
&]F
Table Sheet
$GK
001
]1H
003
Numeric Format Sheet
$HO
Global Numeric Format
'8@
Numeric Format, List Sheet
&9@
001
e1+
002
Heading Field
&^;
Table Sheet
$GK
001
e3$
002
Screen or Printer
&U3
Table Sheet
$GK
001
e8(
001
Table Sheet
$GK
001
\C[
002
Numeric Notation
&`V
Numeric Format Sheet
$HO
001
]A]
002
Significant Digits
'!Y
Numeric Format Sheet
$HO
001
]M5
002
Decimal Places
'"X
Numeric Format Sheet
$HO
001
]R)
002
Padding
'#P
Numeric Format Sheet
$HO
001
]Z5
002
Display Leading Zero
'$U
Numeric Format Sheet
$HO
001
^"-
002
Decimal Point Symbol
'%Z
Numeric Format Sheet
$HO
001
^%)
002
Digit Grouping Symbol
'&`
Numeric Format Sheet
$HO
001
^)\
003
Zero Representation
'(?
Decimal Point Symbol
'%Z
Numeric Format Sheet
$HO
001
^3^
002
+/- Notation
')<
Numeric Format Sheet
$HO
001
^7J
002
Prefix
'*3
Numeric Format Sheet
$HO
001
^<B
002
Suffix
'+*
Numeric Format Sheet
$HO
001
^C7
002
Justification
',(
Numeric Format Sheet
$HO
001
^JL
002
Left Margin Width
'-*
Numeric Format Sheet
$HO
001
^Q1
002
Right Margin Width
'.-
Numeric Format Sheet
$HO
001
^Y4
002
Symbolic Value Format
'/3
Numeric Format Sheet
$HO
001
_"0
002
Character Strings
":B
Numeric Format Sheet
$HO
002
Q:4
Q3=
002
Save as Text File
%8[
Save as TK Model
%.R
001
_*)
002
Stop on List Error
'21
List Solve
%C@
001
_-X
002
Automatic Iteration
'3+
List Solve
%C@
002
_2_
\TA
002
Comparison Tolerance
'41
Iterative Solver
"*Z
001
_8B
002
Typical Value
'5+
Iterative Solver
"*Z
001
_D>
003
Maximum Iteration Count
'6J
Comparison Tolerance
'41
Iterative Solver
"*Z
001
_OR
004
Global Numeric Format
'8@
Iterative Solver
"*Z
Comparison Tolerance
'41
Convergence Factor
"/W
001
]6.
003
Append Variable Names
'9W
Numeric Format Sheet
$HO
ASCII Save
%GJ
001
_]$
001
Use Page Breaks
':<
001
`#K
002
Number Pages
';2
Print Command
$SR
001
`,/
002
Form Length
'<'
Print Command
$SR
001
`3!
002
Printed Page Length
'=$
Print Command
$SR
001
`9X
002
Printed Page Width
'=`
Print Command
$SR
001
`@]
002
Left Margin
'>U
Print Command
$SR
001
`G3
002
Printer Setup String
'?S
Print Command
$SR
001
`MY
002
Display Intermediate Values
'18
Print Command
$SR
001
NOW
007
Field Box
'C&
Plot Box
'E.
Table Box
'F_
Text Box
'HO
Slash Commands in PV
%K:
Sheets
!W^
Screen Composition
![7
002
N[_
fHN
006
Plot Box
'E.
Table Box
'F_
Text Box
'HO
Slash Commands in PV
%K:
Presentation View
'@X
Copy Command
$O`
001
O6%
005
Table Box
'F_
Text Box
'HO
Slash Commands in PV
%K:
Presentation View
'@X
Field Box
'C&
001
OA-
005
Text Box
'HO
Slash Commands in PV
%K:
Presentation View
'@X
Field Box
'C&
Plot Box
'E.
001
OLY
006
Slash Commands in PV
%K:
Presentation View
'@X
Field Box
'C&
Plot Box
'E.
Table Box
'F_
Greek and International Letters
!'C
001
e?3
003
Rule Sheet
%[B
Rule Function Subsheet
%[B
Procedure Function Subsheet
%\"
001
eI'
000
001
eS>
000
001
f!A
001
Disk Index
"9-
001
fHN
001
Field Box
'C&
247
ÚÄÄÄÄÄÄ Getting Started ÄÄÄÄÄÄ¿
$#"
³   Problem Solving with TK   ³
"'-
³   Model Structure           ³
!VI
³   Rules                     ³
!($
³   Statements                ³
!)4
³   Functions                 ³
!-7
³   Keystroke Macros          ³
$.C
³   Solvers                   ³
"'R
³   Unit Conversion           ³
""I
³   Sheet View                ³
!W^
³   Presentation View         ³
'@X
³   Top Level Commands        ³
"0X
³   Saving And Loading        ³
$V_
³   Troubleshooting           ³
!#)
ÀÄÄÄÄÄÄÄÄ Help Index ÄÄÄÄÄÄÄÄÄÙ
!"G
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  A  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Action Command, !
"]O
Apostrophe
'LT
Argument Variables
&/5
Arithmetic Expression
!RK
Arithmetic Operators
!SF
Arrow Keys
"8"
ASCII Storage, /SA
%6X
Assignment Statement
!)4
Associated List
&%,
Automatic Iteration
'3+
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  B  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Backsolving
"-C
Beep Signal
!%/
Blank Command, /B
$O:
Block Solve, /LB
%D;
Boolean Functions
!A\
Brackets, List Elements
"%0
Break Command, Ctrl-Break
"4-
Built-in Functions
!7&
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  C  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Calculation Unit
&"@
Cancel Command, F2
"4-
Character Strings
":B
Comment
&#^
Comment Sheet
$K'
Comparison Tolerance
'41
Complex Arithmetic
!KB
Complex Functions
!@[
Conditional Rule
!($
Conditional Statement
!)4
Configuration
%:9
Constants
!9>
CONTINUE Statement
!)4
Convergence Factor
"/W
Copy Command, /C
$O`
Cursor Functions
!FD
Curve Fitting
"<8
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  D  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Delete Command, /D
$PF
Delete files, /SD
%:^
Dialog Functions
!GC
DIF storage, /S#
%3F
Differential Equations
"<8
Differentiation
"<8
Direct Solver
")G
Disk Index
"9-
Display Unit
%`^
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  E  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Edit Field Command, F5
"7<
Editing Commands
"5`
Error Symbol, >
!%T
Esc Key
"8"
Examine Command, /E
$UX
EXIT Statement
!)4
Exponential Functions
!:G
Expression
!M.
Extended-ASCII Characters
!'C
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  F  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Field Box in PV
'C&
File Functions
!I?
Filling Lists
%;T
First Guess
&$A
FOR-NEXT Loop
!)4
Formatting Display Values
$HO
Function Keys, F1 - F10
"2*
Function Sheet
$*7
Function Syntax
!-7
Functions, Built-in 
!7&
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  G  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Get Element, /LG
%ED
Getting Started
$#"
Global Filename Characters
":"
Global Sheet
$K;
Goal Seeking
".K
Goto Command, :
#`?
GOTO Statement
!)4
Greek Letters
!'C
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  H  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Home, End Keys
"8"
Hyperbolic Functions
!;/
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  I  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
IF-THEN-ELSE Rule
!($
IF-THEN-ELSE Statement
!)4
Include Files
$[B
Inconsistent Rule
#_C
Indeterminate Expressions
!TM
Input Variables
&4)
Insert Command, /I and Ins
$QJ
Integration
"<8
Interactive Table
$GK
International Letters
!'C
Interpolation, Linear and Cubic
!1=
Invertible Mathematical Functions
!;W
Iterative Solver
"*Z
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  K  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Keystroke Macro Sheet
$+]
Keystroke Macros
$.C
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  L  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Label Statement
!)4
Library, TK
"<8
List Command, /L
%A%
List Elements, Square Brackets
"%0
List Fill Command, !
%;T
List Function, User-defined
!1=
List Functions, Built-in
!B]
List Sheet
$D!
List Solve, F10 or /LL
%C@
Lists
"$$
Loading Files, F3 or /S
$YN
Local Variables
!+1
Logarithmic Functions
!:G
Logical Expression
!P=
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  M  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Macros
$.C
Mathematical Constants
!8V
Mathematical Functions
!>:
Mathlook
'MC
Matrices
"<8
Memory Indicator
!]U
Menuing System, TK
'M#
Menus, User-Defined
!HB
Merge Files
$[B
Message Functions
!DF
Miscellaneous Functions
!JZ
Model Structure
!VI
Move Command, /M
$RN
Multi-Argument Functions
!B]
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  N  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Name Syntax
!&^
Navigation Commands
"4<
Non-Invertible Math Functions
!=2
Numeric Format Sheet
$HO
Numeric Value
!L)
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  O  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Optimization
"<8
Output Variables
&6E
Overdefined
"*8
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  P  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Parameter Variables
&,]
Percent Symbol, %
'K&
PgUp, PgDn Keys
"8"
Plot Box in PV
'E.
Plot Sheet
$D<
Pound Symbol, #
'LE
Precedence of Operators
!SF
Presentation View, PV
'@X
Print Command, /P
$SR
Problem Solving with TK
"'-
Procedure Function
!/I
Prompt/Error Line
!`(
Put Element, /LP
%E"
PV, Presentation View
'@X
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  Q  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Question Mark, ?
"#?
Quit Command, /Q
$V'
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  R  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Recursion
!/I
Relational Expression
!QP
Reset Command, /R
$U:
Result Variables
&1N
RETURN Statement
!)4
Rule Function
!0C
Rule Sheet
$)2
Rule Syntax
!($
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  S  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Save Entire Model, F4 or /SS
$\+
Save Lists
%1.
Save One Sheet, /S1
$]O
Save Text
$SR
Screen Composition
![7
Search With Goto Command
#`?
Select Sheet Command, =
$(!
Sheet View
!W^
Sheets
!W^
Significant Digits
!L)
Slash Commands, /
$L@
Solution Indicator
!]U
Solve Command, F9
"&"
Solvers
"'R
Special Keys
"8"
Spreadsheet Data Import/Export
%1.
Square Brackets, List Elements
"%0
StartUp Macro
$3`
Statement Syntax
!)4
Statistics
"<8
Status Field, Procedures and Macros
%\"
Status Field, Rule Function Subsheet
%[B
Status Field, Rule Sheet
%[B
Status Field, Variable Sheet
%ZL
Status Functions
!CE
Status Line, Presentation View
!_"
Status Line, Sheet View
!]U
Storage Command, /S
$V_
String Functions
!ED
Symbolic Expression
!O]
Symbolic Value
!L)
Syntax, Function
!-7
Syntax, Rule
!($
Syntax, Statement
!)4
Syntax, TK Name
!&^
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  T  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Table Box in PV
'F_
Table Sheet
$GK
Text Box in PV
'HO
Text Storage
%8[
Time Command, /T
$VK
TK Library
"<8
TK Menuing System
'M#
TK Names, Syntax
!&^
TK-Specific Functions
!CE
Top Level Commands
"0X
Trigonometric Functions
!9_
Troubleshooting
!#)
Typical Value
'5+
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  U  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Unconditional Rule
!($
Unconditional Statement
!)4
Unit Conversion
""I
Unit Sheet
$C$
User-defined Functions
$*7
User-Defined Menus
!HB
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  V  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Values
!L)
Variable Sheet
$(R
Version Command, /V
$VK
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  W  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Wildcards
":"
Window Command, /W
$%,
WKS Storage, /SW
%5/
ÍÍÍÍÍÍÍÍÍÍÍÍÍ  Z  ÍÍÍÍÍÍÍÍÍÍÍÍÍ
!!N
Zero-by-Zero Division
!UI
006
  Help File for TK SOLVER RELEASE 2.0

  Version 1.0, March 16, 1992

  Copyright 1991, 1992
  by Universal Technical Systems, Inc.
008
UNKNOWN
  If this Help screen appears, please note the
  context from which Help was called, including
  the current cursor location and any error
  messages or command prompts on the screen.
  Call 1-800-HELP-UTS and report this bug to
  Technical Support.
                      Thank you.
018
HOW TO USE HELP:
 If (More) is shown in the lower right corner,
  PgDn will show more information.
 The arrow keys will move you among the Related
  Topics, if any, or among the entries in the
  Index.  Press ÄÙ to see information on topic.
 From anywhere in TK press F1 to get help on
  the feature, error, or place that the cursor
  is covering (e.g. to get help on Rule Sheet,
  type = R F1).
  ÚÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ PgDn    ³ displays next screen          ³
  ³ PgUp    ³ displays previous screen      ³
  ³ L       ³ last topic displayed          ³
  ³ I       ³ displays the Index            ³
  ³ F1      ³ displays this screen          ³
  ³ Q or F2 ³ quits Help                    ³
  ÀÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
019
HOW TO USE THE HELP INDEX:
 From any Help screen press  I  to invoke the
  Help Index.  If you are anywhere else in
  TK, press  F1 I.
 The arrow keys  or  will move you along
  the index.  Pressing ÄÙ will select the
  topic covered by the cursor.
 The next time the Help Index is displayed, the
  cursor will be in the same place you left it
  the last time.
- You may read through the major topics listed
  in the box on the first page of Help Index to
  familiarize yourself quickly with TK.
 The Help commands apply to the Help Index with
  the following additions or modifications:
  -  PgDn or PgUp displays the next or the
                  the previous index page.
  -  Home or End  moves the cursor to the
                  beginning or end of the Index.
088
TROUBLESHOOTING

Most problems encountered when using TK are not
caused by the program doing something wrong or
by lack of TK skills on your part.  Trouble
usually comes from inconsistency in the
equations of the model and/or infeasible input
data or constraints.  In fact, TK may be useful
in sorting out mathematical and other fallacies.

EXAMPLE:
  How long would it take to repay a $90,000
  loan at 10% interest with a monthly payment of
  $500?  You can protect yourself from such
  absurd questions by letting TK check for the
  minimum payment when the term is infinite (it
  happens to be $750 in this case).

Here are a few troubleshooting hints:
 If a "red" error message indicates a solver
  error, move through the Rule Sheet and Func-
  tion Subsheets to find the culprit rule or
  statement marked by an error symbol > .
 Check the balance of number of equations,
  total number of variables, number of inputs
  and number of unknowns.  There is no way (TK
  or other) to solve for more unknowns than the
  number of independent equations.
 Mimic the Direct or Iterative Solver path to
  find out what unknown is evaluated by what
  equation.
 Use toggle switch  C  (in the rules' Status
  fields) to 'turn off' selected equations and
  isolate trouble spots.
 Add error terms to equations showing incon-
  sistency, and test them.
 Use the functions STATMSG, ERRMSG or DEBUG to
  monitor or terminate the solution process or
  pause at selected points (see the Help screen
  on "Message Functions").
 Use the Examine command (/E) to check on
  current values of variables and expressions.
 Beware of the hard-to-predict effects of
  conditional rules.
 Beware of singularities and indeterminacies
  (such as division of zero by zero which TK
  ignores), or use of extremely large or small
  values.
 Pay serious attention to the selection of
  guess variables and choice of guess values:
  - Try to use as few guess variables as
    possible.
  - In cases with one guess variable, introduce
    an error term, list solve and plot to find
    its dependency on intended guess variable,
    and choose a guess value from the vicinity
    of the root.
  - In cases with two or more guess variables
    use the 'divide and conquer' tactic, i.e.
    solve reduced problems with fewer guess
    variables and add constraints gradually.
 Consider two or more roots or solutions to
  your problem when applicable.
 Remember that the SQRT function returns a
  positive root although the negative one may be
  appropriate; likewise, inverse trigonometric
  functions return principal values.
 Try to avoid the iteration process straying
  into the regions of argument errors (easier to
  say than to do: it may take some luck and some
  sleuthing assisted by TK itself, i.e. turning
  the knowns and unknowns around, checking the
  error terms at the trouble spots, trying
  alternative ways, etc.).
 Beware of divergence and searching for a non-
  existent root when using the Iterative Solver.

  (See the Reference Manual or numerical
  analysis literature for information about
  inner workings of the Newton-Raphson
  procedure.)

TK is equipped with list solving, graphics,
procedure functions, interactive tables, etc.
for thorough diagnostics and efficient trouble-
shooting.  The TK Library also offers a variety
of alternatives to the built-in Iterative
Solver for analysis of ill-behaved problems.
019
BEEP

A beep indicates that execution of a command
and/or action resulted in an error condition,
or was terminated because you pressed F2 or
Ctrl-Break.

In most error situations TK displays an error
message, and provides additional explanation
when you press F1 for Help.  In some cases
(such as trying to move the cursor beyond the
sheet boundary or TK limits) TK just beeps.

When in Help, an illegal action produces a
deeper 'thud'.

You can create a beep with the rule or statement
    call beep()
in a function or keystroke macro subsheet.
012
ERROR SYMBOL  >

  in the Status field of the Rule Sheet and/or
  Rule Function or Procedure Function subsheet
  indicates which rule or statement was found in
  error.

USAGE:
 Move the cursor over the error symbol and
  see an error message on the Status line.
 Press F1 for a Help screen with an explana-
  tory message.
036
TK NAME SYNTAX:
  TK names are used as variable, function,
  keystroke macro, list, numeric format, plot,
  table and comment names, and, when preceded
  by an apostrophe, as symbolic values.

 TK names consist of up to 200 characters
  which may be letters, digits 0 through 9,
  or special characters @ # $ % _ ` ~
 As well as upper and lower case A through Z,
  international and Greek letters are also
  allowed (see last screen in this topic).
 TK names can neither begin with a digit nor
  contain blanks.
 TK names are case-sensitive, i.e. the names
  RESULT, Result, and result are all different
  names (names of built-in functions are not
  case-sensitive).
UNIT NAMES
  Unit names can contain any typeable
  characters, except for leading and trailing
  blanks (embedded blanks are allowed).

DUPLICATE NAMES
  The same name cannot appear twice in the Name
  field column of a sheet; however, the same
  name can be used for a variable, a list, a
  unit, a function, etc.








055
To produce an EXTENDED-ASCII character, hold
  down the Alt key, type the ASCII value on the
  numeric keypad, then release the Alt key.
  (Some printers cannot print some or all of
  these characters.)

INTERNATIONAL & GREEK letters legal in TK names:

  € 128    129   ‚ 130   ƒ 131   „ 132   … 133
  † 134   ‡ 135   ˆ 136   ‰ 137   Š 138   ‹ 139
  Œ 140    141   Ž 142    143    144   ‘ 145
  ’ 146   “ 147   “ 148   • 149   – 150   — 151
  ˜ 152   ™ 153   š 154     160   ¡ 161   ¢ 162
  £ 163   ¤ 164   ¥ 165

  à 224   á 225   â 226   ã 227   ä 228   å 229
  æ 230   ç 231   è 232   é 233   ê 234   ë 235
  ì 236   í 237   î 238
All the characters in this and the next screen
are illegal in TK names but are allowed in units,
comments, titles, plot labels and symbols, table
headings, and Presentation View text boxes.

   001    002    003    004    005    006
   007    014    015    016    017    020
   021    022    023    024    025    028
   029    030    031   › 155   œ 156    157
  ž 158   Ÿ 159   ¦ 166   § 167   ¨ 168   © 169
  ª 170   « 171   ¬ 172   ­ 173   ® 174   ¯ 175
  ° 176   ± 177   ² 178   Û 219   Ü 220   Ý 221
  Þ 222   ß 223   ï 239   ð 240   ñ 241   ò 242
  ó 243   ô 244   õ 245   ö 246   ÷ 247   ø 248
  ù 249   ú 250   û 251   ü 252   ý 253   X 254
  ÿ 255

NOTE:  You do not have to type leading zeros.
Characters 179-218 may be used to draw lines
and boxes.  (A number below always refers to
the character immediately to the left.)

 Ú218ÄÄÄÄÄÄÄÂ194ÄÄÄ¿191   É201ÍÍÍÍÍÍÍË203ÍÍÍ»187
 ³          ³      ³      º          º      º
 Ã195ÄÄ196ÄÄÅ197ÄÄÄ´180   Ì204ÍÍ205ÍÍÎ206ÍÍÍ¹185
 ³          ³      ³      º          º      º
 ³          ³      ³179   º          º      º186
 ³          ³      ³      º          º      º
 À192ÄÄÄÄÄÄÄÁ193ÄÄÄÙ217   È200ÍÍÍÍÍÍÍÊ202ÍÍÍ¼188

 ÚÄÄÄÄÄÒ210ÄÄÄ¿    ÉÍÍÍÍÍÑ209ÍÍÍ»    Ö214ÄÄÄ·183
 ³     º      ³    º     ³      º    º      º
 Æ198ÍÍÎÍÍÍÍÍÍµ181 Ç199ÄÄÅÄÄÄÄÄÄ¶182 Ó211ÄÄÄ½189
 ³     º      ³    º     ³      º
 ÃÄÄÄÄÄ×215ÄÄÄ´    ÌÍÍÍÍÍØ216ÍÍÍ¹    Õ213ÍÍÍ¸184
 ³     º      ³    º     ³      º    ³      ³
 ÀÄÄÄÄÄÐ208ÄÄÄÙ    ÈÍÍÍÍÍÏ207ÍÍÍ¼    Ô212ÍÍÍ¾190
021
RULE SYNTAX

An UNCONDITIONAL rule has the form
    expression = expression

EXAMPLES:
  1. a + b = c * d
  2. remainder = mod(a,b)
  3. theta = pi()^2 + r/2
  4. y * (cos(x) - sin(y)) = ln(x)
  5. x = 5
  6. (x,y) = ptor(r1,é1) + ptor(r2,é2)

A CONDITIONAL rule has the form
    IF logical_expr THEN uncond_rule
or
    IF logical_expr THEN uncond_r ELSE uncond_r
EXAMPLES:
  1. IF x>=0 THEN y=sqrt(x) ELSE msg='sq_err
  2. IF and(w>0, given('z)) THEN q=ln(w)*z
  3. IF evltd('a) THEN b=a*c+d
099
STATEMENT SYNTAX
  Statements define the sequence of actions
  to be executed by a procedure function or
  a keystroke macro.

UNCONDITIONAL statements (note that  { }  here
enclose optional components):
 An ASSIGNMENT statement has the form
      variable_name := expression
  or
      list_element := expression
 A GOTO statement has the form
      goto label_name
 A LABEL statement has the form
      label_name: {statement}
 A RETURN statement has the form
      return
 A CALL statement has the form
      CALL function_name({in_args}{;out_args})
 A FOR-NEXT loop construct has the form
      FOR var_name:= expr1 TO expr2 {STEP expr3}
        {statements}
      NEXT var_name
  where expr1 and expr2 must evaluate to numeric
  values and the optional expr3 must evaluate to
  a non-zero numeric value (defaults to 1).  The
  optional statements inside the loop can
  include other for-next loops.
 A CONTINUE statement has the form
      CONTINUE {variable_name}
  and takes control to the NEXT statement of the
  current loop, or to the NEXT statement of a
  loop at a higher level specified by the
  optional variable_name.
 An EXIT statement has the form
      EXIT {variable_name}
  and takes control to the statement after the
  NEXT statement of the current loop, or to the
  statement after the NEXT statement of a loop
  at a higher level specified by the optional
  variable_name.

EXAMPLES:
  1. r:=sqrt(x^2+y^2)      2. 'x[i][j]:= 0
  3. RETURN                4. Loop:
  5. GOTO loop_end         6. loop_end: NEXT i
  7. CALL quicksort('Age,'Height,'Weight;'Fact)
  8. FOR i:= j TO k
       x[i]:= (k-j)*i
     NEXT i
  9. (x,y) = ptor(r1,é1) + ptor(r2,é2)

CONDITIONAL statements:
  IF logical_expr THEN uncond_st
  IF logical_expr THEN uncond_st ELSE uncond_st

EXAMPLES:
  1. IF x >= 0  THEN  y := sqrt(x)
  2. IF evltd('a) THEN b:=a*c+d
  3. IF and(w>0,given('z)) THEN q:=ln(w)*z
  4. IF Shape='Circle THEN Area:=PI()*Radius^2
  5. IF mod(k,2) THEN x:=2^k
  6. IF abs(x-y)/max(abs(x),abs(y))<1E-6 return
  7. IF log(x)>a THEN y=0 ELSE y='unknown
  8. IF x[i][j]=0 THEN return ELSE goto loop
  9. IF evltd('x,1,0) THEN y=x^2 ELSE y=z^2
 10. IF mode='cart THEN (x,y)=ptor(r,é)

EXAMPLE Mixing All Types:

  CALL blank('fact)     ; call
  j=1                   ; assignment
  FOR i=1 TO length('p) ; begin for-next loop
    p='p[i]             ; assignment
    more:               ; label
    (q,r)=divide(n,p)   ; assignment
    if q<p EXIT         ; conditional exit
    if r>0 CONTINUE     ; conditional continue
    'fact[j]=p          ; assignment
    j=j+1               ; assignment
    n=q                 ; assignment
    GOTO more           ; goto
  NEXT                  ; end for-next loop
  if n>1 then 'fact[j]=n; conditional assignment
  RETURN                ; return

NOTES:
 Blank Statement fields are allowed.
 Any statement may be labeled.  Statement
  fields with labels alone are allowed.
 Label names have the same restrictions as TK
  names, except they may begin with digits.
 The keyword THEN may be omitted before
  CONTINUE, EXIT, GOTO, or RETURN.
 You may use  =  instead of  :=  as the
  assignment operator in assignment statements
  and FOR-NEXT loop constructs.
 Keywords are not case sensitive, e.g. call,
  Call, and CALL are equivalent.
016
LOCAL VARIABLES:
  Any variable name declared in the Argument or
  Result Variables field is local to a rule
  function, i.e. its name is only known within
  the rule function.  A variable name not
  declared in the Argument or Result Variables
  fields is also local, but neither its name nor
  its value is known outside the rule function.

  Similarly, Input Variables, Output Variables
  and undeclared variables are local within a
  procedure function.

  To examine the value of a local variable, use
  the Examine command (/E) while the cursor is
  somewhere in the function subsheet.
057
FUNCTIONS
  are either BUILT-IN or USER-DEFINED.

For built-in functions refer to the related
topic "Built-in Functions" below.

USER-DEFINED functions fall into 3 categories:
 List functions.
 Rule functions.
 Procedure functions.

Both built-in and user-defined functions can be
referred to or called.



A FUNCTION REFERENCE has the general form:

   function_name(arguments)

The number and types of arguments depend on the
function.

EXAMPLES:
  1. pi()
  2. ln(x)
  3. max(a,b,c,d)
  4. given('a,b*c,0)
  5. salary=calc('John)  -  user-defined
     function  calc  referred to in a rule
  6. salary:=calc('John)  -  user-defined
     function  calc  referred to in a statement
In TK a CALL to a built-in or user-defined
function constitutes a rule or a statement by
itself.  It has the general form:

  CALL function_name(args1;args2)

where args1 represent values passed either way
to/from Argument Variables (in rule functions)
or to Input Variables (in procedure functions);
args2 represent values passed either way
to/from Result Variables (in rule functions) or
from Output Variables (in procedure functions).

EXAMPLES:
  1. call initialize()  -  rule or procedure
     function may have no arguments.
  2. call sort(listA,listB)  -  procedure
     function with two arguments representing
     list with raw data and list with sorted
     elements.  There are input arguments only.
  3. call stat_char(;mean,sd,vc)  -
     procedure function with output arguments
     only; it returns statistical
     characteristics of a list of values
     specified in the procedure itself.
054
PROCEDURE FUNCTIONS
  are, in essence, sequential programs written
  in a TK programming language which supports
  branching and looping.

  Values are passed to and from a procedure
  function by a call statement, which is a
  separate rule or statement, or by a function
  reference, which is an expression within a
  rule or statement.

  Unlike rule functions, procedure functions
  may be used recursively, i.e. a procedure
  function may call or refer to itself directly
  or indirectly.  The limit of recursive
  nesting is 24 levels.


Syntax for a CALL to a procedure function from
a rule or statement:
     CALL function_name(in_args;out_args)

Syntax for a REFERENCE to a procedure function
from within an expression:
     function_name(in_args)

Because Input Variable (in_args) and Output
Variable (out_args) names are local and have no
meaning outside the Procedure Function Subsheet,
values for these variables are communicated to
and from the arguments in the function call or
reference according to position.  Arguments
preceding the optional semicolon are related to
Input Variables on the Procedure Function
Subsheet, while those following the semicolon
are related to Output Variables.
NOTES:
 Procedure functions use statements rather than
  rules.  See Related Topic "Statement Syntax".
 The number of Parameter and Input Variables
  can each be from zero to twenty.
 The number of Output Variables can also be
  from zero to twenty if the procedure function
  is called, but can only be one or two if the
  function is referred to.
 The values of variables linked to Input and
  Parameter Variables must be known before the
  function can be executed.
 The Direct Solver cannot backsolve a procedure
  function, i.e. solve for an unknown variable
  linked to an Input Variable.
 The Iterative Solver can backsolve a procedure
  function if a guess value is assigned to a
  variable linked to an Input Variable.
042
A RULE FUNCTION
  consists of a set of rules.  The syntax of
  these rules is the same as the syntax of rules
  in the Rule Sheet.

  Values are passed to and from a rule function
  by a call statement, which is a separate rule
  or statement, or by a function reference,
  which is an expression within a rule or
  statement.

Syntax for a REFERENCE to a rule function from
an expression:
     function_name(arg_exps)

Syntax for a CALL to a rule function from a rule
or statement:
    CALL function_name(arg_exps;res_exps)
Because the names of Argument and Result
Variables are local and have no meaning outside
the Rule Function Subsheet, values for these
variables are communicated to and from the
arguments in the function call or reference
according to position.  Arguments preceding
the optional semicolon are related to
Argument Variables on the Rule Function
Subsheet, while those following the semicolon
are related to Result Variables.

NOTES:
 The number of Parameter and Argument Variables
  can each be from zero to twenty.
 The number of Result Variables can also be
  from zero to twenty if the rule function is
  called, but can only be one or two if the
  function is referred to.
 Rule functions are backsolvable.
 The values of Argument or Parameter Variables
  do not have to be known before the function
  can be executed.  The values of Result
  Variables may be known beforehand and used
  for backsolving within the rule function.
016
LIST FUNCTION
  expresses the relationship between two lists:
  the Range list and the Domain list.

There are four mapping options:
 Table Mapping
 Step Mapping
 Linear Interpolation
 Cubic Interpolation

List functions can be referred to by:
 Rules in the Rule Sheet and Rule Function
  Subsheet.
 Statements in a Procedure Function Subsheet.
 Expressions in value fields, the Examine
  command, etc.
018
The TABLE MAPPING option
  is chosen when the function must map elements
  in the Range list to corresponding elements in
  the Domain list.

  The correspondence is strictly one-to-one:
  the argument must be equal to one of the
  elements in the Domain list in order for a
  corresponding value of the Range list to be
  returned.

NOTES:
 A list function with Table Mapping is
  invertible (i.e. you can specify an element
  in the Range list to find an element in the
  Domain list).
 A mixture of symbolic and numeric values is
  allowed in both the Range and Domain lists.
017
The STEP MAPPING option
  is chosen when the function must return a
  constant function value over a certain
  interval of argument values.  TK scans the
  Domain list from the top, finds the first
  interval which contains the argument, and
  returns from the Range list the single value
  associated with the whole interval (step) of
  the Domain list.

NOTES:
 A list function with Step Mapping is not
  invertible (i.e. you cannot specify an
  element in the Range list to find an element
  in the Domain list).
 A mixture of symbolic and numeric values is
  allowed in both the Range and Domain lists.
013
The LINEAR INTERPOLATION option
  finds the Domain list interval containing
  the specified argument, then calculates the
  function value by linear interpolation on the
  corresponding Range list interval.


NOTES:
 A list function with Linear interpolation is
  invertible (i.e. you can specify a function
  value in order to find an argument value).
 Only numeric values are allowed in the Range
  and Domain lists.
013
The CUBIC INTERPOLATION option
  is similar to the Linear option, except that
  calculation of the function value is based on
  a piecewise cubic approximation rather than a
  linear relationship.


NOTES:
 The Cubic Interpolation function is
  invertible (i.e. you can specify a function
  value in order to find an argument value).
 Only numeric values are allowed in the Range
  and Domain lists.
003
NUMERIC AND SYMBOLIC CONSTANTS:

  See Help on "Values".
009
MATHEMATICAL CONSTANTS:

  PI()  constant ã, 3.141592653589793
  E()   constant e, 2.718281828459045

EXAMPLE:
  circumference = 2*PI()*radius


009
EXPONENTIAL AND LOGARITHMIC FUNCTIONS:

  EXP(x)     number e raised to the xth power
  LN(x)      natural (base e) logarithm of x
  LOG(x)     common (base 10) logarithm of x

EXAMPLE:
  exp(-t^2)=phi

018
TRIGONOMETRIC FUNCTIONS:

  SIN(x)         ASIN(x)       R a d i a n s
  COS(x)         ACOS(x)       ÍÍÍÍÍÍÍÍÍÍÍÍÍ
  TAN(x)         ATAN(x)
  ATAN2(y,x)  (4-Quadrant arc tangent of y/x)

  SIND(x)        ASIND(x)      D e g r e e s
  COSD(x)        ACOSD(x)      ÍÍÍÍÍÍÍÍÍÍÍÍÍ
  TAND(x)        ATAND(x)
  ATAN2D(y,x)  (4-Quadrant arc tangent of y/x)

EXAMPLES:
  1. y - y0 = sin(x)/x
  2. a/sind(à)=b/sind(á)
NOTE:
  The inverse trigonometric functions return
  principal values.
009
HYPERBOLIC FUNCTIONS:

  SINH(x)        ASINH(x)
  COSH(x)        ACOSH(x)
  TANH(x)        ATANH(x)

EXAMPLE:
  (y1-y0)*k + 1 = COSH(k*(x1-x0))

009
OTHER INVERTIBLE MATHEMATICAL FUNCTIONS:
  SQRT(x)       positive square root of x
  ROOT(x,n)     nth root of x
  ERF(x)        error function
  GAMMA(x)      gamma function,
                x-1 factorial for integer x>0
EXAMPLES:
  1. ROOT(-512,3)   returns -8
  2. GAMMA(6)   returns 120
018
NON-INVERTIBLE MATHEMATICAL FUNCTIONS:
  SGN(x)       -1 if x < 0, 0 if x=0, 1 if x > 0
  ABS(x)       absolute value of x
  INT(x)       integer part of x
  ROUND(x)     nearest integer to x
  ROUND(x,y)   nearest multiple of y to x
  CEILING(x)   smallest integer ò x
  FLOOR(x)     largest integer ó x
  STEP(x1,x2)  1 if x1 ò x2, 0 if x1 < x2
  MOD(x1,x2)   remainder of x1/x2
  DIVIDE(x1,x2)  returns two values:
                 int(x1/x2) and mod(x1,x2)
  BESSEL(kind,order,x)  Bessel function of the
                 first or second kind, of the
                 given order, of x
EXAMPLES:
  1. CEILING(-9.7)  returns -9
  2. FLOOR(-9.7)    returns -10
021
MATHEMATICAL FUNCTIONS:

TK Solver offers a generous complement of
mathematical functions (see the appropriate
Help topics).

Any mathematical or special function which is
not among the built-in functions may be created
as a user-defined function.

EXAMPLES:
  1. The cumulative distribution function for a
     normal distribution may be represented by
     a rule function with a single rule,
       CDF = (1+erf((x-m)/sd/sqrt(2)))/2
     (where  m  and  sd  are, respectively,
     mean value and standard deviation)
  2. The greatest common divisor GCD of two po-
     sitive numbers, A and B, may be expressed
     as a recursive procedure function gcd:
  if B>0 then GCD:=gcd(B,mod(A,B)) else GCD:=A
036
COMPLEX FUNCTIONS:

  POWER((x1,x2),x3)
    calculates a complex number equal to the
    complex number (x1,x2) raised to power x3
  RE((x,y))
    returns the real component, i.e. the first
    value, of an expression that evaluates to
    a paired value of the form (x,y).
  IM((x,y))
    returns the imaginary component, i.e. the
    2nd value, of an expression that evaluates
    to a paired value of the form (x,y).
  PTOR(r,é)
    calculates a complex number in rectangular
    form equivalent to the complex number in
    polar form where  r  is the radius and  é
    is the angle in radians
  PTORD(r,é)
    same as PTOR, except  é  is in degrees
  RTOP(x1,x2)
    calculates a complex number in polar
    form equivalent to the complex number in
    rectangular form where x1 is the real part
    and x2 is imaginary part
  RTOPD(x1,x2)
    same as RTOP, except the angle returned is
    in degrees

EXAMPLES:
  1. POWER((5,2),3)  returns (65,142)
  2. RE((r,s)*(u,v))  returns -5 if r=1, s=2,
                      u=3, v=4
  3. PTORD(2,60)  returns (1,1.732050807568877)


036
BOOLEAN FUNCTIONS:

  NOT(x)              EQV(x,y)
  AND(series)         IMPLY(x,y)
  OR(series)

where:
  x,y     are logical expressions
  series  is any number of logical expressions

EXAMPLES:
  1. if or(k<3,k>5) then a=4*k
  2. if and(not(w+y>15),w+y>0) then z=ln(w+y)
  3. Answer = eqv(eqv(Tiger,Liar),not(Liar))





    DEFINITIONS (1 = true, 0 = false):

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄ¿
    ³     x      ³  1  ³  1  ³  0  ³  0  ³
    ³     y      ³  1  ³  0  ³  1  ³  0  ³
    ÆÍÍÍÍÍÍÍÍÍÍÍÍØÍÍÍÍÍØÍÍÍÍÍØÍÍÍÍÍØÍÍÍÍÍµ
    ³ NOT(x)     ³  0  ³  0  ³  1  ³  1  ³
    ³ AND(x,y)   ³  1  ³  0  ³  0  ³  0  ³
    ³ OR(x,y)    ³  1  ³  1  ³  1  ³  0  ³
    ³ EQV(x,y)   ³  1  ³  0  ³  0  ³  1  ³
    ³ IMPLY(x,y) ³  1  ³  0  ³  1  ³  1  ³
    ÀÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÙ





072
LIST and MULTI-ARGUMENT FUNCTIONS:

  MAX(list)  or  MAX(series)
    returns largest value from list or series
  MIN(list)  or  MIN(series)
    returns smallest value from list or series
  SUM(list)  or  SUM(series)
    returns sum of values in list or series
  COUNT(x)
    returns the number of non-blank elements in
    the list x
  LENGTH(x)
    returns the number of elements in the
    list x, including blanks
  call BLANK(x)
    blanks the entire list x
  call BLANK(x,i)
    blanks the ith element in the list x
  call BLANK(x,i,j)
    blanks the ith through jth element in the
    list x
  call DELETE(x)
    deletes the list x from the List Sheet
  call LISTCOPY(x,y)
    copies elements of list x to list y
  call LISTCOPY(x,y,i)
    copies list x starting from the ith element
    to the list y
  call LISTCOPY(x,y,i,j)
    copies ith through jth elements of list x
    to list y
  call LISTCOPY(x,y,i,j,k)
    copies ith through jth element of list x
    to list y, starting at the kth element of y
  CHECK(x)
    returns 1 if list x exists, 0 if not
  MEMBER(x,list)  or  MEMBER(x,series)
    returns  k  if the kth element of list or
    the kth value in series is the first to
    match x; returns 0 if there is no match
  DOT(x,list)  or  DOT(x,series)
    dot product of values in list x with values
    in list or in series
  POLY(x,list)  or  POLY(x,series)
    returns value of a polynomial function of x
    with coefficients specified by the list or
    series of values (highest-degree coefficient
    first, coefficients of 0 for missing terms)
  NPV(x,list)  or  NPV(x,series)
    returns net present value of the payments in
    the list or series, assuming interest rate x



EXAMPLES:
  1. max(a,34,b,c,40)
       returns 43 if a=12, b=35, and c=43
  2. poly(x,1,0,0,1)  returns 9 if x=2
  3. poly(x,'y)       returns 143 if x=10 and
       the list y contains the elements {1,4,3}
  4. call LISTCOPY(x,y,i)   copies elements from
       list X, starting from the 10th element,
       to the list Y, if x='X, y='Y, i=10
  5. call LISTCOPY(x,y,i,j)   copies 5th through
       8th elements of list 'X to 1st through
       4th elements of list 'Y, if x='X, y='Y,
       i=5, j=8
  6. call LISTCOPY(x,y,i,j,k)   copies 5th
       through 8th elements of list 'X to 5th
       through 8th elements of list 'Y,
       if x='X, y='Y, i=5, j=8, k=5

072
TK-SPECIFIC FUNCTIONS:

  APPLY(fname,args)
    returns the value of function  fname  of
    argument(s) args
  ELT()
    returns the element number of current
    instance when list solving or block solving
  ELT(x,n)
    returns the value of the nth element of the
    list x
  ELT(x,n,expr)
    returns the value of the nth element of the
    list x, or, if blank, the expression  expr
  PLACE(x,n)
    places a value into the nth element of the
    list x
  VALUE(var) returns the value of variable var
  SOLVED()
    returns 0 when iteration with the Iterative
    Solver is in process, and 1 when iteration
    is finished or Direct Solver is used; may
    serve for triggering some action only after
    the iteration is completed
  VECELT(var,n)
    returns the number of occurrences of the
    variable var in the rule on the nth line of
    the Rule Sheet. May be used for constructing
    Variable-Equation Coincidence (VEC) matrix.

Status functions, short form:
  GIVEN(var)  returns 1 if the variable var is
              assigned an input value, 0 if not
  EVLTD(var)  returns 1 if the variable var was
              evaluated
  KNOWN(var)  returns 1 if the variable var is
              known (i.e. given or evaluated)
  where  var  is an expression that evaluates
  into a symbolic value representing a variable
  name; in the simplest case it may be just a
  variable name preceded by an apostrophe.

EXAMPLES:
  1. VALUE(a)   (returns 12 if a='R and R=12)
  2. if KNOWN('phi) then L=phi*rho
     (the L=phi*rho rule applies if phi is
      known)

Status functions, long form:
  GIVEN(vars,expr1,expr2)
  KNOWN(vars,expr1,expr2)
  EVLTD(vars,expr1,expr2)
  where  vars  means one or more expressions
  that evaluate into variable names.
If all the vars are given, known or evaluated,
the functions return expr1; else, they return
expr2.

EXAMPLES:
  1. Rate = GIVEN('Rate,Rate,10)
    (if Rate is assigned an input value, the
     rule represents the trivial identity
     Rate=Rate; else, the rule assigns a default
     value Rate=10)
  2. L = KNOWN('phi,phi*rho,0/0)
    (if phi is known the rule becomes L=phi*rho;
     if phi is not known the rule is ignored)
  3. if GIVEN('é,'à,'á,1,0) then L=é*R
    (rule applies only if all é,à,á are given)



054
MESSAGE FUNCTIONS:

  BOXMSG(sourcename{,sarg{,narg1{,narg2}}})
    Halts execution and displays text or
    symbolic values from  sourcename  in a box
    located according to  narg1  and  narg2
    (from leftmost/uppermost 0 to rightmost/
    lowermost 1; defaults are .5 for middle of
    the screen).  The argument  sourcename  may
    name either a comment or a list, with the
    comment preferred if both exist.  If present,
    sarg  becomes a title in the upper border.
    A Comment Subsheet may have lines consisting
    of the characters  >>>  to divide the text
    into pages accessed in the box by PgDn/PgUp.
    Similarly, list elements consisting of the
    string  ">>>"  behave as page separators.

  STATMSG(arg1,arg2,...)  Evaluates the
    arguments (any constants or expressions)
    and displays them on the Prompt/Error line
    until replacement by another message or
    completion of solution.
  ERRMSG(arg1,arg2,...)  Terminates the solution
    with a beep, evaluates the arguments (any
    constants or expressions), and displays the
    arguments on the Prompt/Error line.
  PAUSEMSG(arg1,arg2,...)  Pauses the solution,
    evaluates the arguments (any constants or
    expressions), and displays the arguments on
    the Prompt/Error line.  Pressing any key
    resumes the solution.  A call to PAUSEMSG is
    active on every pass of the Direct Solver.
  DEBUG(arg1,arg2,...)  Same as PAUSEMSG.  Use
    DEBUG for developmental debugging, PAUSEMSG
    for permanent part of the model.

EXAMPLES:
  1. call STATMSG('OFN,OFN,'at,'step,ns)
     may display  OFN 3.456 at step 9
  2. if mod(n,2) then call ERRMSG('Wrong,'n)
     displays error message  Wrong n  if
     n is not even
  3. call DEBUG('qp,qp) will display  qp 1.23
     when the variable qp is known and equal to
     1.23, and  qp ???  when it is not known.








054
STRING MANIPULATION FUNCTIONS:

  STRLEN(string)
    Returns the number of characters in the
    string argument.
  STRINDEX(string1,string2)
    Returns an integer representing the starting
    position of string1 within string2, or 0 if
    string1 is not a substring of string2.
  LEFT(string,n)
    Returns the  n  leftmost characters of the
    string argument.
  RIGHT(string,n)
    Returns the  n  rightmost characters of the
    string argument.
  MID(string,n1,n2)
    Returns the  n2  characters of the string
    argument, starting at n1.
  JOIN(args)
    Returns a single string composed of the
    arguments, which may be strings and/or
    integers.














STRING CONVERSION FUNCTIONS:

  EVAL(string)
    Returns the result of evaluating the string
    using local values of variables from the
    function or macro from which it is called.
  CEVAL(string)
    Returns the result of evaluating the string
    using local values of variables from the
    function or macro in which the cursor is
    currently located; similar to /EE.
  GEVAL(string)
    Returns the result of evaluating the string
    using global values of variables.
  STRING(arg)
    Returns a string representation of  argument.


054
CURSOR CONTENTS/POSITION FUNCTIONS:

  CONTENTS()
    Returns a string containing the contents
    of the field under the cursor (Sheet View)
    or of the line or field under the block
    cue (Presentation View).
  STATLINE()
    Returns a string containing the contents
    of the Status Line.
  ERRLINE()
    Returns a string containing the error
    message on the Prompt/Error line.
  CURSHEET()
    Returns a symbolic value naming the sheet
    ('V, 'F, ...) or subsheet ("v:height",
    "f:Simpson", ...) on which the cursor
    is currently located in the Sheet View.
    Value is prefixed with PV if Presentation
    View is on screen ("PV:f:Romberg").  Value
    is suffixed with IT if cursor or block cue
    is in an interactive table or a Field Box
    linked to an IT ("t:Results:IT").
  CURFIELD()
    Returns a symbolic value representing the
    cursor position in Sheet View or the block
    cue position in Presentation View.
  CURROW()
    In Sheet View, returns the row number of the
    field under the cursor.  In the Presentation
    View, returns the line number of the block
    cue (relative to top line of PV).
  CURCOL()
    In Sheet View, returns the column number of
    the field under the cursor.  In the Presen-
    tation View, returns the column number of
    the block cue (relative to left edge of PV).
  CURSIZE()
    Returns the width and height of the Sheet
    View field or the Presentation View box
    currently containing the cursor.













018
DIALOG FUNCTIONS:

  QUERY(string)
    Displays the string as the heading of a
    confirmation menu.  Returns 1 or 0 if
    user chooses Yes or No, respectively,
    from menu.
  PROMPT(string1{,string2})
    Displays string1 as prompt on Prompt/Error
    line.  Optional string2 serves as default
    response.
  MENU(string1,string2|args)
    Displays a TK-style selection menu with
    title  string1  and returns the line number
    of the selected item.  Items to select come
    from list  string2  or series  args.  (For
    more information, bring up the Help Index
    and select the topic "User-Defined Menus".)
036
USER-DEFINED MENUS

A user-defined menu can display up to 20
options.  Syntax for creating a menu is
  MENU(string1,string2|args)
where string 1 specifies the menu heading and
the menu choices are specified either by the
elements in the list named by string2 or by
args, a series of expressions evaluating into
numeric or symbolic values.

A blank element in the argument list or an empty
string ("") in the series of expressions will
produce a bar separator in the menu.

Selection is performed by typing the first
letter of the item or by highlighting the item
with the cursor and pressing ÄÙ.  The element
or position number of the item is returned.

Example:

  Item=MENU("Proceed?",'Yes,'No,"",'Maybe)

will produce the menu
                         ÚÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
                         ³ Proceed?    ³
                         ÔÍÍÍÑÍÍÍÍÍÍÍÍÍµ
                             ³ Yes     ³
                             ³ No      ³
                             ÃÄÄÄÄÄÄÄÄÄ´
                             ³ Maybe   ³
                             ÀÄÄÄÄÄÄÄÄÄÙ

after which typing Y, N or M will result in Item
receiving the value 1, 2 or 4, respectively.
018
FILE READ/WRITE FUNCTIONS

  ASCIIREAD(string)
    Loads the ASCII file named by  string  into
    TK lists.  (Similar to /SAL command.)  File
    extension assumed to be .ASC unless
    specified otherwise.
  ASCIIWRITE(string1,sargs)
    Saves TK lists named by  sargs  to ASCII
    file named by  string1.  (Similar to /SAS
    command.)  File extension assumed to be .ASC
    unless specified otherwise.

NOTE:
 The string must use double backslashes to
  represent backslashes in the path to the
  file, e.g.
   CALL ASCIIREAD("\\tk\\special\\prm10000.asc")
011
MISCELLANEOUS FUNCTIONS

  DATE()
    Returns the current system clock date as a
    symbolic value, e.g. "04-Jul-1992".
  TIME()
    Returns the current system clock time as a
    symbolic value, e.g. "18:43:32.58" for
    32.58 seconds past 6:43 pm.
  BEEP()
    Generates a beep.
034
COMPLEX ARITHMETIC:
  A complex number, a+bi, is represented as a
  pair of two real numbers or expressions
  enclosed in parentheses, (a,b) .

  When doing complex arithmetic, all operands
  in the rule or statement must be paired
  expressions.  If a function is used as an
  operand it must be two-valued.

EXAMPLES:
  1. (a,b)+(c,d) = (p,r)*(s,t)
  2. (y,yi):=(a,0)*power((x,xi),3)+(b,0)

NOTES:
 Four basic arithmetic operations (+ - * /)
  can be performed on paired operands.  The
  built-in function POWER must be used for
  exponentiation (see example  2  above).
 The rules of precedence of operators with
  paired expressions is the same as with simple
  operands.
 The TK backsolving feature fully applies to
  paired operands.  For instance, if a,b,c,d,s,t
  are known in the example  1  above, p and r
  will be evaluated.
 Rule functions or procedure functions used in
  conjunction with paired expressions must have
  two output or result variables.
  Example:
              (y,yi)=csin(x,xi)
  where csin is a user-defined rule function
  which calculates the sine value of a complex
  number and returns two result variables.
037
VALUES are either numeric or symbolic.

NUMERIC values are any real numbers in standard
  or exponential format with up to 16
  significant digits and non-zero absolute value
  between 1E-307 and 1E308, or zero.

EXAMPLES:
  1. 1.09092          3. 18437560000000000000000
  2. 6.672E-11        4. 0

SYMBOLIC values can be either valid TK names
  preceded by an apostrophe or generalized
  character strings surrounded by double quote
  marks.  A generalized character string can be
  empty, i.e.  "" ,  but an apostrophe must be
  followed by a TK name and cannot be empty.

EXAMPLES:
  1. 'x        2. 'Bank        3. 'a_row_1
  4. '_line    5. 'aj%d4       6. 'à
  7. "line d"  8. "a*j/d[4]"   9. "øK"

NOTE:
 A symbolic value used in a reference to a list
  element or as an argument specifying the name
  of a TK object (variable, list, etc.) may have
  either an apostrophe prefix or surrounding
  double quote marks.  The symbolic value must
  be a valid TK name in these situations, so the
  convention is to use the apostrophe.

  VALID:
    1. 'Xupper[i]      2. given('ab,'cd,1,0)
    3. "Xupper"[i]     4. given("ab","cd",1,0)
  NOT VALID:
    5. "X upper"[i]    6. given("ab,cd",1,0)
015
An EXPRESSION can be:
 a constant value.
 a variable name.
 a function reference.
 an arithmetic expression.
 a relational expression.
 a logical expression.
 a symbolic expression.
 a list reference.

EXAMPLES (in respective order):
1. 3409  or  'size    5. a >= 34
2. size               6. AND(a <= 34,b > a)
3. SQRT(speed)        7. GIVEN('Gas,Gas,'Oxygen)
4. (y2-y1)/(x2-x1)    8. 'x[i]
011
A SYMBOLIC expression can be:
 a symbolic value.
 an expression that evaluates into a symbolic
  value.

EXAMPLES:
  1. given('soup,'entree,'dessert,'OK,'Incomp)
  2. pet       (if  pet  equals, say, 'baboon)
  3. 'wood
  4. '$100_note
  5. 'DOG_and_PONY_show
011
A LOGICAL expression can be:
 a relational expression.
 a Boolean function.
 any other expression that evaluates into
  1 (true) or 0 (false).

EXAMPLES:
  1. x <= y
  2. or(x <= y,x >= w)
  3. given('x)
  4. abs(sgn(x))
018
A RELATIONAL expression
  has the form of two expressions linked by
  a relational operator.

  Relational operators are:
    =           equal
    <           less than
    >           greater than
    <=    =<    less than or equal to
    >=    =>    greater than or equal to
    <>    ><    not equal

  Relational expressions evaluate into numerical
  values 1 (true) or 0 (false).

EXAMPLES:
  1. x*y <= u*v
  2. animal <> 'dog
013
An ARITHMETIC expression can be:
 Two expressions linked by an arithmetic
  operator (+ - * / ^).  The constituent
  expressions (operands) must evaluate into
  numeric values.
 Any expression preceded by unary + or - .

EXAMPLES:
  1. a + b / (c * d)
  2. (b > 3) + a
  3. (a+b)^ln(x)
  4. exp(cos(x)+sin(y))
  5. -SIN(X)
029
ARITHMETIC operations
  are (in order of precedence):

  ^      exponentiation
  +  -   unary plus and minus
  *  /   multiplication and division
  +  -   addition and subtraction

NOTES:
 Operations at the same precedence level are
  performed from left to right, except for
  exponentiation operations, which are performed
  from right to left.
 When arithmetic and relational operators are
  combined, the relational operators have the
  lowest precedence level.
 The order of operations may be controlled by
  parentheses.
EXAMPLES:
  1. 2 + 3 * 4     evaluates into 14
  2. (2+3) * 4     evaluates into 20
  3. 4e150/2e180*3e200  evaluates into 6e170
  4. 4e150*3e200/2e180  causes exponent overflow
  5. 4 ^ 2 ^ 3     evaluates into 65536
  6. (4^2) ^ 3     evaluates into 4096
  7. 3 > 2 > 4     evaluates into 0
  8. 3 > (2>4)     evaluates into 1
  9. 2 < 3 * 4     evaluates into 1
  10. (2<3) * 4    evaluates into 4
026
INDETERMINATE expressions

When evaluating expressions, the values of
operands or function arguments may be available,
but the information they carry may be insuffi-
cient to determine the result.  There are nine
such situations in TK:

     x = 0/0
     1 = 1^x           0 = 0^x
     1 = x^0           x = 0^0
     x = atan2(0,0)    (a,x) = rtop(0,0)
     x = atan2d(0,0)   (a,x) = rtopd(0,0)

(The unknown  x  cannot be evaluated from any of
these equations.)


NOTES:
 TK ignores an indeterminate expression when
  encountered in a rule, and leaves the rule
  'unsatisfied'.
 If an indeterminate expression is encountered
  in a procedure function, the error condition
  "Unevaluated expression in procedure" occurs
  and the solution is terminated.
013
ZERO-BY-ZERO DIVISION

is one of nine indeterminate expressions that
are ignored when encountered by the solvers.

The 0/0 construct may be used as an alternative
to conditional rules.

EXAMPLE:
  a + b = GIVEN('x,'y,c*d,0/z)
  (if  x  and  y  are assigned input values then
  the rule is a+b=c*d; otherwise, the rule is
  a+b=0 if z<>0 or ignored if z=0)
041
TK MODELS
  serve as a base for problem solving in TK.

TK models consist of the following objects:

 Variables declared in the Variable Sheet with
  attributes defined in Variable Subsheets.

 Rules declared in the Rule Sheet.

 User-defined functions declared in the
  Function Sheet and defined in List, Rule or
  Procedure Function Subsheets.

 Keystroke macros declared in the Keystroke
  Macro Sheet and defined in Keystroke Macro
  Subsheets.

 Unit conversions defined in the Unit Sheet.

 Lists declared in the List Sheet and list
  values defined in List Subsheets.

 Plots declared in the Plot Sheet and defined
  in Line Chart, Bar Chart or Pie Chart Plot
  Subsheets.

 Tables declared in the Table Sheet and
  defined in Table Subsheets.

 Numeric formats in the Numeric Format Sheet
  with definitions in Numeric Format Subsheets.

 Comments declared in the Comment Sheet and
  defined in Comment Subsheets.

 Global settings in the Global Sheet.

TK models may also use the Presentation View
to display several different kinds of objects
simultaneously.
018
SHEETS and the SHEET VIEW
  Communication between the user and TK is
  conducted through the sheets.  Each sheet
  contains information on different TK objects
  which together form a TK model.

TK has eleven sheets:
 Variable Sheet        Rule Sheet
 Function Sheet        Keystroke Macro Sheet
 Unit Sheet            List Sheet
 Plot Sheet            Table Sheet
 Numeric Format Sheet  Comment Sheet
 Global Sheet

TK also has a Presentation View (PV), which
allows simultaneous viewing of several different
classes of TK objects (variables, rules,
functions, plots, etc.).
039
SCREEN COMPOSITION, Sheet View (SV)

The TK screen in the Sheet View consists of:
 MESSAGE AREA:  The top two lines, called
  the Status Line and the Prompt/Error Line.
 INFORMATION AREA:  Lines 3 through 24, may
  contain one or two windows with TK sheets.
  The windows may be split or unsplit using
  the Window command, /W, and the sheets may be
  changed using the Select Sheet command, = .
 BOTTOM PROMPT LINE:  A reminder of some of the
  most frequently used commands.  (May be turned
  on or off by a setting in the Configuration
  Editor.  See related topic for information on
  saving configuration settings.)

SCREEN COMPOSITION, Presentation View (PV)

The TK screen in the Presentation View
consists of:
 MESSAGE AREA:  The top line, called the
  Status Line.
 INFORMATION AREA:  Lines 2 through 24 of the
  visible screen, scrollable over as many as
  32000 lines in the PV.  See related topic
  "PV" for details on navigating around the
  PV and creating the various kinds of boxes
  displayed in the PV.
 BOTTOM PROMPT LINE:  Similar to Bottom Prompt
  Line in the Sheet View, but also carries
  macro and operational control messages when
  appropriate.
NOTES:
 Ctrl-PgUp or Ctrl-UpArrow increases the size
  of the lower window and decreases the size of
  the upper window in the Sheet View.  Ctrl-PgDn
  or Ctrl-DownArrow have the opposite effect.
 Press the \ key to bring up the Presentation
  View, the Esc key to return to the Sheet View.
027
The STATUS LINE in the Sheet View
  is the top line on the TK screen.

It contains the following information (from left
to right):

 Information related to the current cursor
  position:
  -  row number and field label.
  -  field name.
  -  field contents (e.g. full internal
     representation of a numeric value
     with up to 16 significant digits).

 MEMORY INDICATOR, approximate amount of
  unused memory in kilobytes (K).


 SOLUTION INDICATOR, shows the status of the
  Variable Sheet:
  OK  indicates no change in the Variable or
      Rule Sheets or any Function or List Sub-
      sheet since the last solution, i.e. the
      displayed output values may be trusted,
  F9  there has been a change, and the Solve
      command  F9  should be invoked to restore
      the integrity of the sheets' contents.
016
The STATUS LINE in the Presentation View
  is the top line on the TK screen.

It contains the following information (from left
to right):

 The title "Presentation View".
 The row, column, and page number of the
  current location of the block cue.

NOTES:
 Messages that would appear in the Prompt/Error
  Line in the Sheet View are displayed on the
  Status Line in the Presentation View.
 There is no Memory Indicator or Solution
  Indicator in the Presentation View.
011
The PROMPT/ERROR LINE
  is the second line on the TK screen.

It serves for:
 Displaying error messages and prompts.
 Indicating which solver is being used.
 Entering names of files to be loaded, saved
  or deleted.
 Specifying variables or expressions to be
  evaluated with the /E commands.
 Typing in other command-related information.
032
UNIT SHEET FIELDS

FROM field: Contains the first unit.

TO field: Contains the second unit.

MULTIPLY BY field: Contains the unit conversion
  factor, i.e. number of second units equivalent
  to a single first unit.  If left blank, a
  factor of 1 is assumed.

ADD OFFSET field: Contains the value in the
  second unit equivalent to a value of zero in
  the first unit.  If left blank, an offset of
  0 is assumed.

COMMENT field: serves for commenting on units,
  showing their full names, or other informa-
  tion; ignored by unit conversion mechanism.

NOTE:
  Multiply By and Add Offset fields accept any
  valid numeric value or expression.

EXAMPLES:
  1. Conversion from degrees Celsius to degrees
     Fahrenheit is defined by a conversion
     factor of 1.8 (may be entered as 9/5) and
     an offset value of 32.
  2. Conversion from radians to degrees may be
     defined by a conversion factor of 180/pi()
     which evaluates into  57.2957795131 .
035
The UNIT CONVERSION
  feature in TK works as follows:

 Each variable and list may be optionally
  assigned a Calculation unit and a Display
  unit.
 All calculations are performed in Calculation
  units and the values are displayed in Display
  units.
 The Display units are defined in the Unit
  fields in the Variable and List Sheet.
 A Display unit entered into a blank Unit field
  also becomes a Calculation unit.
 Both Calculation and Display units may be
  changed in the appropriate fields in the
  Variable and List Subsheets.
 When the Display unit is changed the input or
  output value is converted instantaneously.
 The conversion is performed according to the
  unit conversion definitions in the Unit Sheet.

NOTES:
 The assigned Calculation units must be
  consistent with the usage of the variables
  and/or list elements in the rules.
 The unit conversion may be defined indirectly.
  For instance, if the conversions ft/in, in/cm
  and m/cm are defined, the conversion between
  ft and m is automatically defined.
 If the conversion between the Calculation
  unit and the desired Display unit is not
  defined in the Unit Sheet, a question mark (?)
  appears in front of the value.


015
QUESTION MARK, ?

A puzzling event for the uninitiated is the
appearance of  ?  in front of an input or
output value in the Variable Sheet.  It simply
indicates an error in the unit specification or
a missing unit conversion (check the Unit
Sheet!).

NOTE:
 With the advent of TK Solver Release 2.0,  ?
  no longer invokes Help and Ctrl-Q is no longer
  required to type the  ?  character in a field.
  Use F1 to invoke Help, and simply type ? (i.e.
  Shift-/) in a field.
093
LISTS

Unlike a variable, which can have only a single
value, a list represents a series of values
(numeric or symbolic) called list elements.  A
list is referred to by its name.  A list element
is referred to by a list name and an element
number, which must be a positive integer
greater than zero.

Lists are used in TK in several ways:
 They may be associated with variables and used
  in list solving or block solving, i.e. they
  supply the values of input variables and
  receive the values of output variables at each
  instance of list or block solving.
 They represent argument and function values
  defining the user-defined list functions.
 They provide a framework for communicating
  data to and from TK in DIF, WKS or ASCII
  format.
 In addition to the above, they may simply
  represent ordered sets of values used and
  stored temporarily or permanently within
  the models.

Duplicate list names are not allowed.  A list
and a variable may have the same name, but that
does not imply any relation between the two --
except when, say, a list  abc  is created by
typing L in the Status field of the variable
abc.

On various sheets and subsheets the lists are
referred to by their names, which must be TK
names.  In expressions and as function arguments
the lists are referred to by their names in the
form of symbolic values.  In that case their
names, as with any symbolic values, must be
preceded by an apostrophe.  For example:
  call blank('abc)  has the same effect as
  call blank(uv)    if the value of variable
                    uv  is  'abc .

For a simpler and clearer way of referring to
list elements, see "Square Brackets."

List elements are not "protected", i.e. the
values of list elements may be overwritten any
time during a single action of the Direct
Solver.  (Note: Variables which appear in rules
are "protected"; their values, once assigned by
user or by Solvers, cannot be overwritten.)

To see how lists are used in list solving see
the Help topic "List Solve."

A new list name is automatically added to the
List Sheet when you:
 Type L in the Status field on the Variable
  Sheet or a Variable Subsheet.
 Assign a list name to the Associated List
  field on a Variable Subsheet.
 Assign a list name as the domain or range on
  the List Function Subsheet.
 Assign a list name in the X-Axis, Y-Axis or
  or any similar field on a Plot Subsheet.
 Enter a list name in the List field on a
  Table Subsheet.

A new list is also generated when a rule or a
statement places or assigns a value to a non-
existing list.  For example, if a list  abc
did not exist, a rule place('abc,2)=pi() or a
statement 'abc[2]:=pi() creates it and places or
assigns 3.14159... to its 2nd element.

If you need to use a multi-dimensional array
consisting of lists of lists of lists ..., and
the master and subordinate lists had not been
set up beforehand, TK will create the whole
hierarchy upon executing the assignment
statement.  For example, execution of a
statement
            'abc[i][j][k]:= i*j*k
when i=4, j=6 and k=5 will create a master list
abc  and place the name of a subordinate list,
'abc#4, in its 4th element; then a list  abc#4
will be created, and the name of a lower level
subordinate list  'abc#4#6 will be placed in its
6th element; finally, a list  abc#4#6  will be
created and a value of 120 (=i*j*k) will be
placed in its 5th element.
051
SQUARE BRACKETS

Square brackets offer a simpler and clearer way
of referring to list elements.  For example the
rules
  place('xyz,3)^2 = 8*elt('xyz,2)
  elt('xyz,2) + elt('xyz,3) = 2*place('xyz,1)
may be replaced by
  place('xyz,3)^2 = 8*'xyz[2]
  'xyz[2] + 'xyz[3] = 2*place('xyz,1)

that is,
  elt(list_name,element_number)
may be replaced by
  list_name[element_number]
where
  list_name is a symbolic value or a variable
  name equal to a symbolic value representing a
  name of an existing list.
  element_number is an expression that evaluates
  into a positive integer referring to a
  non-blank element.

A PLACE function reference may also be replaced
by a bracket construct when used on the left
hand side of an assignment statement in
procedure functions.  Thus the two rules from
above, rewritten as assignment statements, may
be simplified to
  'xyz[3]:= sqrt(8*'xyz[2])
  'xyz[1]:= ('xyz[2] + 'xyz[3])/2

The PLACE function cannot be replaced by bracket
constructs in rules, i.e. in the Rule Sheet and
Rule Function Subsheets.

The ELT function must be used when there is a
chance that the referred-to list element is not
available and a third function argument is
supposed to be returned.  The following example
illustrates this point:

                 List xyz exists,     List xyz
                   kth element        does not
               equals 49   is blank    exist
 ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ
   'xyz[k]         49       fails       fails
 elt('xyz,k,0)     49         0         fails

(Failure involves an error condition and
message  ELEMENT: Argument error.)
018
The SOLVE command (F9)
  solves a model with the Direct or Iterative
  Solvers and displays the results in the Output
  fields of the Variable Sheet.

USAGE:
 Assign the known values of input and/or guess
  values for variables in the Variable Sheet.
 Press F9.

NOTES:
 TK applies the Iterative Solver only if you
  have provided guess values for one or more
  variables (by typing G in the Status field on
  the Variable Sheet or entering a First Guess
  value in the Variable Subsheet).
 The Solve command may be used from anywhere
  in TK.
013
PROBLEM SOLVING WITH TK
  typically involves three stages:

  1. Setting up a TK model either by loading
     appropriate TK file(s) or by developing
     the model from scratch (i.e. typing the
     rules into the Rule Sheet).

  2. Formulating the problem (i.e. assigning
     appropriate input or guess values to
     selected variables in the Variable Sheet).

  3. Solving the model by pressing F9.
082
SOLVERS

TK employs two major mechanisms for solving
mathematical problems numerically:
 The Direct Solver using a consecutive sub-
  stitution procedure.
 The Iterative Solver using a modified Newton-
  Raphson procedure on top of the Direct Solver.

The main strength of TK is detecting and per-
forming automatically any inverse operations
required in the process of solving equations
or satisfying rules and constraints.  This
technique covers what is commonly known as
'backsolving' or 'goal seeking'.



EXAMPLES:
1. If  y = sin(x)  is stated and  y  is known,
   TK takes  arc sine  of  y  to evaluate  x.
2. If  ln(a) = b + c*d  is stated (spelling out
   addition and multiplication), and  a,b,c  are
   known, TK uses subtraction and division to
   evaluate  d=(ln(a)-b)/c .
3. If  or(sick,tired)=and(home,rest)  is stated,
   and  "tired"  is known to be  1  (true), then
   the Direct Solver infers that one should stay
   home=1  and have  rest=1 ; it doesn't matter
   whether "sick" is known or unknown.

TK action in most applications comes down to
solving one or more equations, linear or non-
linear, algebraic or transcendental (hence the
frequent use of the term 'equation processor'
in association with TK).
Typically there are  m  independent equations
with  n  unknowns (món).  Possible outcomes of
applying the solvers are as follows:
 Values of n-m variables are given, the
  Direct solver solves for  m  unknowns.
 The same, the Iterative Solver solves for
  m  unknowns.
 The same, solution not completed because of
  indeterminacy condition.
 The same, solution not completed because of
  domain error.
 The values of fewer than n-m variables given,
  solution is incomplete.
 The values of more than n-m variables given,
  one of the equations becomes inconsistent,
  and the solution process is terminated.


EXAMPLES (in order of the above cases):
Assume two equations with four variables:
           a * (.5+x)^2 = sin(y/2)
           b * (y-pi()) = ln(x)
  1. x=1.5 and y=2 assigned as inputs.  Direct
     Solver returns a=.21036775 and b=-.3551749.
  2. a=.2 and b=-.5 assigned as inputs and  x
     is made guess = 1.5.  Iterative Solver
     returns  x=1.6085001 and y=2.1909884.
  3. a=4/9 and y=pi() assigned as inputs. Direct
     Solver evaluates x=1 from the 1st equation,
     and the 2nd is ignored because of b=0/0.
  4. a=pi() and y=4/9 assigned as inputs. Direct
     Solver returns x=-.2351325 from the 1st
     equation, solution stops on attempt to take
     ln(x).
  5. Only a=pi() assigned as input.  Nothing is
     evaluated.
  6. b=-.5, x=1.5 and y=2 assigned as inputs.
     Direct Solver evaluates a=.21036775 from
     the 1st equation and stops on inconsistency
     error in the 2nd.

The solvers are the basic tools of TK's decla-
rative concept.  They are complemented by user-
definable procedures, i.e. sequential programs
for specific tasks that are hard or impossible
to accomplish in a purely declarative language.
019
The DIRECT SOLVER
  is activated by pressing F9.  The Output
  fields on the Variable Sheet are then
  cleared and the new solution displayed.

Solution of a Single Equation:
  The Direct Solver solves an equation (rule)
  for an unknown variable, provided the values
  of all the other variables in that equation
  are known.  The unknown must not appear more
  than once and must not be an argument of a
  non-invertible function.

Solution of a System of Equations:
  The Direct Solver scans through the rules of a
  model and resolves them one after another
  until no further evaluation is possible.
  After each variable is evaluated it is
  substituted into the other rules.
016
OVERDEFINED

CAUSE:
  The marked variable appears in an equation
  that was found inconsistent.  The variable
  may be directly or indirectly responsible
  for this condition, which usually means that
  the problem was overdefined.

SOLUTIONS:
 Blank the input value of the offending
  variable (if assigned) or of any other
  variable marked 'Overdefined' or some other
  variable that may be responsible.
 Delete, cancel or edit the rule marked
  'Inconsistent'.
036
The ITERATIVE SOLVER
  solves equations or sets of equations that
  cannot be solved by the simple substitution,
  inversion and evaluation performed by the
  Direct Solver alone.

  In order to invoke the Iterative Solver, one
  or more unknown variables must be assigned
  guess values which temporarily play the role
  of inputs.

  The additional inputs make some equations
  overdefined (i.e., they cause errors inter-
  nally).  The Iterative Solver manipulates the
  guess values, and calls the Direct Solver
  repeatedly until each of the error terms is
  reduced below the Comparison Tolerance.

USAGE:
 Make sure that your problem is well-defined,
  i.e. there are as many independent equations
  as unknowns.
 Determine which variables remain unsolved
  after applying the Direct Solver, and which
  of them, if known, would trigger solving the
  rest of the equations (even at the expense of
  causing inconsistencies); select as few of
  them as needed.
 Type G in the Status field and a guess value
  in the Input field of the selected variables.
 Press  F9 .

NOTE:
  The Iteration Count and Convergence Factor are
  displayed on the Prompt/Error line during
  iteration.
024
BACKSOLVING

In a narrow sense: Detecting and performing
  automatically any inverse operations required
  in the process of solving equations or satis-
  fying rules and constraints.
EXAMPLE:
  TK's Direct Solver will solve an equation
  a+b=c*d  for any unknown variable provided
  the values of the other three are known.

In a broad sense: Turning an output variable
  into input (by assigning it an input value),
  and solving for what was before an input
  variable.
EXAMPLE:
  y=x*ln(x) returns  y=13.621  given  x=7 .
  If  y  is constrained to  13 , backsolving

  yields  x=6.788 .  The Iterative Solver had
  to be used here (starting with a guess x=7),
  although in many situations the backsolving
  may be conveniently handled by the Direct
  Solver alone.
049
GOAL SEEKING

  is a process of finding "how can" a certain
  result be achieved.  "What if" analysis is a
  process of finding what result you get if you
  change the value of an ingredient.  The
  processes may appear similar, but the goal
  seeking is more general.

  To perform goal seeking with conventional
  "what if" program, you must assign new values
  to an ingredient by trial and error until the
  result turns out to have the value you wanted.
  With TK, you simply assign the value of the
  desired result as an input and let TK find
  the needed value of the ingredient in just a
  single solution.

  TK lets you swap the ingredient and result
  variables arbitrarily, and switch effortlessly
  between "what if" and "how can" analyses.

EXAMPLE:
  When monthly deposits D are made over a period
  T years at a yearly interest rate IR, the
  future value V is given by the formula

        V = D*((1+IR/12)^(12*T)-1)/(IR/12)

  What if D=$200, IR=.08=8% and T=10 years?
  TK responds with  V=$36,589.20 .

  How can a goal V=$40,000 be achieved if one
  can stretch the time over which the payments
  are made?  You switch the input and output
  values, and TK responds with 10.63 years.
  If time is fixed at 10 years, the monthly
  deposits would have to increase to D=$218.64.
  TK's Direct Solver found that through
  simple backsolving.

  How can the same goal be reached through a
  better interest rate if D and T remain fixed
  at $200 and 10 years respectively?  The
  Iterative Solver must be used because the
  Direct Solver cannot isolate IR in the above
  formula.  You guess IR=.08, (i.e. type G in
  its Status field), press F9, and TK returns
  the result of IR=.0958=9.58%.
017
The CONVERGENCE FACTOR
  is displayed during iteration on the Prompt/
  Error Line.  It shows the progress of the
  Iterative Solver toward the solution.

The Convergence Factor is defined as

         ln ( ABSERR / (CT*TV) )

(see explanation of terms in "Comparison
Tolerance").

If there are more guesses and error terms than
one, the maximum ABSERR (i.e. the bottleneck)
is used.  The iteration is usually considered
successful when the convergence factor falls
below zero.
014
TOP LEVEL COMMANDS
ÀÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
   ÃÄÄÄÄÄÄÄÄ Function Keys
   ÃÄÄÄÄÄÄÄÄ Slash Commands
   ÀÄÄÄÄÄÄÄÄ Navigation Commands

  Move the cursor over one of the Related Topics
  below and press ÄÙ.

NOTES:
 If you need help on Help, press F1.
 If you ever need to get back to this screen:
  Press F1 ÄÙ    (from within Help).
  Press F1 F1 ÄÙ (from TK).
030
FUNCTION KEYS

ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º F1: On-Line Help    ³ F2: Cancel Entry      º
ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º F3: Load File (/SL) ³ F4: Save File (/SS)   º
ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º F5: Edit            ³ F6: One-Window (/W1)  º
ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º F7: Display Plot    ³ F8: Display Table     º
ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º F9: Solve           ³ F10: List Solve (/LL) º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼

USAGE:
 From any sheet, press the desired key and the
  command or command sequence will be executed.

NOTE:
 Some function keys allow additional commands
  by pressing them while holding down the Shift
  key:

ÉÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º Shift-F3  or  F11:  ³ Shift-F4  or  F12:    º
º    Load Menu        ³    Shell to System    º
ÇÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º Shift-F7:           ³ Shift-F8:             º
º    Copy Plot to PV  ³    Copy Table to PV   º
ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
019
CANCEL COMMAND and BREAK COMMAND

  The Cancel Command (F2) terminates your action
  (e.g. typing a multi-keystroke command or
  editing a field), beeps, and returns TK to the
  state before the action.  F2 also quits Help.
  You press F2 when you change your mind about
  what you are doing and want to have it undone.
  Once you complete an action (e.g. enter new
  contents in a field by pressing ÄÙ), F2 will
  not undo it.

  The more powerful Break Command (Ctrl-Break)
  may be used to the same effect as the Cancel
  Command, but in addition it terminates any TK
  action in progress:  solving, list filling,
  loading and saving files, plotting, etc.
  You press Ctrl-Break when you want TK to
  stop what it is doing.
057
NAVIGATION COMMANDS in Sheet View:

Ä    Moves cursor one field to right
Ä    Moves cursor one field to left
     Moves cursor up one line
     Moves cursor down one line
ÄÄÝ  Moves cursor to rightmost field
ÞÄÄ  Moves cursor to leftmost field

Home  Moves cursor to top of sheet
End   Moves cursor to end of sheet
PgDn  Moves cursor N-2 lines down
PgUp  Moves cursor N-2 lines up
       (where N is number of lines displayed in
       current window)
=     Select Sheet command:  Selects sheet to
      be displayed in current window.
>     Dive command:  Brings up a relevant
      subsheet in the current window.
<     Return command:  Brings back original
      sheet or subsheet.
;     Switch command:  If two windows are
      displayed on the screen, the Switch
      command moves the cursor between them,
      otherwise it has no affect.
Ctrl-PgDn  Increases the size of upper window
           at the expense of lower window
Ctrl-PgUp  Increases the size of lower window
           at the expense of upper window





NAVIGATION COMMANDS in Presentation View:

Ä    Moves block cue one position to right
Ä    Moves block cue one position to left
     Moves block cue up one line
     Moves block cue down one line
ÄÄÝ  Moves block cue eight positions to right
ÞÄÄ  Moves block cue eight positions to left

Home  Moves block cue to upper left corner of
        current box or, if already there, to
        upper left corner of next higher box.
End   Moves block cue to upper left corner of
        next lower box.
PgDn       Moves block cue 5 lines down
PgUp       Moves block cue 5 lines up

Ctrl-PgDn  Moves cursor one screen down
Ctrl-PgUp  Moves cursor one screen up

=     Select Sheet command:  Selects sheet to
      be displayed in current window.  (Sheet
      View will replace Presentation View.)
016
EDITING COMMANDS

ÉÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º  F5  ³ Edit field: make changes in a field º
ÇÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  /B  ³ Blank a specified block of fields   º
ÇÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  /D  ³ Delete a specified block of rows    º
ÇÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  /I  ³ Insert blank lines in a specified   º
º      ³ block of rows; Ins inserts one line º
ÇÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  /C  ³ Copy a specified block of fields    º
ÇÄÄÄÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¶
º  /M  ³ Move a specified block of rows      º
ÈÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
027
The EDIT FIELD command  (F5)

The in-field editor is invoked in the field
marked by the cursor when:
 You press F5.
 TK detects an error in attempted entry.

When in the Editor:
 Move the cue  _  with the Arrow or other
  special keys:
      -            one character to left
      -            one character to right
      PgUp or ÞÄÄ  8 characters to left
      PgDn or ÄÄÝ  8 characters to right
      Ctrl -       one word to left
      Ctrl -       one word to rightt
      Home or      to beginning of field
      End  or      to end of field
 Add characters by typing them at the location
  marked by the cue  _ .
 Delete a character above the cue with the
  Delete key (Del).
 Delete a character to the left of the cue
  with the Backspace key (ÍÍ).
 Enter the changed contents by pressing ÄÙ.
 Exit the Editor without making any changes by
  pressing F2 or Ctrl-Break.
033
SPECIAL KEYS

    Home    PgUp    Tab (ÞÄÄ)         Ins
    End     PgDn    Shift-Tab (ÄÄÝ)   Del

and the four Arrow keys and Backspace key (ÍÍ)
play specific roles in navigation around TK
sheets, field editing, and sheet editing (see
the specific Help topics below).

Other important special keys:

  Ctrl-Break  Interrupts actions, such as
                 solving, saving or loading
                 files, printing, etc.
  Esc         Steps backward through a series
                 of options to a command.

               Exits to the Sheet View from the
                 Presentation View, a TK menu,
                 the Configuration Editor, the
                 Disk Index, or a Help screen.
               Cancels a prompt for entry in
                 the Prompt/Error Line (SV)
                 or the Status Line (PV).
  Alt         Must be pressed when invoking a
                 macro by its short name (e.g.
                 Alt-R), when typing extended-
                 ASCII character codes (e.g.
                 Alt-224), and when entering a
                 semicolon as the 1st character
                 in a field (e.g. Alt-;).
  Shift-PrtSc Dumps the screen to a printer.
053
DISK INDEX
  lists disk files available to the current
  Storage command option.

USAGE:
 Type /S followed by any option character
  (You can press F3 instead of typing /SL
  or press F4 instead of typing /SS).
 Choose one of the following three actions:
  - Press F5 to start editing default filename.
  - Type a drive designator and pathname (or a
    blank to access default directory).
  - Type a partial filename with wildcards to
    limit the display to related files.
 Press ÄÙ to display the Disk Index.
 Move the cursor to the row of the desired
  file.
 Press ÄÙ.
NOTES:
 If the desired file is not listed you will
  have to press F2 to exit the Disk Index
  and start over.
 The Disk Index displays only files with
  the extension appropriate for the specified
  Storage option.  The extension is always .TK
  except for:
      Storage option  Extension
      /S#S   /S#L     .DIF
      /SWS            .WKS
      /SWL            .WKS or .WK1
      /SAS            .ASC
      /SAL   /SD      any extension
 The Disk Index also displays the names of
  subdirectories.  Dive into a subdirectory by
  pressing  >  or  ÄÙ  and return to the
  parent directory by pressing  < .
To load multiple files:
 Access the Disk Index with F3 ÄÙ, or any
  other load option of the Storage command.
 Type numbers in the Selection column to
  specify the order of loading (you can change
  the order by typing new numbers, or 0 to
  blank a selection).
 Press ÄÙ.

To delete multiple files:
 Access the Disk Index with /SD ÄÙ.
 Type numbers in the Selection column to
  specify the order of deletion (you can change
  the order by typing new numbers, or blank the
  selection by typing 0).
 Press ÄÙ.
 TK asks for confirmation for each deletion.
022
WILDCARDS  or  GLOBAL FILENAME CHARACTERS

are special characters you can use within a
filename and extension to specify a group of
files to be displayed in the Disk Index.

  ?  indicates that any character can occupy the
     given position,

  *  indicates that any character can occupy the
     given position and all the remaining
     positions in the filename or extension.

EXAMPLES:
  1. F3 rlc??a ÄÙ  will bring up RLC01A.TK,
     RLC05A.TK and RLC23A.TK in the Disk Index,
     and will ignore RLC01B.TK, RLC23B.TK and
     RLC01AB.TK .
  2. F3 rlc0* ÄÙ  applied to the same directory
     will bring up RLC01A.TK, RLC05.TK,
     RLC01B.TK and RLC01AB, and it will ignore
     RLC23A.TK and RLC23B.TK .
010

SOLUTIONS:
 Review the offending rule and other rules and
  input values to see what caused the error.
 If the error occurred during iteration: change
  the guess value(s) and/or review the strategy
  of using the Iterative Solver.
 If the error occurred when entering or exami-
  ning an expression: examine the ingredients,
  then edit the expression or make a new entry.
007

SOLUTION:
 Dump the screen to a printer and/or write down
  everything that might relate to this problem.
 Save the current model to a disk right away
  if possible.
 Notify UTS (call 1-800-HELP-UTS).
014
PARSER ERROR

  This error should not have occurred.

REMEDIAL ACTION:
 Write down everything that might relate to
  this problem, copy the offending rule,
  statement or expression and/or dump the screen
  to the printer by pressing PrtSc.
 If a rule or a statement entry was attempted,
  put a  "  in front of it and press Enter.
  Else, exit the editor pressing F2.  In any
  case save the current model to a disk.
 Notify UTS (call 1-800-HELP-UTS).
011
MISSING OPERAND

CAUSE:
  Attempting to enter a rule, statement or
  expression which contains a dangling operator,
  equal sign, or other symbol.

SOLUTION:
  The cue points to the problem spot.  Edit the
  entry so that each operator has a name, value,
  or valid expression on either side of it.
010
MISSING OPERATOR

CAUSE:
  Attempting to enter a rule, statement or
  expression which contains two parenthetical
  expressions with no operator between them.

SOLUTION:
  Edit the entry so that adjacent parenthetical
  expressions are related by an operator.
016
FUNCTION MAY ONLY BE USED WITH CALL

CAUSES:
 An attempt to refer to a built-in function
  that can only be used with CALL (e.g. DELETE,
  LISTCOPY, or BLANK).
 A reference to a procedure or rule function
  with a number of Output or Result Variables
  different from the one or two function values
  required in the context of the function
  reference.

SOLUTION:
  Edit the rule, statement or expression so that
  it does not refer to the function but instead
  calls it.
010
MISSING FUNCTION NAME

CAUSE:
  You entered a rule, statement or expression
  consisting of the keyword CALL not followed by
  anything at all.

SOLUTION:
  Add a function name and argument list, if any,
  to the CALL rule, statement or expression.
011
INVALID CALL SYNTAX

CAUSE:
  You attempted to call a function with a rule
  or statement which did not conform to the
  syntax:

  CALL function_name(in_args;out_args)

SOLUTION:
  Edit the entry so that it satisfies syntax.
010
INVALID RETURN STATEMENT

CAUSE:
  You attempted to enter a Return statement that
  does not follow the syntax:
  RETURN

SOLUTION:
  Edit the statement so that it obeys the above
  syntax.
015
INVALID FOR STATEMENT

CAUSE:
  You attempted to enter a FOR statement that
  did not follow the syntax:

     FOR var_name = expr1 TO expr2

  or

     FOR var_name = expr1 TO expr2 STEP expr3

SOLUTION:
  Edit the statement so that it does conform to
  the above syntax.
017
FUNCTION TYPE MUST BE DECLARED

CAUSES:
 Attempting to call or refer to a user-defined
  function which has not been assigned a Type
  on the Function Sheet.
 The function name has not been entered on the
  Function Sheet.

SOLUTIONS:
 In case of a rule or a statement make it a
  comment (Home " Enter) while the function
  is being defined, go to the Function Sheet
  and define the function, then uncomment the
  rule or statement again (F5 Del Enter).
 Check spelling to see if the function name
  was misspelled.
009
EXPECTED OPERATOR NOT FOUND

CAUSE:
  You attempted to enter a rule, a statement or
  an expression that is missing an operator
  between two operands.

SOLUTION:
  Change the entry so that it is valid.
015
UNDEFINED FUNCTION

CAUSES:
 You entered an undefined function name
  immediately preceding a left parenthesis.
 You entered a variable name immediately
  preceding a left parenthesis.

SOLUTIONS:
 Edit the function name so it refers to
  an existing built-in or user-defined
  function.
 Edit the expression to add an operator
  between the variable name and the left
  parenthesis.
009
INVALID FUNCTION NAME

CAUSE:
  You entered an invalid TK name immediately
  preceding a left parenthesis.

SOLUTION:
  Specify a valid function name when referring
  to a function.
011
TOO MANY FUNCTION ARGUMENTS

CAUSE:
  Attempt to enter a rule, a statement or an
  expression that contains a function reference
  or call with more arguments than the
  definition of the function specifies.

SOLUTION:
  Edit the entry so that the function reference
  or call has the correct number of arguments.
011
TOO FEW FUNCTION ARGUMENTS

CAUSE:
  Attempt to enter a rule, a statement or an
  expression that contains a function reference
  or call with fewer arguments than the
  definition of the function specifies.

SOLUTION:
  Edit the entry so that the function reference
  or call has the correct number of arguments.
010
NUMBER OUTSIDE NUMERIC RANGE OF PROGRAM

CAUSE:
  You attempted to enter an input value, a rule
  or a statement with a value whose absolute
  value is greater than 1E308.

SOLUTION:
  Edit the entry so that all values fit the
  numeric range of TK.
012
COMPLEX ARGUMENT FOUND WHERE REAL ARGUMENT
EXPECTED

CAUSE:
  Using a pair of numbers (complex number) or an
  expression which returns a complex result as
  an argument of a function requiring real
  number argument(s).

SOLUTION:
  Supply the real number argument to the
  function where it is required.
016
EXPRESSION TOO COMPLICATED

CAUSE:
  You attempted to enter an expression with too
  many pending operations or operands, or a rule
  or a statement that contains such an
  expression.

SOLUTION:
  Edit the expression to reduce the number of
  pending operations or operands and enter the
  new rule, statement or expression.

NOTE:
  The cue indicates the point at which TK
  could no longer handle the expression.
010
INVALID COMPLEX COMPUTATION

CAUSE:
  Attempting to enter in a value field an
  expression which includes complex numbers.

SOLUTION:
  Enter only a single numeric or symbolic
  value, or an expression that evaluates into
  a single value.
010
IF CLAUSE IN EXPRESSION

CAUSE:
  Attempting to enter a conditional clause, rule
  or statement into a field which accepts only
  evaluable expressions.

SOLUTION:
  Enter only an expression which evaluates to an
  acceptable value.
016
MIXED REAL AND COMPLEX OPERANDS

CAUSE:
  An expression must not have both single and
  paired expressions as operands to the same
  operator.

SOLUTION:
  Edit the expression or rule so that it
  contains only single operands or only
  paired (complex) operands.

NOTE:
  As an exception, POWER requires the first
  argument to be a complex number, and the
  second to be a single expression.
013
RECURSIVE FUNCTION REFERENCE

CAUSE:
  Attempting to define a rule function with a
  reference to the function itself.

SOLUTION:
  Edit the rule that caused the error.  Remove
  any reference to the function being defined
  and enter the new rule.

NOTE:
  Only procedure functions can be recursive.
017
MISSING LABEL

CAUSES:
 The Label name was removed while editing a
  statement in a procedure function, so the
  statement begins with a ":"
  The syntax for a labeled statement is:
    label_name: statement
 Attempting to enter an invalid GOTO statement
  that is missing the label name.  The syntax
  for the GOTO statement is:
    GOTO label_name

SOLUTIONS:
 Edit the statement so that the colon (:) is
  preceded with a label name.
 Specify a label name after the key word GOTO.
010
DUPLICATE LABEL NAME

CAUSE:
  Attempting to use the same label name in more
  than one labeled statement in a procedure
  function.

SOLUTION:
  Edit the entry so that each labeled statement
  has a unique label name.
010
INVALID TARGET FOR ASSIGNMENT

CAUSE:
  Attempting to enter a statement which includes
  a target for an assignment which is neither a
  variable nor a list reference.

SOLUTION:
  Edit the entry so the target is either a
  variable or a list reference.
011
MISPLACED COMMA

CAUSE:
  Attempting to enter a rule, a statement or an
  expression which contains a comma between
  values or between names that are neither
  function arguments nor the real and imaginary
  components of a complex number.

SOLUTION:
  Edit the entry to remove any extraneous comma.
009
MISPLACED SEMICOLON

CAUSE:
  Attempting to enter an expression which
  contains a semicolon which is not separating
  input and output function arguments.

SOLUTION:
  Remove the semicolon.
011
OUT OF MEMORY (while parsing)

CAUSE:
  TK ran out of memory while loading
  a file or parsing an entry.

SOLUTION:
  Save information with the Storage command
  and then clear some or all of the model
  with the Reset command (/R) or Delete
  command (/D).
018
FUNCTION TYPE IS ALREADY DEFINED

CAUSE:
  Attempting to re-define the Type of a
  user-function.

SOLUTION:
 Delete the function from the Function Sheet
  with /D and enter its name again with the new
  Type specification.  (All contents of the
  Function Subsheet will be lost upon deletion
  of the function.  To retain subsheet
  contents, create a new function of the
  desired Type in the opposite window and use
  the Copy command (/C) to transfer contents.
  Use   Home " Enter   to comment out rules or
  statements before copying between rule and
  procedure functions.)
012
NO ROOM FOR PLOT

CAUSE:
 The plot would be too tall and narrow or too
  short and wide to allow for display.

SOLUTION:
 Use linear scaling instead of isotropic, and,
  if applicable, control the appearance of the
  plot by setting minimum and maximum values
  for the axes.
 Change the data.
017
NO INPUT LISTS OR EMPTY INPUT LIST

CAUSES:
 You pressed F10 (/LL) to list solve when no
  "input list" (i.e. list associated with input
  variable) was specified.
 You pressed F10 (/LL) to list solve when all
  input lists were empty.

SOLUTION:
  Associate each input list with an input
  variable by typing "L" in the status field of
  the variable (if it does not already have an
  "L" in it) and/or "I" (if the variable does
  not already have an input value).  Fill the
  lists on their List Subsheets and then solve
  with the F10 (/LL) command.
012
NO VARIABLES WITH ASSOCIATED LISTS

CAUSES:
 Attempting to list solve (F10), or to use the
  Put or Get options of the List command (/LP
  and /LG) when there were no lists associated
  with variables on the Variable Sheet.

SOLUTIONS:
 Type "L" in the Status column of a variable to
  declare an associated list for that variable.
  Repeat the F10 or /LP or /LG command.
009
INVALID CONFIGURATION FILE

CAUSE:
  The TK2.CFG file is not set up correctly.

SOLUTION:
  Refer to the related topic "Configuration"
  for the correct format and contents of the
  configuration file.
015
NOT A VALID ASCII FILE

CAUSE:
  Attempting to load an ASCII file which does
  not have the proper file structure of list
  names, list elements and delimiters
  recognized by TK.

SOLUTIONS:
 Load a file which was saved with the ASCII
  option of the Storage command (/SAS).  (Such
  files have .ASC extensions.)
 If the errant file was created or modified
  with an ASCII editor, study a file saved from
  TK with /SAS and mimic its structure.
015
NO POINTS TO PLOT

CAUSE:
  You attempted to display a plot without first
  specifying non-empty list(s) of values on
  the Plot Subsheet.

SOLUTION:
  Enter the name(s) of non-empty list(s) on the
  Plot Subsheet and press F7.

NOTE:
  A list for which there is no unit conversion
  definition between the display unit and
  calculation unit is treated as an empty list.
014
INVALID RANGE SPECIFIED

CAUSE:
  Attempting to specify a block for blanking,
  moving, inserting or copying which includes
  both the body of the sheet and the top area of
  the sheet.

SOLUTIONS:
 When blanking or copying, specify fields in
  the top area or in the body of the sheet, but
  not both.
 When inserting or moving, select rows only in
  the body of the sheet.
009
BLOCK INCLUDES FIELD THAT CANNOT BE BLANKED

CAUSE:
  You attempted to blank a block of fields that
  included one or more fields that cannot be
  blanked or left blank.

SOLUTION:
  Leave the field as is.
013
NOT ENOUGH ROWS AVAILABLE ON SHEET

CAUSES:
 Attempting to insert row(s) in a sheet so
  that it would have more than 32000 lines in it
 Attempting to use the Move command in a sheet
  that is full.

SOLUTIONS:
 Delete unnecessary lines, if any, before
  inserting new line(s) or moving line(s).
 If possible, combine lines to make room for
  new lines.
024
INCOMPLETE SAVE

CAUSES:
 When you used the /SS (F4) command it caused
  a disk error.  (Disk errors may be caused by
  saving to a faulty disk drive, a damaged
  disk, a write-protected disk, or a disk that
  is full.)
 You pressed Ctrl-Break during the save
  procedure.

SOLUTIONS:
 If the error occurred because the Ctrl-Break
  key was pressed, begin the Storage command
  again and allow the save procedure to be
  completed.
 If the problem is elsewhere, check the disk to
  determine whether it is full or write-
  protected.  Try saving the file to a different
  disk drive or disk.
 The incomplete save may have created a partial
  file on the first disk.  Delete this file
  using the /SD command or an appropriate
  command at DOS level.
019
INCOMPLETE LOAD

CAUSES:
 Attempting to load a damaged file.
 Attempting to load a TK file which is not
  compatible with the version of TK you are
  using.
 You pressed the Ctrl-Break key during the
  loading procedure.

SOLUTIONS:
 If the file has been damaged, the load is
  completed up to the damaged section.
  Information that has been damaged must be
  recreated.
 If the error occurred because Ctrl-Break was
  pressed, reset TK (/RAY) and load the
  file again; allow the load procedure to be
  completed.
021
NO FILES FOUND WITH MATCHING FILENAMES

CAUSES:
  You attempted to view the files on the disk
  with the Disk Index when there were either:
 No files available for the current Storage
  command option.
 No filenames matching the wildcard you
  specified.
 No disk found in the specified drive.

SOLUTIONS:
  Start the Storage command again and:
 Use a different wildcard specification to look
  for files on the disk.
 Type a complete filename instead of looking at
  the Disk Index.
 Type a path to a subdirectory containing files
  relevant to the Storage command option before
  looking at the Disk Index.
 Insert a disk into the specified drive.
008
INVALID STEP SIZE

CAUSES:
 Attempting to enter a step size of 0 for the
  Add Step option of the List Fill command.
 Attempting to enter a step size of 1, 0, or
  less than 0 for the Multiply by Step option
  of the List Fill command.
009
OUT OF MEMORY (while doing a table)

CAUSE:
  TK ran out of memory while generating a table.

SOLUTION:
  Save information with the Storage command
  (/SS), then clear some or all of the model
  with the /R or /D command.
017
NO VALUES TO PRINT

CAUSES:
 You attempted to display or print a table by
  pressing F8 without first specifying at least
  one list that contains at least one value.
 You attempted to display or print a table
  where the first element number specified in
  the First Element field is larger than the
  length of any of the lists.

SOLUTIONS:
 Enter the name of at least one non-empty list
  in the List field on the Table Subsheet, then
  press F8.
 Enter a First Element value which is smaller
  than the length of any of the lists.
021
TABLE LINE TOO LONG

CAUSES:
 Horizontal table:  A table line (consisting of
  the longest header or list name, the column
  separators, and the greatest column width) is
  too long to fit on a single output line.
 Vertical table:  At least one column in the
  table (including the column separators on each
  side) is too long to fit on a single output
  line.

SOLUTION:
  Adjust the settings on the Table Sheet, or, if
  appropriate, the printer settings on the
  Global Sheet.

NOTE:
  The maximum line length is 80 columns on the
  screen, or as specified on the Global Sheet
  for a printed table.
013
PAGE SIZE TOO SMALL FOR TABLE

CAUSE:
  If the page size defined on the Global Sheet
  by the Page Length and Page Width fields is
  too small to accommodate a table created from
  the Table Subsheet, any attempt to print the
  table results in an error.

SOLUTION:
  Move to the Global Sheet and re-define the
  page size to accommodate the dimensions of
  the table.
012
NOT A VALID STORAGE FILE

CAUSES:
 Attempting to load a file that is not a valid
  TK model file.
 Attempting to load a TK model file that has
  been incorrectly changed with a text editor.

SOLUTIONS:
 Specify a filename for a valid TK model file.
 Modify the file again with a text editor to
  make it a valid TK model file.
022
WARNING: NAME CONFLICT; USER FUNCTION RENAMED

CAUSE:
  Loading a TK model file that contained one
  or more list, rule, or procedure functions
  whose name duplicates the name of a TK
  built-in function.

NOTES:
 TK marks the errant function names on the
  Function Sheet by prefixing them with a
  percent sign  % .  You can rename the marked
  functions so they do not duplicate built-in
  function names.
 It is up to you to find and modify all rules
  and/or statements with calls and references
  to the errant functions.  TK may or may not
  mark those rules with  %  to indicate they
  are unparsed, depending on whether the number
  of arguments appropriate for the built-in
  functions matches the number of arguments
  used in the calls and references.
013
OBJECT DELETED; SUBSHEET NO LONGER ACCESSIBLE

CAUSE:
  You attempted an action in a subsheet which
  does not exist anymore.  (When an object, for
  instance a variable, is deleted the contents
  of the associated subsheet are also deleted,
  even if the subsheet still appears on the
  screen in the other window.)

SOLUTION:
  Create a new object.  Dive to the new
  subsheet to enter information.
012
FIELD RESERVED FOR OUTPUT

CAUSE:
  Attempting to enter a value in the Output
  field on the Variable Sheet or Subsheet, the
  Arguments field on the Function Sheet, the
  Elements field on the List Sheet, or the
  Lines field on the Comment Sheet.

SOLUTIONS:
 Leave the field as is.
 Enter a new value in an Input field.
010
OPTIONS: YES  NO

CAUSE:
  You attempted to enter anything else than "Y"
  or "N" in a field that requires a yes or no
  input.

SOLUTIONS:
 Enter a "Y" or "N".
 Leave the field as is.
015
INVALID INTEGER

CAUSES:
 Attempting to enter a number that was not
  an integer in a field requiring an integer
  entry.
 Attempting to enter an integer greater than
  32767 or less than -32767 in a field requiring
  an integer entry.

SOLUTIONS:
 Edit the incorrect entry to make the number a
  valid integer.
 Press F2 to restore the most recent correct
  value.
012
OUT OF MEMORY

CAUSE:
  Any action which uses up the remaining memory
  available for running TK results in an
  error and cancellation of the action in
  progress.

SOLUTION:
  Save information with the Storage command
  (/SS) and then clear some or all of the model
  with the /D or /R command.
009
FIELD CANNOT BE BLANKED

CAUSE:
  You attempted to enter blanks in a TK
  field that cannot be left blank.

SOLUTIONS:
 Leave the field as is.
 Enter a non-blank value.
010
FIELD DOES NOT EXIST

CAUSE:
  Attempting to enter an entry when the cursor
  is on a line on the Global Sheet which does
  not have a corresponding legend.

SOLUTION:
  Move cursor to a valid field on the Global
  Sheet before entering a value.
051
The Action command (!) has an effect ONLY on:

ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³ Sheet                  ³Key³ Action         ³
ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÅÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
³ Rule Sheet             ³ ! ³ Solves Model   ³
³ Variable Sheet         ³ ! ³ Solves Model   ³
³ List Function Subsheet ³ ! ³ List Fill      ³
³ List Subsheet          ³ ! ³ List Fill      ³
³ Vert Interactive Table ³ ! ³ List Fill      ³
³ Plot Sheet             ³ ! ³ Displays Plot  ³
³ Plot Subsheet          ³ ! ³ Displays Plot  ³
³ Table Sheet            ³ ! ³ Displays Table ³
³ Table Subsheet         ³ ! ³ Displays Table ³
ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ



Function Key Substitutes for the Action command
are effective from anywhere in TK:

  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÂÄÄÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ ANY Sheet         ³ F7  ³ Display Plot   ³
  ³ ANY Sheet         ³ F8  ³ Display Table  ³
  ³ ANY Sheet         ³ F9  ³ Solve Model    ³
  ³ ANY Sheet         ³ F10 ³ List Solve     ³
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÁÄÄÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ









The Action command has NO effect on:

  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ Sheets With No Action       ³
  ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ Function Sheet              ³
  ³ Global Sheet                ³
  ³ List Sheet                  ³
  ³ Numeric Format Sheet        ³
  ³ Numeric Format Subsheet     ³
  ³ Procedure Function Subsheet ³
  ³ Rule Function Subsheet      ³
  ³ Unit Sheet                  ³
  ³ Variable Subsheet           ³
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
010
UNDEFINED VARIABLE

CAUSE:
  You attempted to enter the name of a variable
  that does not exist or does not have a value.

SOLUTIONS:
 Edit the variable name so that it refers to a
  defined variable name.
 Press F2 to cancel and try again.
011
EXPRESSION CANNOT BE EVALUATED

CAUSES:
 Attempting to enter an unevaluable expression
  on the Prompt Line during /EE or /EC.
 Attempting to enter an unevaluable expression
  in a value field (e.g. 0/0).

SOLUTION:
  Enter only valid TK expressions for
  evaluation.
010
DUPLICATE VARIABLE NAME

CAUSE:
  Attempting to enter the same variable name
  twice in a Parameter, Input, Output, Argument
  or Result Variables field.

SOLUTION:
  Edit the entry so no variable name is listed
  more than once.
014
TOO MANY VARIABLES

CAUSES:
 Attempting to enter more than:
  - 20 variables into the Parameter Variables,
    Input Variables or Output Variables field
    on a Procedure Function Subsheet;
  - 20 variables into the Parameter Variables,
    Argument Variables or Result Variables field
    on a Rule Function Subsheet.

SOLUTION:
  Edit the entry so that no more than the
  allowed number of variables is listed.
012
DISK INDEX: NOT ENOUGH MEMORY TO CONTINUE

CAUSE:
  Using the Disk Index to view the contents
  of the disk exhausted the memory available
  to TK.

SOLUTION:
  Save all or part of the model with F4 without
  using the Disk Index so you can delete all
  or part of the model to free up memory to
  allow use of the Disk Index.
012
MISSING UNIT CONVERSION unit1 TO unit2

CAUSE:
  Attempting to load a DIF file which contains a
  unit conversion between unit1 and unit2 which
  is not defined in the current model.

SOLUTION:
  Define the unit conversion between the
  calculation (unit1) and display unit (unit2)
  on the Unit Sheet, and load the DIF file
  again.
013
CANNOT SAVE DIF FILE: MISSING UNIT CONVERSION

CAUSE:
  Attempting to save a list to a DIF file when
  the unit conversion between the calculation
  and display unit is not defined on the Unit
  Sheet.

SOLUTIONS:
 Define the unit conversion between the units
  specified on the List Subsheet.
 Blank out the units on the List Subsheet
  before saving the list to a DIF file.
010
VARIABLE DOES NOT EXIST

CAUSE:
  You attempted to enter a nonexistent
  variable for evaluation when using the
  Variable option of the Examine command.

SOLUTION:
  Repeat the command and enter the name of an
  existing variable.
010
BUILT-IN FUNCTION ALREADY EXISTS

CAUSE:
  You attempted to declare a function on the
  Function Sheet with the same name as one of
  the TK built-in functions.

SOLUTIONS:
 Use the built-in function instead.
 Change the user function name.
014
CANNOT PLOT NEGATIVE OR ZERO VALUES ON
LOGARITHMIC AXES

CAUSE:
  Attempting to plot a negative or zero value on
  a logarithmic axis.

SOLUTION:
  When plotting points on a logarithmic axis,
  make sure there are no negative or zero values
  in the associated list.  You may need to
  convert the data to be plotted (i.e. to move
  the entire axis scale into a positive
  interval).
014
NO VARIABLE NAME

CAUSE:
  Attempting to enter an option in the Status
  field when there was no name in the Name
  field.

SOLUTIONS:
 Enter a rule containing the variable in the
  Rule field on the Rule Sheet before using the
  Status field.
 Enter the variable name into the Name field on
  the Variable Sheet before using the Status
  field.
018
WARNING: PARSER ERRORS DETECTED DURING LOADING

CAUSE:
 Loading a model with invalid rules or
  statements.  Those rules or statements that
  are left unparsed are marked with  a  %
  symbol in the Status field.
 Loading a model with discrepancies between
  the number of arguments in the user-defined
  function definitions and in calls or refe-
  rences to those functions in rules and
  statements.
 References to user-defined functions absent
  from the current or loaded model.

SOLUTION:
  Edit and modify the rules, statements, or
  function definitions before using the model.
010
MISSING UNIT CONVERSION

CAUSE:
  Incompatible calculation units between
  variable and associated list.

SOLUTION:
  Define the unit conversion between the
  calculation units of the variable and its
  associated list on the Unit Sheet.
010
TOO MANY LISTS

CAUSE:
  Attempting to enter more than 5 list names
  in the Y-Axis List(s) field on a Bar Chart
  Subsheet.

SOLUTIONS:
  Edit the entry so it names 5 or fewer lists.
  Press F2 to restore the previous entry.
016
NOT A VALID WORKSHEET FILE

CAUSE:
  Using /SWL to load a file which was not
   originally saved from Lotus 1-2-3, Lotus
   Symphony, or from TK (with /SWS), or was
   altered in some way.

SOLUTION:
  Use /SWL only to load files which were saved
   from TK with /SWS, from Lotus 1-2-3, or from
   Lotus Symphony and have not been modified.
  If the file was created by another program
   which supports the Lotus worksheet file
   format, try loading the file into Lotus 1-2-3
   to verify its structure.
009
OBSOLETE STORAGE FORMAT

CAUSE:
 You are trying to load a TK file saved with
  the old TK!Solver.

SOLUTION:
 Convert the file first using the UPTOPLUS
  program.
015
INCORRECT HELP FILE VERSION

CAUSE:
 You are using a TK2.HLP file which is not
  compatible with the version of TK Solver
  you are running.

SOLUTION:
 Save your work and quit TK.
 Replace the incorrect TK2.HLP file with one
  you received with your current version of the
  TK Solver program.  Make this file available
  in the current or program directory.
 Resume your TK session and invoke Help when
  needed.
020
CAN'T EXECUTE PLOT DRIVER

CAUSES:
 All or part of the TKDRIVER.CFG file is
  invalid or corrupted, depriving TK of the
  device driver information necessary to print
  a plot.
 Not enough memory for TK to run the device
  driver that translates screen plot information
  into a form suitable for the selected plotting
  device.

SOLUTIONS:
 If you edited the TKDRIVER.CFG file to modify
  the printed image, check for conformance to
  the file format.  If the file is corrupted for
  some other reason, make a fresh copy from the
  product disk onto your hard drive.
 Delete non-essential parts of your model to
  release some memory and try again.
010
CAN'T OPEN PLOT FILE

CAUSE:
 The file specified in the Plot Output
  Filename field in the Global Sheet cannot be
  opened because of a disk I/O error.

SOLUTION:
 Save your work, quit TK, and check your
  system.
008
ERROR WHILE PLOTTING

CAUSE:
 The output device was disconnected or failed
  for some other reason.

SOLUTION:
 Check your output device and try again.
012
NO MEMORY FOR PLOTTING WITH 'CURVES'

CAUSE:
 There is not enough memory to generate the
  curve-smoothing points.

SOLUTION:
 Use the  Lines  option  (set it for all
  or some  Y-axis  lists on the Line Chart
  Subsheet).
 Delete non-essential parts of your model
  to release some memory and try again.
014
NOT ENOUGH MEMORY TO SHELL TO DOS

SOLUTIONS:
 Save current model if necessary and quit TK.
 Before running TK2, free up memory by removing
  memory-resident programs, printer spoolers,
  disk caches, etc.

NOTE:
 If you shelled to DOS from some other program,
  such as a word processor, to run TK and you
  want to return to the other program, simply
  quit TK rather than trying to shell to DOS
  to run the original program again.
011
COMMAND PROCESSOR COULD NOT BE LOADED

CAUSE:
 COMMAND.COM file was not on path or was
  damaged when shell to DOS was attempted.

SOLUTION:
 Be sure an intact copy of COMMAND.COM is
  on the path before running TK.  (Consult
  your DOS documentation for information on
  the PATH command.)
009
INVALID MACRO COMMAND

CAUSE:
 A parser error occurred while interpreting a
  macro command in a macro statement.

SOLUTION:
 Consult the related topic to check the syntax
  of the macro commands in the errant statement.
021
INCLUDED OBJECT CANNOT BE MODIFIED

CAUSES:
 An attempt to delete any object or delete or
  blank any field within any object belonging
  to an included file.
 An attempt to modify or overwrite any field
  within any object belonging to an included
  file.  (This includes LISTCOPY, PLACE, or
  assignment to a list element in an included
  list.)
 An included keystroke macro attempts to delete
  itself with the {SelfDestruct} macro command.

SOLUTIONS:
 Do not attempt to modify an included object,
  either manually or by running a keystroke
  macro.
 If you must modify an included object, you can
  use one of the options of the Storage command
  Include option, /SI.  (See related topic.)
010
NO FILES HAVE BEEN INCLUDED

CAUSE:
 Attempting to use the Include files option of
  the Storage command (/SI) when no files have
  been included in the model.

SOLUTION:
 Use /SI only after including files in the
  model.
016
(EDIT) INVALID DEVICE DRIVER NAME

CAUSE:
 Attempting to enter a device driver name that
  is not one of the device drivers available in
  the TKDRIVER.CFG file.

SOLUTION:
 Enter a name from the list of names displayed
  in the Configuration Editor's information area
  when the cursor is in the Device Driver column.
 If the information area displays the message
  "TKDRIVER.CFG not found; device driver choices
  not available", shell to DOS (Shift-F4) or
  quit TK and copy the file TKDRIVER.CFG from
  the product disk to the TK directory.
013
INVALID FILE SPECIFICATION

CAUSE:
  A string of up to 200 characters may be
  entered as a filename, but the length and
  validity of a file specification is usually
  determined by the operating system of the
  computer.

SOLUTIONS:
 Check your operating system manual for a valid
  filename.
 Enter a valid filename.
012
FILE DOES NOT EXIST

CAUSE:
  Attempting to load a file that does not exist
  in the specified directory.

SOLUTIONS:
 Specify an existing filename (and a pathname
  if applicable) for the file to be loaded.
 Access the Disk Index to view the files
  available in the current directory and
  subdirectories.
014
FILE IS PROTECTED

CAUSES:
 You attempted a storage action to a filename
  that is protected by the operating system.
  (If the file is write protected because it is
  part of a commercial application, you may
  want to save to a different filename.)
 You attempted a storage action specifying
  a directory rather than a filename.

SOLUTIONS:
 Specify another filename or drive specifier.
 Insert another disk.
011
DISK IS PROTECTED

CAUSE:
  Attempting to save a model to a
  write-protected disk.

SOLUTIONS:
 Take the write-protection tab off the
  disk, so it can be written to.
 Save to a different disk which is not
  write-protected.
012
DISK SPACE OR DISK DIRECTORY FULL

CAUSES:
 Attempting to save to a disk that has too
  little space left to store the file.
 The disk already contains the maximum number
  of files allowed on the disk by your operating
  system.

SOLUTIONS:
 Delete some files first.
 Save the file to a different disk.
001
DISK RELATED SOFTWARE ERROR
014
NO SUCH DEVICE OR DEVICE NOT READY

CAUSE:
  Most likely you tried to save a file to a
  drive that was not ready (no disk, or drive
  door not closed).

SOLUTIONS:
 Check the disk and the drive.
 Check the drive specifier in the file
  specification.
 See your operations manual for further
  information about device names and drive
  specifiers.
012
DISK DATA OR I/O ERROR

CAUSES:
 You attempted an action that caused a hardware
  read/write error.
 You attempted to use a worn or damaged disk.

SOLUTION:
  Try another disk; refer to your hardware
  operations manual for hardware diagnostic
  tests; note this message and contact your
  computer dealer.
011
DISK HARDWARE ERROR

CAUSE:
  You attempted an action that caused a hardware
  read/write error.

SOLUTION:
  Try another disk; refer to your hardware
  operations manual for hardware diagnostic
  tests; note this message and contact your
  computer dealer.
009
FILENAME CONTAINS WILDCARD

CAUSES:
 You specified a filename on the command line
  that contained a wildcard.

SOLUTION:
  Do not used wildcards in this situations;
  specify an exact filename instead.
006
SYSTEM ERROR

CAUSE:
  This error message appears as the result of
  an unknown program error that interfered with
  the evaluation of an expression.
008
DIVISION BY ZERO

CAUSES:
 Attempting to enter an expression the
  evaluation of which would involve division
  of a non-zero number by zero.
 Attempting to divide a non-zero number by
  zero in the process of solving a rule.
011
EXPONENT OVERFLOW

CAUSE:
  An arithmetic operation or function evaluation
  produced a value that is outside the numeric
  range of TK, i.e. its absolute value is
  greater than 1E308.

NOTE:
  Exponent underflow is ignored (the result is
  taken as zero).
006
INVALID NUMERIC ARGUMENT

CAUSE:
  An expression evaluated to a numeric value
  where a symbolic value or a string value was
  required.
005
SQRT: ARGUMENT ERROR

CAUSE:
  Attempt to take the square root of a negative
  argument.
005
LOG: ARGUMENT ERROR

CAUSE:
  Attempt to take logarithm of zero or negative
  argument.
008
EXPONENTIATION: ARGUMENT ERROR

CAUSE:
  Attempt to perform the direct or inverse
  exponentiation operation (^) with negative
  argument and fractional exponent combined
  (e.g. evaluating (-32)^.2 or solving y=x^2
  for x when y is negative).
006
ASIN: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate arc sine of an argument
  with absolute value greater than 1.  (SIN
  inverse may be responsible.)
006
ACOS: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate arc cosine of an argument
  with absolute value greater than 1.  (COS
  inverse may be responsible.)
006
ASINH: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate hyperbolic arc sine of an
  argument with absolute value greater than
  1E154.  (SINH inverse may be responsible.)
007
ATANH: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate hyperbolic arc tangent
  with an argument with absolute value greater
  than or equal to 1.  (TANH inverse may be
  responsible.)
006
ATAN2: ARGUMENT ERROR

CAUSE:
  Known argument and function values are not
  consistent with each other, e.g. z=0 and
  y=1 in z=atan2(y,x).
018
APPLY: ARGUMENT ERROR

CAUSES:
 Supplying a first argument for the APPLY
  function that is neither a symbolic value
  representing the name of a function nor an
  expression evaluating into such a symbolic
  value.

 Referencing the APPLY function using a
  function which must be called, not referenced.
    For example,   y = apply(fun,'x),
                        where fun='blank
    should be      call apply(fun,'x)

 Trying to apply a Procedure Function which has
  two or more outputs, even if the CALL
  construct is used.
007
VALUE: ARGUMENT ERROR

CAUSE:
  Attempt to use built-in function VALUE with
  an argument which does not evaluate into a
  symbolic value that is the name of an existing
  variable.
006
COUNT: ARGUMENT ERROR

CAUSE:
  Using the COUNT function with a single
  argument which does not evaluate into a
  symbolic value that is a list name.
008
DOT: ARGUMENT ERROR

CAUSE:
 The first argument to the DOT function is not
  a symbolic value that is the name of a list.
 Only two arguments were given, and the second
  is not a symbolic value that is the name of a
  list.
009
ELEMENT: ARGUMENT ERROR

CAUSES:
 The first argument to ELEMENT is not a
  symbolic value which is the name of a list.
 The second argument is not an integer.
 The second argument is not a valid element
  number in the list, and no third argument
  (representing a default value) was given.
009
MAX: ARGUMENT ERROR

CAUSE:
  When the MAX function is given a single
  argument, the argument must evaluate to a
  symbolic value that is the name of a
  non-empty list.  The error occurs when the
  user provides a single argument that does not
  meet these conditions.
009
MIN: ARGUMENT ERROR

CAUSE:
  When the MIN function is given a single
  argument, the argument must evaluate to a
  symbolic value that is the name of a
  non-empty list.  The error occurs when the
  user provides a single argument that does not
  meet these conditions.
007
NPV: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  numeric value.
 The second argument is a symbolic value that
  is not the name of a list.
007
POLY: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  numeric value.
 The second argument is a symbolic value that
  is not the name of a list.
009
SUM: ARGUMENT ERROR

CAUSE:
  When the SUM function is given a single
  argument, the argument must evaluate to a
  symbolic value that is the name of a list.
  The error occurs when the user provides a
  single argument that does not meet these
  conditions.
006
USER FUNCTION: ARGUMENT ERROR

CAUSE:
  This error occurs when the argument given to a
  user-defined function is not in the domain of
  the function.
005
MOD: ARGUMENT ERROR

CAUSE:
  The second argument of the MOD function
  evaluates to zero.
006
SINH: ARGUMENT ERROR

CAUSE:
  Function argument outside the domain.
  Domain:   -710 ó arg. ó 710
  Range : -1E308 ó value ó 1E308
006
COSH: ARGUMENT ERROR

CAUSE:
  Function argument outside the domain.
  Domain: -710.476 ó arg. ó 710.476
  Range :        1 ó value ó  1.798E308
006
TANH: ARGUMENT ERROR

CAUSE:
  Function argument outside the domain.
  Domain: -1E308 ó arg. ó 1E308
  Range :     -1 ó value ó 1
006
ACOSH: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate hyperbolic arc cosine
  of an argument less than 1 or greater than
  1E154.  (COSH inverse may be responsible.)
005
LN: ARGUMENT ERROR

CAUSE:
  Attempt to take logarithm of zero or negative
  argument.
009
POWER: ARGUMENT ERROR

CAUSE:
  The second argument supplied to the function
  was not an integer.

SOLUTION:
  Modify the model so that the second argument
  evaluates into an integer.
006
ERF: ARGUMENT ERROR

CAUSE:
  Attempt to evaluate an inverse erf of an
  argument with an absolute value greater
  than 1.
007
GAMMA: ARGUMENT ERROR

CAUSES:
  - Attempt to evaluate the gamma function of
    zero or a negative integer.
  - Attempt to evaluate the inverse gamma
    function of a value less than .886 .
007
PLACE: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  symbolic value that is a list name.
 The second argument does not evaluate to an
  integer.
001
GIVEN: ARGUMENT ERROR
001
KNOWN: ARGUMENT ERROR
001
EVLTD: ARGUMENT ERROR
010

CAUSES:
  - An argument specifying a variable to be
    examined does not evaluate into a symbolic
    value.
  - Attempt to use two arguments.

SOLUTIONS:
  - Check and modify the arguments.
  - Use 1, 3 or more arguments.
007
BLANK: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  symbolic value that is a list name.
 The second argument, if present, does not
  evaluate to an integer.
009
LENGTH: ARGUMENT ERROR

CAUSE:
  The function argument did not evaluate to a
  valid list name.

SOLUTIONS:
 Edit the reference so the argument is valid.
 Enter a valid input entry for the argument.
010
MEMBER: ARGUMENT ERROR

CAUSE:
  Only two arguments were given and the second
  argument does not evaluate to a valid list
  name.

SOLUTIONS:
  Edit the function reference so that the
  arguments are valid.
009
BESSEL: ARGUMENT ERROR

CAUSES:
 The first argument (specifying the kind of
  Bessel function) was not 1 or 2.
 The second argument (the order of the Bessel
  function) was not an integer.
 The third argument was negative or zero and
  the Bessel function was of the second kind.
009
LISTCOPY: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  symbolic value that is a list name.
 The second argument does not evaluate to a
  symbolic value.
 One of the additional optional arguments does
  not evaluate to an integer.
012
VECELT: ARGUMENT ERROR

CAUSES:
 The first argument does not evaluate to a
  symbolic value that is a variable name.
 The second argument does not evaluate to a
  line number in the Rule Sheet (i.e. a positive
  integer less than or equal to 32000).

SOLUTION:
  Examine and correct the function arguments
  and/or their source.
012
STRING FUNCTION: ARGUMENT ERROR

CAUSES:
 An offset or length argument to a string
  function, e.g.
    n in LEFT(string,n)
    n in RIGHT(string,n)
    n1 or n2 in MID(string,n1,n2)
  evaluated to something other than a
  positive integer.
 A numeric argument to the JOIN function
  evaluated to a noninteger.
009
EVAL: ARGUMENT ERROR

CAUSE:
 A string argument could not be parsed into an
  expression which could be evaluated.

NOTE:
 This message is returned by the functions
  CEVAL and GEVAL as well as EVAL.
015
RECURSIVE FUNCTION CALL

CAUSES:
 Attempting to invoke a rule function that
  calls itself either directly or indirectly
  through a "chain" of functions.
 Attempting to use the built-in function APPLY
  to call a user-defined function that directly
  or indirectly calls APPLY.

SOLUTIONS:
 Edit one of the functions so that the
  functions do not call each other.
 Edit the rule function so that it does not
  call itself.
011
GOTO TO NON-EXISTENT LABEL

CAUSE:
  The GOTO statement specifies a label name that
  does not exist within the procedure function.

SOLUTIONS:
 Change the GOTO statement so that it
  specifies an existing label name.
 Add the label name elsewhere so the GOTO
  statement will be able to make a jump to it.
010
FOR WITHOUT NEXT

CAUSE:
  Attempting to execute a procedure function
  that contains a FOR statement without a
  corresponding NEXT statement.

SOLUTION:
  Add a NEXT statement to match the appropriate
  FOR statement.
013
MISMATCHED FOR AND NEXT

CAUSES:
 NEXT var_name  statement does not have same
  var_name as  FOR var_name:=....  statement
 At least two FOR-NEXT loops are overlapped.

SOLUTIONS:
 Change var_name so that it is the same
  variable name in both the FOR and the NEXT
  statement.
 Modify the code in the procedure function so
  that no two FOR-NEXT loops overlap.
011
NEXT WITHOUT FOR

CAUSE:
  Attempting to execute a procedure function
  that contains a NEXT statement that is not
  matched to a FOR statement.

SOLUTION:
  Modify the procedure function so that every
  NEXT statement is matched to the right FOR
  statement.
009
ZERO VALUE FOR STEP

CAUSE:
  Attempting to specify a Step value of zero in
  a FOR statement.

SOLUTION:
  Edit the statement and change the Step value
  so that it is not zero.
006
INVALID BOOLEAN ARGUMENT

CAUSE:
  Attempt to evaluate a Boolean function or
  inverse Boolean function of an argument or
  arguments other than 0 or 1.
005
INVALID SYMBOLIC COMPUTATION

CAUSE:
  Attempt to perform a numeric operation on
  a symbolic value, e.g. 3*'p or sin('q).
014
FUNCTIONS NESTED TOO DEEP

CAUSE:
  User-defined functions made more than 24
  nested calls, most likely as a result of
  recursive procedure function calls.

SOLUTIONS:
 If the procedure function was meant to be
  recursive, walk through the code and modify
  it so that it will not make too many
  recursive calls.
 If it was not meant to be recursive, modify it
  so that it will not call itself.
014
WRONG NUMBER OF ARGUMENTS

CAUSE:
  Attempting to resolve a rule or statement
  which refers to a user-defined function which
  has had its number of arguments changed since
  the rule or statement was entered.

SOLUTIONS:
 Edit the rule or statement so that the number
  of arguments matches the number of arguments
  in the user-defined function's definition.
 Modify the user-defined function to correspond
  to its usage in rules and/or statements.
016
ITERATION: DEPENDENCY ERROR

CAUSES:
 The number of independent equations in the
  model is less than the number of unknowns.
 The error terms generated by guesses become
  too small or too insensitive to small changes
  in guess values.


SOLUTIONS:
 Check the choice of guess variables and/or
  guess values.
 Check the number of independent equations vs.
  the number of unknowns; modify the model and/
  or reformulate the problem.
012
OPERATION ABORTED

CAUSES:
 Ctrl-Break was pressed during solution of the
  model or during execution of a keystroke
  macro.
 Esc, F2, or Ctrl-Break was pressed in response
  to a message or menu generated by BOXMSG,
  DEBUG, MENU, PAUSEMSG, PROMPT, or QUERY.
 Esc, F2, or Ctrl-Break was pressed while macro
  stepping was on, as indicated by the "Macro
  stepping" message displayed on the screen.
013
ITERATION: TOO MANY GUESSES

CAUSES:
 The number of error terms generated by
  guesses is less than the number of guess
  variables.
 Some of the error terms are too small (i.e.
  they are within the Comparison Tolerance).

SOLUTIONS:
 Solve the problem with a reduced number of
  guess variables.
 Change the guess values.
021
 Scale the problem up or down; review the
  strategy of using the Iterative Solver.
 Change the settings of the Comparison Tole-
  rance and Typical Value on the Global Sheet.

NOTE:
  A good start to sort out the problem if the
  cause and remedy are not obvious:
1. Change the guess variables into plain input
   variables by toggling G in their Status
   Field, and solve again (F9).
2. Look up a rule marked with > (inconsistent).
3. Go into Editor (F5) and modify the rule by
   adding an error term (say, err1) to its left
   hand side.
4. Repeat solving and adding error terms to
   inconsistent rules as long as there are any
   left.
5. Examine the effect of intended guess
   variables on the error term variables which
   have been appended to the Variable Sheet.
017
OUT OF MEMORY

CAUSE:
  There is no room to accommodate additional
  information generated during some operation
  (iteration, list operations, ASCIIREAD,
  recursion in a procedure or keystroke macro,
  etc.).

SOLUTIONS:
 Save information with the Storage command
  and then clear some or all of the model
  with the Reset command (/R) or Delete
  command (/D).
 Limit recursive procedures and macros to
  a number of recursions that can fit within
  available memory.
012
USER-DEFINED ERROR

CAUSE:
  The model resolution was terminated by
  executing the built-in function ERRMSG
  to flag a model-specific error.

SOLUTION:
  Look up the rule or statement with the
  executed ERRMSG function, analyze the path
  leading to it, and modify the data or model
  to avoid the recurrence.
010
UNDEFINED USER FUNCTION

CAUSE:
  Attempting to evaluate a rule or statement
  that refers to an undefined user function.

SOLUTION:
  Define the needed function on the Function
  Sheet and Subsheet, or edit the rule or
  statement to only use defined functions.
033
UNEVALUATED EXPRESSION IN STATEMENT

CAUSES:
 An undefined variable, e.g. TK cannot
  perform the assignment  a:= b  if  b
  is currently unknown.
 An indeterminate expression, e.g.
  c:= exp1/exp2  where  exp1  and  exp2
  both evaluate to 0.
 A square bracket construct referring to a
  list element is missing an apostrophe, e.g.
  xy[k]:= 5  where you intended  'xy[k]:= 5
  (the former is valid only if the variable
  xy  currently has a symbolic value that is
  a legal TK name, say  'xy  or  'Z).


SOLUTION:
  Search Procedure Function and Keystroke Macro
  Subsheets to locate the error symbol (>) that
  marks the Status field of the errant statement,
  then use the Examine command (/E) to evaluate
  expressions and variables in the unevaluable
  statement.

NOTE:
  Error symbols also mark a chain of calling
  rules and/or statements leading to the
  procedure or macro containing the errant
  statement.  Start your search on the Rule
  Sheet.  If there is no error mark there,
  continue with the subsheet of the macro
  whose invocation began model execution.
025
NOT IN LOOP

CAUSES:
 CONTINUE or EXIT statement that is not inside
  any for-next loop at all.
 CONTINUE varname  or  EXIT varname  to a
  NEXT varname  statement that is either not
  found anywhere inside the current outermost
  loop or is found at a level nested below the
  level of the CONTINUE or EXIT statement.  An
  example of the latter:
    FOR i=1 TO 5
      CONTINUE j     ; CONTINUE j on 1st level
        FOR j=1 TO 5
        NEXT j       ; but NEXT j on 2nd level
    NEXT i

SOLUTIONS:
 Delete the offending statement.
 If the marked statement is outside any loop,
  move it inside a loop.
 Edit the marked statement so that it branches
  to a  NEXT varname  statement contained some-
  where in the current main loop but not at a
  level below the marked statement.
008
INVALID REPEAT COUNT

CAUSE:
 Evaluation to something other than a positive
  integer for an expression serving as a repeat
  count for a key designator in a macro command.
  Examples: {Right 2.3}
            {Down k*j} if k*j=-3
004
STRING TOO LONG

CAUSE:
 The JOIN function exceeded 200 characters.
021
BOXMSG: Argument error

CAUSES:
 The first argument (specifying either a
  comment name or a list name) is not a
  symbolic value for a valid TK name.
 The comment or list specified by the first
  argument does not exist.
 The second argument (specifying the title) is
  a numeric value rather than a symbolic value.

SOLUTIONS:
 Make sure that the first argument evaluates to
  a symbolic value naming an existing comment
  or list.
 Make sure that the second argument evaluates
  to a symbolic value.  If you want to specify a
  box position other than the default but don't
  want a box title, use an empty string for the
  second argument, e.g.
    CALL BOXMSG('box1,"",.2,.8)
017
MENU: Argument error

CAUSES:
 There are two arguments, and the second is not
  the name of a list, or is the name of an empty
  list.
 Every menu item in the list or series is an
  empty string (""), a blank list element, or
  a string beginning with a blank.

SOLUTIONS:
 If there are only two arguments, make sure the
  second names a non-empty list.
 Make sure the menu items in the list or series
  are not all empty or blank.
 Do not begin a symbolic value for a selectable
  menu item with a blank space.
009
ASCIIWRITE: Argument error

CAUSE:
 One of the arguments after the first argument
  is not the name of an existing list.

SOLUTIONS:
 Name only existing lists when calling this
  function.
036
ASCII function: File error

CAUSES:
 Attempting to use a text string with no
  literal backslashes in the path to the
  specified filename, e.g. attempting
   CALL ASCIIREAD("\tk\special\prm10000.asc")
  instead of the correct
   CALL ASCIIREAD("\\tk\\special\\prm10000.asc")
 The specified file could not be opened, could
  not be written to, or ran out of space during
  an ASCIIWRITE.
 The specified file could not be opened or was
  not a valid ASCII file for an ASCIIREAD.

SOLUTIONS:
 Check that each backslash in the path is
  represented by a double backslash in the
  character string (the first forces interpre-
  tation of the second as a literal character).
 Verify that the path/filename argument has no
  typos, does not contain wildcards, specifies a
  file instead of a directory, and (for a read)
  specifies an existing file that is a valid
  ASCII file for TK.
 Verify that the drive and file are not write
  protected (for a write).  If the file is
  protected because it is part of an application
  you may want to avoid overwriting it and save
  the data to a new file instead.
 If you are using a floppy drive, check that a
  formatted floppy is in the drive and the drive
  door is closed.
 If the problem persists, check for a hardware
  problem (bad cable connections, faulty disk
  media, etc.)
016
INCONSISTENT

CAUSE:
  The marked rule was checked by the Direct
  Solver and found in error.  This happens most
  often when all variables in an equation have
  input values or were evaluated from other
  equations, and the difference between the
  left and right sides is greater than the
  Comparison Tolerance.

SOLUTION:
 Blank the input value of a variable marked
  'Overdefined' or some other variable that
  may be responsible.
 Delete, cancel or edit the offending rule.
064
The GOTO command
  moves the cursor to any location on the
  current sheet, or searches for a string
  of characters in the current column.

MOVING the cursor with GOTO:
 Type  :  to issue the Goto command.
 Type the number of the destination row.
 If you want to move to a different column as
  well as a different row, type the first letter
  of the heading of the destination column (e.g.
  "i" for Input column).
 Press ÄÙ.

NOTES on moving the cursor with GOTO:
 The Goto command accepts two numeric arguments
  if the cursor is in an Interactive Table, e.g.
  :3,5ÄÙ  moves cursor to 3rd row, 5th column.
SEARCH for BEGINNING of a character string:
 Move the cursor to the column to be searched.
 Type  :"
 Type a search string of characters to search
  for (upper and lower case letters must match).
 Press ÄÙ.

TK searches for the first field whose initial
characters match those specified, starting with
the field under the cursor and ignoring leading
blanks in both the search string and contents
of fields.  The search string must not be wider
than the width of the current column.





SEARCH for EXACT character string:
 Move the cursor to the column to be searched.
 Type  :'
 Type a search string of characters to search
  for (upper and lower case letters must match).
 Press ÄÙ.

Starting with the field under the cursor, TK
searches for the first field whose entire field
contents exactly match the specified search
string.  Leading blanks are significant.  The
search string can be wider than the width of
the current column.





NOTES on searching with GOTO:
 TK searches the entire column, going
  downward from the current cursor location
  and cycling through the top of the column.
 If you use either search option without a
  search string, i.e.  :"ÄÙ  or  :'ÄÙ ,
  TK remembers the last search string and
  searches for it again.  In this case,
  however, the field under the cursor is
  NOT included in the search.
007
HELP CALLED WHILE VIEWING A PLOT

The Help facility automatically resets the
graphics mode when it is invoked while a plot is
being displayed in any high-resolution mode.
Type F7 to display the plot again after quitting
Help.
012
OVERWRITING A FILE
  means saving new information in the file in
  place of old information.

USAGE:
 If you want to replace the old information
  with the new information, confirm the save
  command by typing Y.  This DESTROYS the old
  contents of the file.
 If you want to preserve the old file, type N
  to cancel the command.  Save the new file
  under another filename.
007
GETTING STARTED

  If you have never used TK before, work through
  the tutorials in the Introduction Manual.  If
  you have used TK before, you may want to look
  at some models in the TK Library.

011
  If you started TK from the directory \TK, you
  can access the TK library by quitting Help and
  pressing the F11 function key (or Shift-F3 if
  your system doesn't have F11).  When prompted
  for a menu filename, type and enter TK2LIB.

  Use the arrow keys to browse around the menu
  screen and press Enter to select and load a
  model.  (If TK beeps, you simply need to go
  farther downward to reach the model level.)

011
NOTES:
 TK can be configured to your computer system
  with a TK2.CFG file (see "Configuration").
 You may find it helpful to read through the
  rest of the major topics in the box at the
  beginning of the Help Index.
 You can load a model at the same time you
  start up TK.  For example, from DOS  type:
          TK2 \TK\TUTORIAL\LOAN ÄÙ
  to start TK and then load the model LOAN
  from the subdirectory \TK\TUTORIAL.
011
RESET ALL PV BOXES ?

TK requires confirmation before resetting the
Presentation View.  Type Y to go ahead and
reset, N to discontinue the Reset command.

NOTE:
 Resetting PV boxes affects only the
  Presentation View.  It does NOT affect
  the contents of any objects linked to
  the Presentation View.
012
SELECT BOXES to Delete, Move, or Freeze.

 Select a box by moving the cue into it.  The
  border changes color to signal the selection.
 Once the cue is in a box, you can toggle
  between selected and not selected by
  pressing Spacebar.
 Select more than one box by moving the cue
  from one box to another.

Press Enter to perform the Delete, Move, or
Freeze command on all the selected boxes.
030
The WINDOW command (/W)
  controls the number of windows on the screen
  (one or two) and determines which sheet is
  displayed in the other window.

To change the display from two windows to
a SINGLE window:
 Place the cursor on the sheet you want to
  retain.
 Press F6, or type /W1.

To split the screen into TWO windows and/or to
display any sheet in the other window:
 Type /W.
 Type the first letter of the sheet you want
  displayed in the other window (or move the
  cursor in the Option Box to desired sheet and
  press ÄÙ).
NOTES:
 To display a different sheet in the current
  window, use the Select command (=) instead of
  the /W command.
 Ctrl-PgUp or Ctrl-UpArrow increases the size
  of the lower window and decreases the size of
  the upper window in the Sheet View.  Ctrl-PgDn
  or Ctrl-DownArrow have the opposite effect.
 The Window command only works in the Sheet
  View.  See related topics "SV" and "PV" for
  information on the Sheet View and the
  Presentation View.
016
The SELECT command (=)
  displays any TK sheet in the current window.

USAGE:
 Press  =  to issue the Select command.
 Type the first letter of the sheet you want
  to select, or move the cursor to the desired
  sheet name in the Option Box and press ÄÙ.

NOTES:
 You can use the Select command from any sheet
  or subsheet, or from the Presentation View.
  (In the latter case, you will be returned to
  the Sheet View.)
 To display a new sheet in the opposite
  window, use the Window command (/W).
016
The VARIABLE SHEET
  contains information about the variables used
  in the model.  Each row can contain data
  related to one variable.

  There are Variable Subsheets containing more
  information about each variable.  Items
  entered into fields in the Variable Sheet are
  updated simultaneously in corresponding
  fields in Variable Subsheets.

ACCESS:
 Type =V to display it in the current window.
 Type /WV to display it in the second window.
 Use the Dive command (>) to see the Variable
  Subsheet associated with a specific variable.
009
The RULE SHEET
  contains the equations for a model.  Each
  row can contain one equation and its status
  (and, optionally, a comment following the
  symbol ; ).

ACCESS:
 Type =R to display it in the current window.
 Type /WR to display it in the second window.
018
The FUNCTION SHEET
  contains a summary of all user-defined
  functions.  A function can be:
    - a list function,
    - a rule function, or
    - a procedure function.
  Each row can contain the description of one
  function.  Information entered in the Function
  Sheet is updated automatically in correspon-
  ding fields on Function Subsheets.

ACCESS:
 Type =F to display it in the current window.
 Type /WF to display it in the second window.
 Use the Dive command (>) to see the Function
  Subsheet associated defining a particular
  function, and the Return command (<) to
  return to the Function Sheet.
033
CHARACTER STRINGS

Prior to TK Solver Release 2.0, TK recognized
only TK names prefixed with an apostrophe as
valid symbolic values.  TK currently allows, in
addition, generalized character strings composed
of any typeable characters including blanks and
IBM extended-ASCII foreign letters and graphics
characters.

Valid TK names may still be preceded by a single
quotation mark, i.e. apostrophe (').  Character
strings which are not valid TK names must be
enclosed by double quote marks (") at beginning
and end.  Strings may be up to 200 characters,
including delimiting quotes.

Entries in Name fields of TK sheets are not
considered symbolic values and therefore do
not require any delimiting quotes.  Likewise,
strings in Comment fields, in Text fields, and
following a semicolon in a rule or statement do
not require any delimiting quotes.

The related topic Symbolic Value Format gives
information on formatting displays of strings.

Backslashes and double quote marks appearing
inside a string must be preceded by a backslash
to force interpretation as a plain character.
For instance, "\"\\TK\\RY\"" will be displayed
as "\TK\RY" if Symbolic Value Format is set to
No quotes, otherwise as "\"\\TK\\RY\"".
017
The KEYSTROKE MACRO SHEET
  contains a summary of all keystroke macros.
  Each row can contain the description of one
  macro.  Information entered in the Keystroke
  Macro Sheet is updated automatically in
  corresponding Keystroke Macro Subsheets.

ACCESS:
 Type =K to display it in the current window.
 Type /WK to display it in the second window.
 Dive (>) into and return from (<) the Macro
  Subsheet defining a particular macro.

NOTE:
 The related topics for keystroke macros are
  "chained".  Simply keep pressing ÄÙ to see
  them in sequence.
043
KEYSTROKE MACROS provide an outer layer of
  procedural programmability which can summon
  either the declarative (ÍÍÍÍ) or the
  procedural (ÄÄÄÄ) component of TK:

    ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
    ³           Keystroke Macros            ³
    ³   ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿   ³
    ³   ³  ÉÍÍÍ»  ÚÄÄÄÄÄÄÄÄÄÄÄ¿  ÉÍÍÍ»  ³   ³
    ³   ³  º   º  ³ Procedure ³  º   º  ³   ³
    ³   ³  º   º  ³ Functions ³  º   º  ³   ³
    ³   ³  º   º  ÀÄÄÄÄÄÄÄÄÄÄÄÙ  º   º  ³   ³
    ³   ³  º   ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼   º  ³   ³
    ³   ³  º    Rules and Solvers    º  ³   ³
    ÀÄÄÄÙ  ÈÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼  ÀÄÄÄÙ

Keystroke Macros automate tasks which would
otherwise be too laborious or too complicated
for a user to perform manually.

Virtually any keystroke sequence can be recorded
or programmed as a macro.  Here are only a few
of the practically limitless capabilities of
keystroke macros:

 Transform list values according to a function
  specified in response to a prompt.
 Automate the printing of reports.
 Optimize without special objective functions.
 Generate VEC (Variable/Equation Coincidence)
  matrices to help determine which variables to
  assign guess values for iteration.
 Examine the Unit Sheet and Variable Subsheets
  to check for missing unit conversions.
 Share knowledge and computing ability of
  experts with end users who may not be as
  familiar with the underlying mathematics or
  the sophisticated ways that TK can handle
  solutions to complex problems.

NOTE: The related topics for keystroke macros
      are "chained".  Simply keep pressing ÄÙ
      to see them in sequence.
014
KEYSTROKE MACRO NAMES

One macro can have either or both of two names:
 A TK name specified in the Name field.
 A single key (A-Z, 0-9, or F1-F10) specified
  in the Short field.

NOTES:
 Like other TK names, macro names specified in
  the columm of Name fields are case sensitive.
 Macro short names designate keys rather than
  symbolic values and are not case sensitive.
 No duplicate names are allowed within the
  Name column or within the Short column.
016
STARTUP MACRO

If a model containing a keystroke macro named
StartUp is loaded into a freshly run or reset
TK, that macro will be invoked automatically.

A StartUp macro is comparable to AUTOEXEC.BAT on
a PC, .login on a UNIX system, or INIT files on
a Macintosh.  They all act to perform a set of
tasks automatically when a fresh start is made.

Different TK models can each have a different
StartUp macro with contents customized to match
the individual model.  You can use StartUp to
set the screen display, present information via
box messages, present an option menu, etc.
016
RUNNING KEYSTROKE MACROS

There are two ways to run a macro:
 Type Ctrl-K, then enter the full macro name
  at the prompt, e.g.  Ctrl-K Unit_Check ÄÙ
 Hold down the Alt key and type the letter,
  number, or function key which designates the
  macro short name, e.g.  Alt-U, Alt-4, Alt-F7

NOTES:
 Since macros can emulate keystrokes, one macro
  can call another.  Two examples:
    {{Ctrl-K}Unit_Check{Enter}}
    {{Alt-U}}
 Full names for macros are case sensitive, but
  short names are not.
030
RECORDING KEYSTROKE MACROS

Keystroke macros can be created in two ways:
Typing and entering statements in a Keystroke
Macro Subsheet, or recording manual keystrokes.

TO RECORD A MACRO:
 Type Ctrl-R to initiate recording.
 Enter either a full or a short macro name.
  - A short name must be preceded by an Alt key
    designator (Alt or A) and a separator
    (hyphen or blank), e.g. Alt-S, A-9, a F4.
  - If the macro already exists, TK prompts
    whether to extend or replace its contents.
 Type the keystrokes you want to record.
 Type Ctrl-R to terminate recording.

All keystrokes performed between response to
the prompts and the 2nd Ctrl-R will appear as
statements recorded in the specified macro.

The following keystrokes will be recorded as
the corresponding keystroke macro commands.

    Ctrl-K        {Macro}
    Ctrl-S        {Step}
    Ctrl-F1       {CharIn}
    Ctrl-F2       {LineIn}
    Ctrl-F5       {FieldEdit}
    Ctrl-L        {Redisplay}
061
STATEMENTS in Keystroke Macros

Like procedure functions, keystroke macros use
assignment statements, FOR-NEXT loops, labels,
and conditional and unconditional branching to
control the flow of execution.  Also like
procedures, macros can use locally defined
variables and Parameter Variables.

Unlike procedures, macros have neither Input nor
Output Variables.  However, macro statements can
contain keystroke sequences and macro commands.
Subsequent screens in this topic describe only
syntax unique to keystroke macros.  A related
topic describes syntax of statements common to
procedure functions and keystroke macros.
A macro executes a keystroke sequence enclosed
in braces { } just as if you typed the keys
manually.  For instance,  {=F/S1FR}  will bring
up the Function Sheet and initiate the command
to save a range of functions.

{=f/s1fr} will do the same since TK commands and
built-in function names are not case sensitive.
Upper/lower case does matter when keystrokes are
applied to entry fields:  {   d'Artagnan}  will
type into the field under the cursor 3 blanks
followed by the name d'Artagnan.

As well as surrounding a keystroke sequence,
braces can enclose a macro command or a key
designator for a function key or special key.
For example,  {/WV{F6}{Pause}}  will make sure
the Variable Sheet is in a single window, then
pause the execution of the macro until the user
presses a key.  (Macro commands and key desig-
nators are included in the current chain of
topics.)

A few characters with special meanings must be
preceded by a backslash if they are to be
interpreted as a keystroke rather than in the
context of their special meaning:

  \~  Tilde, legal in TK names but stands as a
      shortcut for {Enter} if not preceded by \
  \{  Left brace, normally begins a keystroke
      sequence or encloses a macro command
  \}  Right brace, normally ends a keystroke
      sequence or encloses a macro command
  \\  Backslash, normally forces keystroke of a
      special character

Examples:

  {{F3}\\USER1\\ABC.TK~} ; load file ABC.TK from
                         ; subdirectory  \USER1
  {=V:1n~:"x\~n~} ; bring up Variable Sheet, go
                  ; to first row in Name column,
                  ; search for variable name
                  ; that begins with  x~n
073
KEYSTROKE MACRO COMMANDS
  are equivalent to certain keystrokes (e.g.
  {Macro} for Ctrl-K) or invoke actions for
  which keystrokes are not available.

{Macro}  Same as Ctrl-K, initiates invoking a
         macro from the Prompt/Error line.
{Step}   Same as Ctrl-S, toggles stepping mode
         on and off.  When on, pressing any key
         advances the macro by one keystroke.
{Pause}  Temporarily halts macro execution,
         which is then resumed by pressing any
         key or terminated with Ctrl-Break.
{Doff} or {DisplayOff}  Turns off redisplay of
         sheets until {Don} is encountered or
         execution of all macros is terminated.
         Suspends macro stepping mode.  Does not
         turn off display of prompts and menus
         created by DEBUG, MENU, PROMPT, and
         QUERY functions or the Macro error:
         Continue-Stop-Abort message.
{Don} or {DisplayOn}  Resumes sheet redisplay.
         Redisplay is also switched on when all
         macros are done executing.
{Redisplay}  Same as Ctrl-L, causes screen to be
         refreshed.  Does nothing if redisplay
         has been turned off by {Doff}.
{Type sarg}  Types in at current cursor location
         the characters in  sarg,  which must
         evaluate into a symbolic value.
         (Should be followed by {Enter} or a
         navigation keystroke.)
{CharIn} Invokes user input of a single
         character.  (Cursor should be in a
         field which can accept a character.)
{LineIn} Invokes user entry of a sequence of
         characters terminated by the Enter key.
         (Cursor should be in a field which can
         accept an entry.)
{FieldEdit}    Invokes edit mode if cursor is in
         a field which can accept an entry.  The
         first subsequent press of the Enter key
         ends the command, the second (which may
         be built into the macro) enters the
         contents into the field.
{ErrStopOn}    Stops the macro when an error
         occurs and an error message is posted
         on the Prompt/Error line.  The user is
         prompted whether to Continue, Stop the
         current macro, or (default) Abort all
         macro execution.
{ErrStopOff}   Causes macro to ignore any error
         condition and continue macro execution.
{ErrHandleOn}  Allows a calling macro to resume
         control rather than halting execution
         when a called macro encounters an error.
{ErrHandleOff} Revokes {ErrHandleOn}.  This is
         the default setting
{Esc}    Same as pressing Esc key.
{SelfDestruct} Removes the executed macro from
         the Keystroke Macro Sheet.  Use with
         caution, and never in recursive calls.


NOTES:
 Macro commands are not case sensitive.
 Outermost braces enclose keystrokes and do not
  serve in the syntax for a macro command, e.g.
    {LineIn}
  will only type the characters  LineIn  at the
  current cursor location.  Use
    {{LineIn}}
  to invoke the {LineIn} command.
044
KEY DESIGNATORS in Keystroke Macros

Function Keys:

{F1}  {Help}            {F2}  {Cancel}
{F3}  {Load}            {F4}  {Save}
{F5}  {Edit}            {F6}  {OneWindow}
{F7}  {Plot}            {F8}  {Table}
{ShiftF7} {LinkPlot}    {ShiftF8} {LinkTable}
{F9}  {Solve}           {F10} {ListSolve}
{F11} {ShiftF3} {Menu}  {F12} {ShiftF4} {System}

Special Keys:

{Home}    {End}   {PgUp}   {PgDn}
{Enter}   {Ins}   {Del}    {Esc}
{Up}                  Up-Arrow
{Down}                Down-Arrow
{Left}                Left-Arrow
{Right}               Right-Arrow
{Space}               Space
{BS} {Backspace}      Backspace
{Tab}                 Tab
{BackTab}             Shift-Tab
{CtrlUp} {CtrlPgUp}   Ctrl-Up-Arrow / Ctrl-PgUp
{CtrlDown} {CtrlPgDn} Ctrl-Down-Arrow/Ctrl-PgDn
{WordLeft}            Ctrl-Left-Arrow
{WordRight}           Ctrl-Right-Arrow

Either  ALT  or  A  without regard to case may
designate the Alt key, and either a hyphen or a
blank may separate the Alt from the required
short name of a macro to be invoked, e.g.
{Alt-S} ,  {A-9} ,  {Alt F10} ,  {a 3} .

Key designators may be followed by a blank plus
an expression argument that evaluates to an
integer repeat count.  Examples:

  {{Right 2}} ; move cursor right two fields
  {{Down k*(i>j)}} ; move cursor down k fields
                   ; if i is greater than j
  {{Alt-X 3}} ; invoke the macro with the
              ; short name  X  three times
021
The PRESENTATION VIEW provides a canvas on which
you can display a portrait of your TK model.

The portrait consists of boxes which may contain
text, graphs, or tables.  The boxes may be moved
around on the screen and resized.  The types of
boxes and how they are created are discussed
individually in the related topics.

 Type  \  to display the Presentation View.
 Use the Arrow keys, PgUp, PgDn, Ctrl-PgUp,
  Ctrl-PgDn, Tab, and Shift-Tab to navigate
  the block cue around the screen.  You can
  scroll vertically but not horizontally.
 Type  Esc  to leave the Presentation View.

NOTE:
 When used from the Presentation View, the
  Slash command presents options for moving,
  resizing, and organizing existing boxes.
  (See related topic.)
048
A FIELD BOX contains information linked to a
column of fields in a TK sheet or subsheet.

You can move the block cue into a Field Box
in the Presentation View and add, delete, or
edit the field contents in the linked sheet or
subsheet in much the same way as if you were in
the Sheet View.  (See the related topic Slash
Commands in PV.)

Links to Field Boxes work in the other direction
as well:  Contents of Field Boxes are updated
automatically by new solutions and by changes
made to the linked objects in the Sheet View.


To create a field box:
 Start from the Sheet View and use the Copy
  command in the normal way.
 Type  \  when prompted "Copy; move cursor to
  destination."  The Presentation View will come
  up showing a blue outline of the new box.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the outline if desired.
 Press the Insert key and use the Arrow, PgUp,
  PgDn, Tab, and Shift-Tab keys to resize the
  box outline if desired.
 Press ÄÙ to finalize creation of the new box.
  The outline and contents will appear in white.



NOTES:
 A Field Box linked to an Interactive Table
  Subsheet can be resized to include more than
  one column in one box.  This is not true of
  Field Boxes linked to other kinds of sheets
  and subsheets.
 A Field Box linked to an Interactive Table
  Subsheet allows you to add, delete and edit
  elements in the interactive table while you
  are in the PV, i.e. the link works in both
  directions.  A Table Box is only for display,
  not entry, and does not allow modification
  while in the PV except by updating with a
  new solution.


018
A PLOT BOX displays in the Presentation View a
plot defined by a row in the Plot Sheet.

To create a plot box:
 Start from the Sheet View and use Shift-F7
  instead of F7.  The Presentation View will
  come up showing a blue outline of the new box.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the outline if desired.
 Press the Insert key and use the Arrow, PgUp,
  PgDn, Tab, and Shift-Tab keys to resize the
  outline if desired.
 Press ÄÙ to finalize creation of the new box.

Plots in plot boxes are updated automatically as
new solutions occur.  You can use the Grow Box
command  / G  to resize a Plot Box, but you
cannot otherwise affect a plot in a Plot Box.
019
A DISPLAY TABLE BOX displays in the Presentation
View a plot defined by a row in the Table Sheet.
Unlike a Field Box linked to an Interactive
Table Subsheet, a Display Table Box is strictly
output and does not allow entries in the box.

To create a Table Box:
 Start from the Sheet View and use Shift-F8
  instead of F8.  The Presentation View will
  come up showing a blue outline of the new box.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the outline if desired.
 Press the Insert key and use the Arrow, PgUp,
  PgDn, Tab, and Shift-Tab keys to resize the
  outline if desired.
 Press ÄÙ to finalize creation of the new box.

Tables in table boxes are updated automatically
as new solutions occur.
010
A TEXT BOX contains text which exists only in
the Presentation View and is not linked with
anything else in TK.

To create a new text box, simply move the cue to
some location not in any existing box and start
typing.  All characters are allowed, including
IBM extended-ASCII characters such as character
graphics and Greek and International letters
(see related topics).
008
The UNIT SHEET
  contains definitions for conversions between
  units of measurement.  Each row in the Unit
  Sheet defines a conversion between two units.

ACCESS:
 Type =U to display it in the current window.
 Type /WU to display it in the second window.
017
The LIST SHEET
  contains a summary of all lists in the current
  model (a list is a series of elements which
  are numeric or symbolic values or blanks).
  Each row in the List Sheet can contain the
  description for one list.  Information entered
  in the List Sheet is updated automatically in
  corresponding fields on List Subsheets.  The
  List Subsheet contains more information about
  each list including all element values.

ACCESS:
 Type =L to display it in the current window.
 Type /WL to display it in the second window.
 Use the Dive command (>) to see the List
  Subsheet for a particular list, and the Return
  command (<) to return to the List Sheet.
048
PLOT SHEET
  A plot is a graphical representation of the
  relationship between lists of values.  The
  Plot Sheet contains a summary of all plots
  in the current model.

  Plot Subsheets contain detailed specifications
  for each particular plot.  Use the Dive
  command (>) to dive from the Plot Sheet into
  the Plot Subsheet highlighted by the cursor,
  and the Return command (<) to return to the
  Plot Sheet.

To Display a Plot:
 From anywhere within TK, press F7.  (The plot
  displayed is the one highlighted by the cursor
  the last time the Plot Sheet was displayed.)

To Return to the Current Sheet(s):
  Press ÄÙ. (Any key may be pressed to the same
  effect; if the key happens to be a command, it
  will be acted upon after return.)

To Send a Plot to an Output Device:
 Press  F7  to display the plot.
 Press the  O  key.
 Select an output device or file from the menu
  of entries in the Name in Printer Menu column
  in the Configuration Editor.
 If you selected a file option, enter a file
  name at the prompt.

To Print a Plot to a 9-Pin Dot-Matrix Printer:
 Press  F7  to display the plot.
 Press the  P  key to dump the plot to an IBM
  or compatible printer.  (You can also use the
  PrtSc key if the plot is in TEXT or, if you
  ran the GRAPHICS program in DOS before running
  TK, in LOWCGA or CGA.)

To Copy a Plot to the Presentation View:
 Press  Shift-F7.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the outline if desired.
 Press the Insert key and use the Arrow, PgUp,
  PgDn, Tab, and Shift-Tab keys to resize the
  outline if desired.
 Press ÄÙ to finalize creation of the Plot Box.
004
The LINE CHART Plot Subsheet

  serves for specification of the contents and
  format of the Line Chart plot.
004
The BAR CHART Plot Subsheet

  serves for specification of the contents and
  format of the Bar Chart plot.
004
The PIE CHART Plot Subsheet

  serves for specification of the contents and
  format of the Pie Chart plot.
003

  Press ÄÙ to get information on all
  subsheet fields.
062
TABLE SHEET
  A table shows any number of lists column-
  by-column or row-by-row.  The Table Sheet
  summarizes all tables in the current model.

  Table Subsheets contain detailed specifica-
  tions for each particular table.  Use the
  Dive command (>) to dive from the Table
  Sheet into the Table Subsheet for a table
  highlighted by the cursor, and the Return
  command (<) to return to the Table Sheet.

To Display a Table on the Screen:
 The Screen or Printer field in the Table
  Subsheet must be set to Screen (the default).
 From anywhere within TK, press F8.  (The table
  displayed is the one highlighted by the cursor
  the last time the Table Sheet was displayed.)

To Send a Table to an Output Device:
 The Screen or Printer field in the Table
  Subsheet must be set to Printer.
 Press  F8.
 Select an output device or file from the menu
  of entries in the Name in Printer Menu column
  in the Configuration Editor.  There is also
  an additional option of plain Text file.
 If you selected a file option, enter a file
  name at the prompt.

INTERACTIVE TABLE SUBSHEET:
  You can dive from a Table Subsheet into an
  Interactive Table Subsheet (or interactive
  table for short).  Interactive tables allow
  adding, deleting or changing elements of the
  constituent lists.

  Interactive tables may also be used for
  observing changes in lists during solution
  after pressing F9 or F10, and/or final results
  without bringing up other sheets or subsheets.

NOTE:
 If you use the Print command while in an
  interactive table, the printing is limited
  to the width of 80 characters displayed on
  the screen, regardless of the setting of the
  Printed Page Width field in the Global Sheet.
  To print the width specified by that setting,
  follow the steps described above in "To Send
  a Table to an Output Device".



TABLES AND THE PRESENTATION VIEW:

A table can be either copied to a Table Box or
linked to a Field Box in the PV.   A Table Box
does not allow entries or editing but a Field
Box does.  The related topics Table Box and
Field Box give complete information on getting
a table into the PV.
017
The NUMERIC FORMAT SHEET
  contains a summary of all numeric formats in
  the current model.
  Numeric Format Subsheets contain detailed
  specification for each particular format.

ACCESS:
 Type =N to display it in the current window.
 Type /WN to display it in the second window.
 Use the Dive command (>) to dive into a
  Numeric Format Subsheet highlighted by the
  cursor, and the Return command (<) to return
  to the Numeric Format Sheet.

NOTE: Help screens for all format attributes are
      "chained".  Keep pressing ÄÙ to browse
      through one after another.
010
The COMMENT SHEET
  contains a summary of documentary comments in
  the current model.  Comment Subsheets contain
  the actual text for each particular comment.

ACCESS:
 Type =C to display it in the current window.
 Type /WC to display it in the second window.

NOTES:
010
  -Dive (>) into a Comment Subsheet from a line
   on the Comment Sheet or from a reference to a
   comment, which can occur in a Text field, in
   a Comment field, or in a comment following a
   semicolon in a rule or statement.
  -Return (<) to the original location.
  -Syntax for a reference to a comment is
     * comment_name   or   *comment_name
   where comment_name appears in a Name field
   on the Comment Sheet.
013
COMMENT SHEET FIELDS

 Name:    Unique TK name or blank.
 Lines:   Number of lines in Comment Subsheet.
 Summary: Description summarizing comment.

COMMENT SUBSHEET FIELDS

 Summary: Same as above.
 Text:    A string of up to 200 characters.
  -Only 79 characters per line are displayed.
  -Like entries in Comment fields, Text field
   entries do not have to begin with a semicolon.
017
The GLOBAL SHEET
  contains global settings.  Each row contains
  one setting labeled on the left.

There are four groups of settings:
 Solver control.
 Display control.
 Printer and plot control.
 Configuration settings.

ACCESS:
 Type =G to display it in the current window.
 Type /WG to display it in the second window.

NOTE: The Help screens for all Global Sheet
      settings are "chained".  Keep pressing
      ÄÙ to browse through one after another.
024
SLASH COMMANDS (/): Type / to display the Option
  Box, then type the first letter of the desired
  option (or move the cursor and press ÄÙ).
  ÚÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿  ÚÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ Editing Commands ³  ³ I/O        ³
  ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´  ÃÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ B  Blank fields  ³  ³ S  Storage ³
  ³ C  Copy fields   ³  ³ P  Print   ³
  ³ D  Delete rows   ³  ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ
  ³ I  Insert rows   ³  ÚÄÄÄÄÄÄÄÄÄÄÄÄ¿
  ³ M  Move rows     ³  ³ Other      ³
  ÆÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍµ  ÃÄÄÄÄÄÄÄÄÄÄÄÄ´
  ³ Changing Display ³  ³ E  Examine ³
  ÃÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´  ³ R  Reset   ³
  ³ W  Window        ³  ³ Q  Quit    ³
  ÀÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ  ÀÄÄÄÄÄÄÄÄÄÄÄÄÙ
NOTES:
 Use the Help facility to find more information
  on specific commands, e.g. /S F1 gives you
  help on the Storage command.
 The Function Keys duplicate some of the slash
  commands with one keystroke.
 The Slash command options are different in
  the Presentation View.  See related topic.
008
 Not appearing in the Command Option Box are
  two special Slash commands that display
  information on the Prompt/Error line:
    /V  Displays the program version.
    /T  Displays the time used for completing
        the most recent calculation invoked by
        pressing F9 or F10, or by /EE or /EC
        commands (in min:sec.hundredths).
014
The BLANK command (/B)
  blanks out a block of fields in a single
  column.

USAGE:
 Move the cursor to the first item you want
  blanked and type /B.
 Move the cursor to highlight the block of
  fields you want blanked.
 Press ÄÙ.

NOTE:
  Use the Arrow keys  or , and Home, End,
  PgUp or PgDn keys to move the cursor.
019
The COPY command (/C) copies a block of fields
in a single column to another location.

USAGE:
 Move the cursor to the first field to copy.
 Type /C.
 Move the cursor to highlight the block of
  fields to be copied.
 Press ÄÙ.
 Move the cursor to the desired location to
  begin copying the defined block.  (Type  \
  if the destination is the Presentation View.)
 Press ÄÙ.

NOTES:
 Use the Arrow keys, and Home, End, PgUp or
  PgDn keys to move the cursor.
 Copying information into a field OVERWRITES
  the previous contents of the field.
014
The DELETE command (/D)
  deletes a block of rows you specify.

USAGE:
 Move the cursor to any location in the first
  row you want deleted.
 Type /D.
 Move the cursor to highlight the block of
  rows you want deleted.
 Press ÄÙ.

NOTE:
  Use the Arrow keys  or , and Home, End,
  PgUp or PgDn keys to move the cursor.
018
The INSERT command (/I)
  inserts blank row(s) in the current sheet.

USAGE:
 Move the cursor to the row where you want to
  start inserting.
 Type /I.
 Move the cursor to highlight the number of
  rows you want to be displaced by blank rows.
 Press ÄÙ.

NOTES:
 The row marked by the cursor and all the rows
  below it are displaced downward, but the
  cursor remains in the same place.
 Press the Ins key to insert a single row.
 /I works differently in the Presentation
  View.  See related topic.
027
The MOVE command (/M)
  moves a specified block of rows.

USAGE:
 Move the cursor to the first row to be moved.
 Type /M.
 Move the cursor to highlight the block of
  rows you want to move.
 Press ÄÙ.
 Move the cursor to the destination row where
  you want to place the highlighted block.
 Press ÄÙ.

NOTES:
 Use the Arrow keys  or , and Home, End,
  PgUp or PgDn keys to move the cursor.
 The Move command places the first row of the
  specified block in the destination row and the
  rest of the block in the following rows.
 Existing row(s) at and below the destination
  row are displaced to make room -- they move
  upward if the block is moved down, and
  downward if the block is moved up.
 Blank rows move the same way as if they were
  filled.
 /M in the Presentation View moves boxes rather
  than rows.  See related topic.
022
The STORAGE command (/S)
  loads, saves and deletes files from the disk.

ÚÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿ ÚÄÄÄÂÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ¿
³ L ³ Load file     ³ ³ T ³ Text storage      ³
ÃÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ ÃÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
³ S ³ Save model    ³ ³ I ³ Include files     ³
³ 1 ³ One sheet save³ ³ C ³ Configuration file³
ÃÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´ ÃÄÄÄÅÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ´
³ # ³ DIF storage   ³ ³ D ³ Delete file       ³
³ W ³ WKS storage   ³ ÀÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ
³ A ³ ASCII storage ³
ÀÄÄÄÁÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÙ

USAGE:
 Type /S and the first letter of the option.
 Respond to any additional prompts.  (Press  F2
  to cancel without taking any action.)

NOTES:
 F3 is equivalent to /SL.
 F4 is equivalent to /SS.
035
The PRINT command (/P)
  prints a block of rows from a TK sheet to
  a printer or writes it into a file.

USAGE:
 Move the cursor to the sheet and row where
  you want printing to begin.
 Type  / P .
 Move the cursor to highlight the rows you
  want to print.
 Press ÄÙ.  This will bring up a menu for
  you to specify a printing device or a file.

NOTES:
 Use the Arrow keys  or , and Home, End,
  PgUp or PgDn keys to move the cursor.
 The block is momentarily displayed on the
  screen while it is being printed.
 Ctrl-Break can be used to stop printing or
  saving.
 Blocks of rows saved under a specified file-
  name (to which TK adds the extension .PRF)
  may be exported to word-processing documents.
 If you append a  +  to the filename, each
  new block will be appended to the contents of
  a file saved already under that name.
 You can also dump the screen using the
  Shift-PrtSc key.
 When you apply /P command to an interactive
  table TK will output only columns visible on
  the screen; set "Printer" in Screen or Printer
  field on the Plot Subsheet and use F8 command
  when you want to output a wider table in full.
 Look up the topic "Plot Sheet" in the Help
  Index on how to print plots.
018
The PRINT command (/P) in the Presentation View
  has the options

  All   - Prints the entire PV, including areas
          not visible on screen, if any.
  Range - Prints only the range of lines you
          select by using Up- and Down-Arrow,
          PgUp, PgDn, Home, and End keys. (The
          block cue must be at either the top
          or the bottom of the range before
          you type /P.)
  Pages - Prints only the pages you select by
          responding to prompts for the first
          and last pages to be printed.

Except for accomodating these options, /P in
the Presentation View is similar to /P in the
Sheet View described in the related topic.
021
The RESET command (/R)
  has three options:
V (Variables) clears all input and output
  values from the Variable Sheet and Variable
  Subsheets (First Guess values are not
  affected).
S (Sheet) clears the current sheet or subsheet.
A (All) clears all sheets and subsheets.

USAGE:
 Type /R.
 Type V, S or A to select the option you want.
 Type Y to confirm or N to cancel the Reset.

NOTES:
 NOTHING will stop a confirmed reset.
 Instead of pressing V, S or A and Y or N, you
  may move the cursor in the Option Box to the
  appropriate position and press ÄÙ.
 Use the Quit command (/QY) rather than Reset
  if you want to leave TK.
021
The RESET command (/R) from the Presentation
  View has three options:
V clears all input and output values from the
  Variable Sheet and Variable Subsheets (First
  Guess values are not affected).
P clears Presentation View but does NOT affect
  contents of any TK object linked to the PV.
A clears all sheets and subsheets.

USAGE:
 Type /R.
 Type V, P or A to select the option you want.
 Type Y to confirm or N to cancel the Reset.

NOTES:
 NOTHING will stop a confirmed reset.
 Instead of typing V, P or A and Y or N, you
  may move the cursor in the Option Box to the
  appropriate position and press ÄÙ.
 Use the Quit command (/QY) rather than Reset
  if you want to leave TK.
016
The QUIT command (/Q)

USAGE:
 Type /Q.
 Type Y to confirm the Quit command.
 Type N or press F2 to cancel the Quit command.

NOTES:
 If you want to clear information from the
  sheets without leaving TK, use the Reset
  All command (/RA).
 When you quit TK, the information in the
  current sheets is LOST.  If you want to save
  what you have done before you leave TK,
  use the Save option of the Storage command (F4
  or /SS) before quitting.
028
The LOAD option (F3 or /SL)

USAGE:
 Press F3 or type /SL.
 Type a drive designator and pathname if
  needed.
 Type a filename, or access the Disk Index
  and move the cursor to the row with the
  filename.
 Press ÄÙ.

NOTES:
 If TK is not empty, a prompt asks whether to
  Merge the new model, Reset TK before loading,
  or Include the new model.
 Values from new model replace current values
  of duplicate variables.
 Duplicate rules and unit conversions are
  repeated.
 A model can be loaded right after TK is
  started from DOS by typing the model name as
  a command line argument.  For example:
              TK2 LOAN ÄÙ
  will start TK and load the model LOAN.
 If you load a TK model that contains rules
  or statements that cannot be understood by
  TK, they will be marked with the "unparsed"
  symbol  %  and ignored.
036
MERGE FILES and INCLUDE FILES
  are files that are loaded when TK is not
  freshly started or reset (i.e. after entries
  were made or a file was loaded).

All TK objects from MERGE FILES become a normal
part of the current model.  They are appended
to the current model, may be modified during
the session, and are saved with the model.

The only TK objects from INCLUDE FILES that are
appended to the current model are functions,
keystroke macros, unit conversions, lists,
plots, tables, numeric formats, and comments.
Nothing from the Variable Sheet, Rule Sheet,
or Global Sheet of an include file is appended
to the current model.

Objects from an include file are marked with an
asterisk * in the 80th column of the respective
sheet and the name of the include file enclosed
in parentheses in the subsheet heading.  Include
file objects cannot be modified during the
current session.

Objects from include files are not saved with
the current model.  Instead, the names of the
include files are listed at the end of the model
file.  The include files are automatically
included again when the model file is loaded
into TK.

Merge files become permanent parts of the
current model.  The status of include files
may be altered with the Include option of
the Storage command (see related topic /SI).
005
The SAVE MODEL option (F4 or /SS)
  saves the entire model.

USAGE:
 Press F4 or type /SS.
012
 Type a drive designator and pathname if
  needed.
 Type a filename or, if you want to overwrite a
  file, access the Disk Index.
 Press ÄÙ.

NOTES:
 TK adds the .TK extension to the filename
  if no other extension is specified.
 If you use the Disk Index but find no
  file to overwrite, you must terminate the
  command by pressing  F2  and start over.
003
 See the Help screen on the Storage command for
  information on saving parts of a model rather
  than the whole model.
015
LIST SAVING and LOADING

TK can save or load lists which are stored in
the following formats:

 DIF   (Data Interchange Format)
 WKS   (Lotus worksheet format)
 ASCII

Because many programs recognize one or more of
these formats, lists may be used to transfer
data between TK and other programs.

Details on working with each format are given
in the Related Topics.
021
SAVE 1 SHEET (/S1)
  presents options for saving all or parts of a
  sheet as a model file.  Subsheets belonging to
  the rows from the main sheet are also saved.

  saves all or parts of a sheet as a model file.
  Subsheets corresponding to the rows selected
  from the main sheet are also saved.

USAGE:
 Type /S1.
 Type the first letter of the option you want.

NOTES:
 All the above options save the selected part
  of the model as an independent TK model file.
 See Related Topics for information on saving
  all or part of a particular sheet.
 Lists cannot be saved with /S1.  A Related
  Topic describes other /S command options used
  to save lists in DIF, WKS, or ASCII formats.
009
The SAVE VARIABLES option (/S1V)
  saves the current Variable Sheet and Variable
  Subsheets in a TK file.

USAGE:
 Type /S1V.
 If you are in the Variable Sheet, select the
  range of variables.  (From anywhere else, the
  whole Variable Sheet is saved automatically.)
009
The SAVE FUNCTIONS option (/S1F)
  saves the current Function Sheet and Function
  Subsheets in a TK file.

USAGE:
 Type /S1F.
 If you are in the Function Sheet, select the
  range of functions.  (From anywhere else, the
  whole Function Sheet is saved automatically.)
010
The SAVE KEYSTROKE MACROS option (/S1K)
  saves the current Keystroke Macro Sheet and
  Keystroke Macro Subsheets in a TK file.

USAGE:
 Type /S1K.
 If you are in the Keystroke Macro Sheet,
  select the range of macros.  (From anywhere
  else, the whole Keystroke Macro Sheet is
  saved automatically.)
008
The SAVE UNITS option (/S1U)
  saves the current Unit Sheet in a TK file.

USAGE:
 Type /S1U.
 If you are in the Unit Sheet, select the
  range of units.  (From anywhere else, the
  whole Unit Sheet is saved automatically.)
012
The PLOT SAVE option (/S1P)

  saves the current Plot Sheet and Subsheets
  (and all List Subsheets with lists referred
  to in Plot Subsheets) in a file with the
  specified filename.

USAGE:
 Type /S1P.
 If you are in the Plot Sheet, select the range
  of plots.  (From anywhere else, the whole
  Plot Sheet is saved automatically.)
012
The TABLE SAVE option (/S1T)

  saves the current Table Sheet and Subsheets
  (and all List Subsheets with lists referred
  to in Table Subsheets) in a file with the
  specified filename.

USAGE:
 Type /S1T.
 If you are in the Table Sheet, select the
  range of tables.  (From anywhere else, the
  whole Table Sheet is saved automatically.)
010
The SAVE NUMERIC FORMATS option (/S1N)
  saves the current Numeric Format Sheet and
  Subsheets in a TK file.

USAGE:
 Type /S1N.
 If you are in the Numeric Format Sheet,
  select the range of numeric formats.  (From
  anywhere else, the whole Numeric Format Sheet
  is saved automatically.)
010
The COMMENT SAVE option (/S1C)
  saves the current Comment Sheet and Subsheets
  in a file with the specified filename.  The
  file is in the format of a TK model.

USAGE:
 Type /S1C.
 If you are in the Comment Sheet, select the
  range of comments.  (From anywhere else, the
  whole Comment Sheet is saved automatically.)
017
The DIF STORAGE option
  saves and loads files with .DIF extension.

DIF (Data Interchange Format) is a format in
which files with list values can be written and
read by many different programs, allowing easy
export and import of data.

There are two DIF Storage options:
 DIF Save (/S#S) writes one or more lists
  into a DIF file.
 DIF Load (/S#L) loads a DIF file into TK.

NOTES:
 The DIF Storage option applies only to the
  List Sheet and List Subsheets.
 TK adds the .DIF extension during a save.
017
The WKS STORAGE option
  saves and loads files with the .WKS extension.

WKS is a format in which files in Lotus 1-2-3
are stored.  Since TK can read and write files
in the WKS format, data is easily transferable
between Lotus 1-2-3 and TK.

There are two WKS Storage options:
 WKS Save (/SWS) writes one or more lists into
  a WKS file.
 WKS Load (/SWL) loads a WKS file into TK.

NOTES:
 The WKS Storage option applies only to the
  List Sheet and List Subsheets.
 TK adds extension .WKS during a save.
017
The ASCII STORAGE option
  saves and loads files in ASCII format.

There are two ASCII Storage options:
 ASCII Save (/SAS) writes one or more lists
  into an ASCII file.
 ASCII Load (/SAL) loads an ASCII file into
  TK.

NOTES:
 The ASCII Storage option applies only to the
  List Sheet and List Subsheets.
 TK adds the extension .ASC to the filename
  during an ASCII save, but will load an ASCII
  file with any extension if it has the proper
  structure of list names, list elements and
  delimiters.
021
The TEXT STORAGE option
  saves and loads files comprised of text in the
  form of Comment Subsheets.

There are two Text Storage options:
 Text Save (/STS) writes the contents of one or
  more Comment Subsheets into a text file.
 Text Load (/STL) loads a text file into one
  unnamed Comment Subsheet.

NOTES:
 The Text Storage option applies only to
  comments.
 If you are in the Comment Sheet when you start
  a Text save, select the range of comments.
  (From anywhere else, all comments will
  automatically be saved in one .TXT file.)
 TK adds the .TXT extension to the filename
  if no other extension is specified.
 You can create or edit .TXT files with a word
  processor in text mode.
023
The INCLUDE FILES option of the Storage command
  modifies the status of objects from files that
  have been included with the model.

There are two options to the /SI command:
 Merge (/SIM) converts include files to merged
  files, i.e. removes the asterisks that mark
  the included objects and makes those objects
  normal components of the current model.
 Exclude (/SIE) deletes all the objects from
  include files and removes the names of the
  files from the internal list of include files.

NOTES:
 /SIM and /SIE both prompt for the name of an
  include file to modify.  Press Enter to bring
  up a list of include files to select from; the
  list appears similar to the Disk Index and you
  can use it in the same fashion.  Alternatively,
  you can enter a filename at the prompt.
 Once you have modified an include file with
  /SIM or /SIE, you cannot change it back to an
  include file with /SI.
086
CONFIGURATION OF TK

A text file named TK2.CFG contains settings that
control the configuration of TK.  To change the
settings, type /SC to bring up the Configuration
Editor.  You can move the cursor around and make
entries in much the same way that you can on a
true TK sheet.

You can press F4 to save the changes to TK2.CFG,
or press Enter to leave the Configuration Editor
with the changes in effect only until you quit
TK or bring up the Configuration Editor again.
If temporary changes are already in effect, you
can press F3 to restore the defaults in TK2.CFG.

When TK is first run, it will use the following
default values if it cannot find TK2.CFG in the
current directory, on the DOS path, or (with DOS
3.0 or higher) the directory containing TK2.EXE.

============= CONFIGURATION EDITOR =============
Use Color:                   Yes
Slow Redisplay:              Yes
Solid Line Headings:         Yes
Bottom Prompt Line:          Yes
Check "OK" before Printing:  No
Display Options:        VGA,EGA,CGA,LOWCGA,TEXT
Model Search Path:
Printer Port- Device Driver- Name in Print Menu
LPT1          EPSIBM

NOTES on Individual Fields and Columns:

Use Color:  Set to No if you have a color card
  but a monochrome monitor.
Slow Redisplay:  Set to No if your color card
  can handle fast redisplay without "snow".
Solid Line Headings:  Set to No if your printer
  cannot print graphics characters.
Bottom Prompt Line:  Set to No if you are
  familiar with the TK command structure and
  do not need the bottom prompt line.  This
  also turns off the option prompt for the
  Status field of the Variable Sheet.
Check "OK" before Printing:  Set to Yes if you
  want a prompt for confirmation before printing
  when the Solution Indicator says F9, i.e. when
  the model has not been solved or changes have
  been made since the last solution.
Display Options:  TK supports 7 display devices:
  VGA, EGA, CGA, LOWCGA, HERCULES, ATT, and TEXT.
  You can specify up to five display devices in
  any order, separated by commas.
Model Search Path:  Use this field to specify
  search paths for TK to place in front of a
  TK model or menu filename in case the file
  cannot be found in the current subdirectory.
  The Model Search Path is not necessarily
  related to the DOS Path, but the syntax is
  similar, e.g. \TK\;\tk\UTS\;\TK\AERO\
Printer Port:  You can make up to 8 entries in
  the column of fields under this heading.
  - Type L to select a parallel port.  LPT1 is
    the default, but you can type 1, 2, or 3 at
    any time to change an LPT port number.
  - Type C to select a serial port.  COM1 is the
    default, but you can type 1, 2, 3, or 4 at
    any time to change a COM port number.
  - Type F to select File.
Device Driver:  You can make up to 8 entries
  in the column of fields under this heading.
  The information area at the bottom of the
  configuration editor shows the available
  options defined in the TKDRIVER.CFG file.
  (DRAFT is generally used only on dot-matrix
  printers.  It uses characters generated by
  the printer ROM, whereas EPSIBM draws each
  character without referring to printer ROM.)
Name in Printer Menu:  This column of fields
  allows up to 8 entries that are displayed in a
  menu brought up by any printing action (except
  for pressing 'P' while displaying a plot.)  If
  one of these fields is left blank, the Printer
  Port and Device Driver entries from the
  corresponding row are shown.
017
The DELETE option (/SD)
  deletes files from disk.

USAGE:
 Type /SD.
 Type a drive designator and pathname if
  needed.
 Type the filename, or access the Disk Index
  to mark the file(s) to be deleted.
 Press ÄÙ.
 Confirm the deletion(s) by typing Y.

NOTES:
 The Delete option can delete any kind of file,
  not just TK files.
 See the Help screen on the Disk Index for
  information on multiple deletions.
017
The LIST command (/L)
  performs actions on lists.  When you type /L,
  TK displays four List command options.  Type
  the first letter or character of the option
  you want.
ÉÍÍÍÍÍÍÍÍÑÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ»
º  List  ³ solves the model for lists of      º
º        ³   input values (same as F10)       º
º  Block ³ solves the model for a specified   º
º        ³   block of input values            º
º  Put   ³ stores variable values at a        º
º        ³   specified position in all        º
º        ³   associated lists                 º
º  Get   ³ retrieves values from a specified  º
º        ³   position in associated lists and º
º        ³   assigns the values to variables  º
ÈÍÍÍÍÍÍÍÍÏÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍÍ¼
015
The Block Solve option of the List command
(/LB), the Table Subsheet, and the Line Chart
Subsheet each allow you to specify a range of
list elements to be used.

The first element specified must be between 1
and 32000.  The last element must be between the
specified first element and 32000; if left
blank, the length of the longest relevant list
is used.

On either of the subsheets, it may be necessary
to change these fields in the correct order so
that the last element number is never less than
the first.
035
The LIST SOLVE option  (F10 or /LL)
  solves a model for list(s) of input values.

To solve for lists of input values:
 Type "L" in the status column of each
  variable you want associated with a list.
 Type "LG" in the status column of each
  variable, if any, whose associated list is to
  provide guess values.  (If the associated list
  is empty, fill it or enter a First Guess value
  on the variable's subsheet.)
 Assign any input values on the Variable Sheet
  to all variables associated with lists of
  input values.
 Press the F10 key.



NOTES:
 The list solving proceeds until it exhausts
  the values in the longest input list.
 List solving replaces or blanks any previous
  values in output lists.  (Lists associated
  with variables not evaluated in the process of
  list solving will be partially or totally
  blanked out.  Protect such lists by
  disassociating them from the variables on
  the Variable Sheet before list solving!)
 The Display Intermediate Values field on the
  Global Sheet controls screen display of
  intermediate values during list solving.
  Setting the field to "No" will speed up the
  list solving process.


029
The BLOCK SOLVE option (/LB)
  solves for a subset, or block, of input
  values that may be defined in list(s).

To solve for a block of input list values:
 Type /LB.
 Specify the element number where the block is
  to begin (default: 1).
 Press ÄÙ.
 Specify the last element number for the block
  (default: last element in longest of the lists
  associated with input variables).
 Press ÄÙ.


NOTE:
  There is a subtle difference between list
  solving and block solving.  The latter
  does not require any input variable to
  be associated with a list.

EXAMPLE:
  A model with no list associated with input
  values and with a rule
     P = elt()
  will fail an attempt to list solve.  Block
  solving will result in assigning instance
  numbers to respective elements of a list
  associated with variable  P .
019
The PUT option (/LP)
  copies current input and output values of
  variables to their associated lists at
  the specified element number.

USAGE:
 Type /LP.
 Specify the element number where you want to
  put the values.

EXAMPLE:
  Variables A, B and C are associated with lists
  with the same names.  They have values 13, -8
  and 10, respectively, in their Input fields on
  the Variable Sheet.  If you type /LP5ÄÙ
  (from anywhere in TK) the Put command will
   - place 13 in list A at element number 5
   - place -8 in list B at element number 5
   - place 10 in list C at element number 5
019
The GET option (/LG)
  copies values from lists which are associated
  with variables to the Variable Sheet.  If a
  variable had been assigned an input value, the
  copied list value replaces it; else, the list
  values are copied to output fields.

USAGE:
 Type /LG.
 Specify the element number from which to get
  the values from each list, then press ÄÙ.
EXAMPLE:
  Lists a, b and c have 23, -3 and 17 in their
  7th element positions.  If you type /LG7ÄÙ
  from anywhere within TK, the values 23, -3 and
  17 will be displayed on the Variable Sheet as
  values of variables associated with lists a, b
  and c, regardless of what values may have been
  previously displayed.
040
The EXAMINE command (/E) has three options:
Variable, Expression, and Copy.

The VARIABLE option
  allows you to EXAMINE the value of a variable
  when debugging the model or analyzing error
  conditions.

USAGE:
 Type /EV.
 Type the variable name and press ÄÙ.

The EXPRESSION option
  allows you to EXAMINE the value of an
  expression when debugging the model or
  analyzing error conditions.

USAGE:
 Type /EE.
 Type in the expression and press ÄÙ.
The COPY option
  allows you to copy the field highlighted by
  the cursor onto the Prompt/Error line.
  Depending on contents, this text may be
  evaluated as an expression immediately
  or after editing.

USAGE:
 Type /EC.
 Edit the copied field contents, if necessary
  (initiate editing with F5 or any Arrow key).
 Press  ÄÙ  for evaluation.

NOTES:
 The Expression and Copy options can be used
  as a calculator: just type /EE or /EC and the
  expression you want to evaluate.
 The Expression and Copy options can be used
  to call or refer to a function, e.g.
  /EE call listcopy('a,'b) ÄÙ.
019
The WKS SAVE option (/SWS)
  saves one or more lists in a WKS file.

To save a WKS file:
 Move the cursor to the List Sheet.
 Type /SWS.
 If you are in the List Sheet, select the range
  of lists.  (From anywhere else, the whole
  List Sheet is saved automatically.)
 Type a drive designator and pathname if
  needed.
 Type a filename, or access the Disk Index
  if you want to overwrite a file.
 Press ÄÙ.

NOTES:
 TK adds the .WKS extension to the filename.
 If you use the Disk Index but find no
  file to overwrite, you must start over (F2).
018
The WKS LOAD option (/SWL)
  loads a WKS file into TK.  The filename
  you specify must have the .WKS extension.

To load a WKS file from a disk:
 Type /SWL.
 Type a drive designator and pathname if
  needed.
 Type the filename, or access the Disk
  Index and move the cursor to the row of
  the filename.
 Press ÄÙ.

NOTE:
  If you load a WKS file with list names which
  duplicate list names in the current model, TK
  adds the new lists to the List Sheet and List
  Subsheets, and prefixes their names with % .
028
The ASCII SAVE option (/SAS)
  saves one or more lists in an ASCII file.

USAGE:
 Move the cursor to the List Sheet.
 Type /SAS.
 If you are in the List Sheet, select the range
  of lists.  (From anywhere else, the whole
  List Sheet is saved automatically.)
 Type a drive designator and pathname if
  needed.
 Type a filename, or access the Disk Index
  if you want to overwrite a file.
 Press ÄÙ.

NOTES:
 TK adds the .ASC extension to the filename.

 If you use the Disk Index but find no
  file to overwrite, you must start over (F2).
 TK saves the full internal representation of
  the numeric values of list elements with up to
  16 significant digits.  If a different format
  is desired, it must be defined in a Numeric
  Format Subsheet, and declared as the Global
  Numeric Format in the Global Sheet.  TK then
  will offer you the option of saving the list
  values in the global format.
018
The ASCII LOAD option (/SAL)
  loads an ASCII file into TK.

To load an ASCII file from a disk:
 Type /SAL.
 Type a drive designator and pathname if
  needed.
 Type the filename, or access the Disk
  Index and move the cursor to the row of
  the filename.
 Press ÄÙ.

NOTE:
  If you load an ASCII file with list names
  which duplicate list names in the current
  model, TK adds the new lists to the List
  Sheet and List Subsheets and prefixes their
  names with  % .
026
The DIF SAVE option (/S#S)
  saves one or more lists in a DIF file.

USAGE:
 Move the cursor to the List Sheet.
 Type /S#S.
 If you are in the List Sheet, select the
  range of lists.  (From anywhere else, the
  whole List Sheet is saved automatically.)
 Type a drive designator and pathname if
  needed.
 Type a filename, or access the Disk Index
  if you want to overwrite a file.
 Press ÄÙ.

NOTES:
 TK adds the .DIF extension to the filename.

 If you use the Disk Index but find no
  file to overwrite, you must start over (F2).
 DIF files store information about units.  If
  you save in DIF format any list possessing
  Calculation and Display units, you cannot
  load the DIF file back into a different TK
  session unless the conversion between those
  units is defined in the Unit Sheet.
022
The DIF LOAD option (/S#L)
  loads a DIF file into TK.  The filename
  you specify must have the .DIF extension.

To load a DIF file from a disk:
 Type /S#L.
 Type a drive designator and pathname if
  needed.
 Type the filename, or access the Disk
  Index and move the cursor to the row of
  the filename.
 Press ÄÙ.

NOTES:
 If a DIF file has list names which duplicate
  list names in the current model, TK adds the
  new lists to the List Sheet and List Subsheets
  and prefixes their names with  % .
 If a DIF file defines the Calculation and
  Display units for any list, the file cannot
  be loaded unless the conversion between those
  units is already defined on the Unit Sheet.
018
The ACTION command (!)
  on List Subsheets, List Function Subsheets and
  Vertical Interactive Tables fills lists (i.e.,
  generates a series of values).

OPTIONS:
 Add Step.
 Linear Spacing.
 Multiply by Step.
 Geometric Spacing.

NOTES:
 The List Fill command may be used to fill all
  or part of a list, or to overwrite old list
  values with new ones.
 On the List Function Subsheet and Vertical
  Interactive Table, a named list must already
  be present to be filled.
018
The ADD STEP option (!A)
  fills a list by adding an increment (STEP
  SIZE) to the first value to obtain the next
  value, and so on up to the LAST VALUE.

USAGE:
 Place the cursor on the element where you want
  the list fill to begin.
 Type !A.
 Enter the first value.
 Enter the step size (cannot be equal to 0).
 Enter the last value (must be greater than
  first value if step size is positive, less
  than first value if step size is negative).

NOTE:
  The last value generated may be equal to but
  will not surpass the last value limit.
013
The LINEAR SPACING option (!L)
  fills a list so that the difference between
  any two consecutive values is constant.

USAGE:
 Place the cursor on the first element you
  want filled.
 Type !L.
 Enter a first value.
 Move cursor to the last element you want
  filled, using Arrow keys (or other naviga-
  tion keys) or GOTO (:) command.
 Enter the last value.
019
The MULTIPLY BY STEP option (!M)
  fills a list by multiplying each value by a
  specified amount (STEP SIZE) to obtain the
  next value, from the first value specified up
  to a specified limit (LAST VALUE).

USAGE:
 Place the cursor on the first element you
  want filled.
 Type !M.
 Enter the first value.
 Enter the step size (must be > 0 and <> 1).
 Enter the last value (must be same sign as
  first value, and must be greater than first
  value if step size > 1, or less than first
  value if step size < 1).
NOTE:
  The last value generated may be equal to but
  will not surpass the last value limit.
014
The GEOMETRIC SPACING option (!G)
  fills a list so that the ratio between any
  two consecutive values is constant.

USAGE:
 Place the cursor on the first element you
  want filled.
 Type !G.
 Enter an initial value.
 Move cursor to last element you want filled,
  using Arrow keys (or other navigation keys)
  or GOTO (:) command.
 Enter a last value (must have same sign as
  the first value).
028
SLASH COMMANDS (/) in the Presentation View are
somewhat different from those in the Sheet View.

Commands unique to the Presentation View:
  /G  Grow box
  /U  Untangle boxes
  /F  Freeze box

Commands unique to the Presentation View but
invoked by the same keystrokes as commands
in the Sheet View:
  /M  Move box (instead of Move rows).
  /D  Delete box (instead of Delete rows).
  /I  Insert rows (between boxes, instead of
      between rows on a sheet).

Commands that work essentially the same way:
  /B  Blank fields
  /L  List
  /S  Storage
  /P  Print
  /E  Examine
  /R  Reset
  /Q  Quit

NOTE:
 Refer to the related topics for further
  information on each command.
011
The MOVE BOX command (/M in Presentation View)
  moves one or more selected boxes.

USAGE:
 Type /M.
 Select one or more boxes by visiting each one
  with the block cue.
 Press ÄÙ to finalize the selection.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the selected boxes.
 Press ÄÙ to finish the Move boxes command.
009
The GROW BOX command (/G) resizes a single box
  in the Presentation View.

USAGE:
 Move the block cue into the box to be resized.
 Type /G.  The box changes to a green outline.
 Use the Arrow, PgUp, PgDn, Tab, and Shift-Tab
  keys to move the lower right corner.
 Press ÄÙ to finish the Grow box command.
009
The DELETE BOX command (/D in Presentation View)
  deletes one or more selected boxes.

USAGE:
 Type /D.
 Select one or more boxes by visiting each one
  with the block cue.
 Press ÄÙ to finalize the selection.
 Press ÄÙ to delete the boxes.
010
The UNTANGLE BOXES command (/U) removes overlaps
  between boxes in the Presentation View.

USAGE:
 Type /U.

NOTE:
 TK starts with the box at the lower right and
  moves a box only downward, not right or left,
  to untangle it from a higher box it overlaps.
015
The FREEZE BOX command (/F) permanently changes
  one or more selected boxes to Text boxes.

 Type /F.
 Select one or more boxes by visiting each one
  with the block cue.
 Press ÄÙ to finalize the selection.
 Press ÄÙ to freeze the boxes as Text boxes.

NOTES:
 The Freeze Box command works only for Field
  Boxes and Table Boxes.  You cannot freeze
  Plot Boxes.
 You can use the Freeze Box command to keep
  intermediate results in a solution process.
015
The INSERT ROWS command (/I) in Presentation
  View inserts blank horizontal space between
  boxes, moving down one line all boxes below
  the current box or the block cue.

USAGE:
 Type /I.

NOTE:
 /I in the Presentation View affects only the
  positions of boxes, not rows within boxes.
  To insert rows within a Field Box or Table
  Box, you must use /I on the corresponding
  linked object in the Sheet View.  Use the
  Enter key to insert rows in Text Boxes.
011
The GLOBAL NUMERIC FORMAT options
  provide control over the format of numeric
  values saved in ASCII files.

USAGE:
 Press N or ÄÙ if you wish to save the values
  in full internal representation with up to 16
  significant digits.
 Press Y if you wish to save the values in the
  format declared as a Global Numeric Format on
  the Global Sheet.
035
STATUS field
  shows the status of the variable and may be
  used for changing its status.

  G  Indicates a guess status.
  L  Indicates that the variable is associated
     with a list of values.
  >  Indicates the variable is involved in an
     error condition (for more information, go
     to the Rule Sheet, place the cursor over
     the > in the Status field of the errant
     rule, and press F1).
  ?  Indicates missing unit conversion between
     Calculation units of the variable and its
     associated list (appears after pressing F10
     if Stop on List Error field in Global Sheet
     has been set to Yes)
  The default is blank.
SELECTING OPTIONS:
  With the cursor in the Status field, type the
  first letter of the option you want to apply:

  I - Current value will be assigned as input
      (equivalent to entering that value in the
      Input field).
  O - Current value will appear in the Output
      field.
  B - Blanks the Input or Output field, removes
      guess status.
  G - Current value will be assigned as a guess
      (this is a toggle switch).
  L - The variable will be associated with a
      list with the same name or with the name
      specified in the Variable Subsheet
      (this is a toggle switch).
034
The STATUS field (Rule Sheet and Rule Function
  Subsheet) shows the status of the rule.

STATUS field in the Rule Sheet:
Blank Indicates a successful solution of
      the rule.
  *   Indicates an unsolved rule.

STATUS field in both Rule Sheet and Rule
Function Subsheet:
  C   Indicates that the rule is switched off.
  >   Indicates an error during the evaluation
      of the rule.  Move the cursor to the
      Status field to display the error
      message.
  %   Indicates an unparsed rule.  The rule
      probably refers to an unavailable user-
      defined function or function with
      incompatible number of argument and
      result variables.

STATUS field in the Rule Function Subsheet:
Blank Indicates that the rule was satisfied or
      left unsolved (active) during the last
      call of the function.  Cover the field
      with the cursor and find out on the
      Status line.

USAGE:
 Place the cursor in the field to display an
  explanatory message on the Status line.  If
  there is an error symbol  >  press F1 for
  a Help screen explaining the error.
 Press C to switch the rule off or on.
024
The STATUS field (Procedure Function and
  Keystroke Macro Subsheets) displays the
  status of the statement in that line.

STATUS:
Blank Indicates active statement.
  C   Indicates that the statement is switched
      off.
  >   Error in the specified statement line.
      Move the cursor to the Status field to
      display the error message.  Press F1.
  %   Indicates an unparsed statement.  The
      statement probably refers to an unavai-
      lable user-defined function or function
      with incompatible number of argument and
      result variables.


USAGE:
 Place the cursor in the field to display an
  explanatory message on the Status line.  If
  there is an error symbol  >  press F1 for
  a Help screen explaining the error.
 Press C to switch the rule off or on.
015
The INPUT field
  accepts and displays the input value of
  a variable.

VALID ENTRIES:
 A numeric or a symbolic value.
 A syntactically correct expression that
  evaluates to a numeric or a symbolic value.

NOTE:
  If a variable is assigned units, its value
  appears in the Display unit.  (If the con-
  version between Display unit and Calculation
  unit is not defined in the Unit Sheet, a  ?
  precedes the displayed value.)
012
The NAME field
  contains the name of an object (variable,
  function, keystroke macro, list, plot,
  table, numeric format, or comment).

VALID INPUTS:
 TK name

NOTE:
 No duplicate names are allowed within a Name
  field column on any sheet; however, different
  objects may have the same name.
015
The OUTPUT field
  contains the output value of the variable if
  the variable has one.

USAGE:
 TK uses the Output field for displaying
  solution results.
 An input value or a blanked (but internally
  held) value may be moved to the Output field
  by typing letter O in the Status field.
 If a variable is assigned units, its value
  appears in the Display unit.  (If the con-
  version between Display unit and Calculation
  unit is not defined in the Unit Sheet, a  ?
  precedes the displayed value.)
022
The UNIT field  (Variable Sheet or List Sheet)
  defines the Display unit used to display the
  variable value or the list element values.

USAGE:
 Entering a unit name in a blank Unit field
  also defines the Calculation unit.
 Entering subsequent unit names will change
  only the Display unit.
 The Calculation unit may be changed in the
  Variable Subsheet or List Subsheet.
 When you change the Display unit, the
  variable value or list element values change
  instantaneously, provided that the conversion
  between the Display and Calculation units is
  defined directly or indirectly in the Unit
  Sheet. If a conversion is not defined, a  ?
  will appear in front of the value.
 Blanking out the Unit field in the Variable
  Sheet or List Sheet will blank out both the
  Display and Calculation Unit fields in the
  associated Variable Subsheet or List Subsheet.
016
The DISPLAY UNIT  (Variable Subsheet or List
Subsheet)
  may be assigned to a variable or to list
  elements by:

 Entering its name into the Unit field in the
  Variable Sheet or List Sheet.  (If the field
  is blank, then the Display unit becomes the
  Calculation unit as well.)
 Entering its name into the Display Unit field
  of the associated Variable Subsheet or List
  Subsheet.

See "Unit Conversion" for the role of Display
units in TK.

016
The CALCULATION UNIT  (Variable Subsheet or
List Subsheet)
  may be assigned to a variable or to list
  elements by:

 Entering its name in the Calculation Unit
  field of the associated Variable Subsheet
  or List Subsheet.
 Entering its name into a blank Unit field in
  the Variable Sheet or List Sheet.  (In that
  case it becomes the Display unit as well.)

See "Unit Conversion" for the role of
Calculation units in TK.


010

NOTES on Units, Variables and Lists:
 List solving fails if the Unit Sheet does not
  define a conversion between the Calculation
  units of a variable and its associated list.
 DIF files can store information on Calculation
  and Display units.  TK will not save or load a
  DIF file with unit information unless the Unit
  Sheet defines a conversion between each list's
  Calculation and Display units.
018
COMMENTS

Comment fields appear on the Variable, Function,
Keystroke Macro, Unit, List, and Numeric Format
Sheets (and related Subsheets).  Any typeable
characters may be entered in these fields to
document the model.

Comments preceded by a semicolon may also be
appended at the ends of rules in the Rule Sheet
and Rule Function Subsheets and in statements
in Procedure Function and Keystroke Macro
Subsheets.  Examples:
  A=pi()*r^2  ; area of circle
  {=V:1n~}  ; go to first Name field in V.S.

Very extensive comments can be handled by the
Comment Sheet (see the related topic).
013
The FIRST GUESS field contains a blank (the
default) or the value you specify to serve for
automatic assignment as a guess and initiation
of the Iterative Solver.

It works only when the variable is not assigned
an input value and cannot be evaluated by the
Direct Solver, and also no variable anywhere on
the Variable Sheet has a G in its Status field.

VALID INPUTS:
 A numeric value.
 An expression evaluating into a numeric value.
006

NOTE:
 If any variable has a G in its Status field,
  all First Guesses are suppressed and iteration
  occurs only on those variables having a G in
  their Status fields.
019
The ASSOCIATED LIST field
  contains the name of the list currently
  associated with the variable.

USAGE:
 Typing L in the Status field of the Variable
  Sheet or Subsheet toggles an association
  between the variable and a list on and off.
 If this field is blank when L is toggled on,
  the variable's name will be entered here.
 If the name of a nonexistent list is entered
  here, TK creates a new list with the same
  Display and Calculation units as the variable.
 If the list already exists, its Display and
  Calculation units will stay the same even if
  they are different from the variable's.
 To cancel a list association, blank this field
  (/B) or type L in the Status field.  This does
  not delete the list.
008
The NUMERIC FORMAT field (Variable Sheet)
  contains the name of the numeric format that
  controls the appearance of an input, output or
  guess value displayed on the Variable Sheet or
  the Variable Subsheet.  A numeric format
  specified here will take precedence over a
  different numeric format specified on the
  Global Sheet.
016
The NUMERIC FORMAT field (List Sheet)
  designates the name of the numeric format
  that controls the display of the list values.
  A numeric format specified here will take
  precedence over a different numeric format
  specified on the Global Sheet.

NOTE:
  Assigning a numeric format to a list affects
  the display of the list values
    1. on the List Subsheet itself,
    2. in List Function Subsheets (the domain
       and range lists), and
    3. in tables if there is no numeric format
       assigned to the list in the Table
       Subsheet.
008
The NUMERIC FORMAT field (Table Sheet)

  Each list can have its own Numeric Format
  specified in this field.  When a table is
  displayed, the Numeric Format specified here
  will have precedence over a Global Numeric
  Format or a Numeric Format specified on a List
  Subsheet.
011
The GLOBAL NUMERIC FORMAT field
  names the numeric format from the Numeric
  Format Sheet that controls the display of
  values.  A numeric format specified locally
  (i.e. on a Variable, List or Table Subsheet)
  will take precedence over a different numeric
  format specified here.

  (See a note in "ASCII Save" about the effect
  of Global Numeric Format on formatting lists
  saved in *.ASC files.)
010
The RULE field
  in Rule Sheet or Rule Function Subsheet
  accepts and displays a rule (equation)
  conforming to TK syntax conventions.

NOTE:
  A Rule field can also contain a comment.
  TK regards all text after a semicolon symbol
  (;) in a Rule field as a comment, not subject
  to evaluation by the solvers.
019
The NUMERIC NOTATION field
  controls the numeric display of a variable.

OPTIONS:
 General: (default setting)  Scientific
  notation is used if the decimal number will
  not fit in the field or table column, or if
  the value contains more than six zeros
  immediately on either side of the decimal
  point.  Otherwise, decimal notation is used.
 Scientific:  All values are displayed in
  scientific notation.
 Decimal:  All values are displayed in decimal
  notation.  If a number is longer than the
  width of the field or table column, a format
  error indicator (>>>>>) fills the field.
 Engineering:  All values are displayed in
  scientific notation with exponents that are
  multiples of 3.
009
The SIGNIFICANT DIGITS field
  specifies the number of significant digits
  showing in numeric value displays.

USAGE:
  The default for this field shows a blank,
  meaning that as many digits as will fit in
  the field or table column will display.  You
  may specify an integer from 1 to 20.
014
The DECIMAL PLACES field
  specifies the maximum number of digits to
  appear after the decimal point.

USAGE:
 The default for this field shows a blank,
  meaning that as many digits following the
  decimal point as will fit in the field or
  table column will be displayed.  You may
  specify any integer from 0 to 200.
 To align decimal points in displays, specify
  the number of decimal places in this field,
  set the Numeric Notation field to "Decimal",
  and the Justification field to "Right".
014
The PADDING field
  controls the type and number of characters
  used to fill display values.

OPTIONS:
 Zero: all numbers show a trailing decimal
  point and zeros filling, or padding, the
  number of decimal places specified in the
  Decimal Places field.
 Blank: all numbers show a trailing decimal
  point and blanks padding the number of places
  specified in the Decimal Places field.
 None: uses no padding, and actual numeric
  values display (this is the default padding).
007
The DISPLAY LEADING ZERO field controls whether
  a leading zero is displayed with a fractional
  number, e.g. 0.28 rather than .28.

USAGE:
 Type N for no.
 Type Y for yes.
011
The DECIMAL POINT SYMBOL field
  specifies the character used to represent the
  decimal point in the numeric format.

USAGE:
 The default character is the period, but you
  can change it to any special character.
 You cannot blank this field.

NOTE:
  Digits and letters are not allowed.
018
The DIGIT GROUPING SYMBOL field
  contains the Digit Grouping Symbol that
  separates numeric values into groups of three
  to the left of the decimal point.

USAGE:
 The default is a blanked field (as opposed to
  a blank character in the field) which
  disables digit grouping, and numeric values
  appear as a single number.
 You can use a blank space or a special
  character as the digit grouping symbol, but
  you cannot use a letter or a digit.
 Use the Blank command (/B) to restore the
  default setting.
 Do not type digit grouping symbols when
  entering numbers.  They will be inserted
  automatically.
008
The ZERO REPRESENTATION field
  specifies the text or symbol used to
  represent a zero value in a display.

USAGE:
 The default for this field is the zero
  character itself.
 The zero representation can include blanks.
010
The +/- NOTATION field
  controls the display of plus and minus signs
  in a display.

OPTIONS:
-  displays the minus sign before negative
   values and no sign before positive values.
+  displays all values with plus and minus
   signs as appropriate.
(  displays negative values in parentheses.
013
The PREFIX field
  specifies one or more characters, such as a
  dollar sign, that appear before a numeric
  value in a display.

USAGE:
 The default for this field is no prefix.
 Do not type the prefix manually.  The numeric
  format inserts it automatically.
 In the display, the prefix appears after any
  sign set by the +/- Notation field.
 If you set a left margin, signs and prefixes
  appear in the margin.
014
The SUFFIX field
  specifies one or more characters, such as a
  percent sign, that appear after a numeric
  value in a display.

USAGE:
 The default for this field is no suffix.
 Do not type the suffix manually.  The numeric
  format inserts it automatically.
 In the display, the suffix appears before a
  closing parenthesis set by the +/- Notation
  field.
 If you set a right margin, closing
  parentheses and suffixes appear in the margin.
014
The JUSTIFICATION field
  controls whether a value display aligns on
  the left or right side or in the center of
  a field (not counting the margins).

OPTIONS:
 Left    Left justification, meaning that the
          first character appears on the left
          edge of the field.
 Right   Right justification.
 Center  Center justification.

NOTE:
 Justification will not infringe upon margins.
015
The LEFT MARGIN WIDTH field
  controls the number of blank spaces that
  follow a value and display on the left edge
  of the field.

USAGE:
 Default value for this field is 0.
 You can enter any integer from 0 to 200.
 If you set a left margin and left
  justification, the value is left justified
  against the inside (right) edge of the margin
  and any leading signs or prefixes appear
  within the margin.
 A leading sign or prefix wider than the
  margin will run over into the field.
016
The RIGHT MARGIN WIDTH field
  controls the number of blank spaces that
  follow a value and display on the right edge
  of the field.

USAGE:
 The default value for this field is 0.
 You can enter any integer from 0 to 200.
 If you set a right margin and right
  justification, the value appears right
  justified against the inside (left) edge of
  the margin and any closing parentheses or
  suffixes appear within the margin.
 If the closing parenthesis or suffix is wider
  than the margin, the value is right-justified
  against the suffix.
015
The SYMBOLIC VALUE FORMAT field
  controls the display of symbolic values.

OPTIONS:
 Single quotes preferred (default)
  Legal TK names are displayed with a single
  quote or apostrophe (') prefix.  Symbolic
  values which are not TK names are displayed
  surrounded by double quotes.
 Double quotes preferred
  All symbolic values are displayed surrounded
  by double quotes (")
 No quotes
  All symbolic values are displayed without
  prefix single or surrounding double quotes.
009
The DISPLAY INTERMEDIATE VALUES field
  turns the display of intermediate values
  during iterative and list solving on and off.

USAGE:
 Type N for no.
 Type Y for yes.
 Solving is faster with intermediate display
  off.
014
STOP ON LIST ERROR field

DESCRIPTION:
  Controls whether or not list solving stops
  upon encountering an error.

USAGE:
 Type N for no.
 Type Y for yes.

NOTE:
  Diagnostic error messages for errors occurring
  during list solving will be lost unless the
  setting is changed from the default No to Yes.
012
The USE AUTOMATIC ITERATION field

  If First Guess values have been assigned on
  one or more Variable Subsheets, the default
  setting of Yes will call the Iterative Solver
  automatically if any of those variables with
  First Guess value assigned cannot be evaluated
  by the Direct Solver.

USAGE:
 Type N for no.
 Type Y for yes.
026
The COMPARISON TOLERANCE
  The Comparison Tolerance (CT) and the Typical
  Value (TV) serve for checking the consistency
  of rules.  A rule is considered satisfied and
  an equation is presumed to hold if either

    RELERR ó CT   or   MABVAL ó CT * TV ,

  where
    relative error    RELERR = ABSERR / MABVAL
    absolute error    ABSERR = abs(LHE-RHE)
    maximum absolute value
               MABVAL = max(abs(LHE),abs(RHE))
    LHE,RHE  are values of left-hand side and
             right-hand side expressions.



  The CT and TV parameters play an important
  role in controlling the Iterative Solver and
  terminating the iteration upon successful
  completion.

NOTE:
  The default values are
  CT = .000001  and  TV = 1 .
024
The TYPICAL VALUE field
  the Typical Value (TV) in conjunction with
  the Comparison Tolerance (CT) serves for
  checking the consistency of rules and, in
  particular, for deciding on successful
  completion of iteration.

  A rule is considered satisfied and an
  equation is presumed to hold if

   max(abs(LHE),abs(RHE))  ó  CT * TV

  where LHE,RHE are values of left-hand and
                right-hand side expressions



  If this condition is not satisfied, the
  'relative error ó CT' criterion is used
  for checking the rule consistency.

  This parameter may be changed on the Global
  Sheet and thus the checking criterion adjusted
  to the order of magnitude of computed values.
021
MAXIMUM ITERATION COUNT field
The steps performed by the Iterative Solver
are counted and the current step numbers are
displayed on the Prompt/Error Line during the
iteration.

The default value of the Maximum Iteration Count
is 10.  If the Iterative Solver does not find a
solution within 10 steps, it stops, TK beeps,
and each guess variable retains its latest guess
value in its Input field and the flag G in its
Status field.  You may press  F9  again.  If the
Iterative Solver just needed more steps, there
is a chance that it will eventually converge to
the solution.  (It is more likely, however, that
the iteration diverged, and you should try again
with different guess values and/or variables.)
NOTE:
  The Maximum Iteration Count may be set on the
  Global Sheet according to the needs of a
  particular problem.
012
The APPEND VARIABLE NAMES field
  controls whether TK automatically adds new
  variable names to the Variable Sheet when you
  enter them on the Rule Sheet.

USAGE:
 Type N for no if you want to manually type
  the names of variables you wish to appear on
  the Variable Sheet.
 Type Y for yes if you want TK to automati-
  cally add the variables to the Variable
  Sheet. (This is the default setting.)
015
The USE PAGE BREAKS field
  inserts page breaks in Print command (/P)
  output and in table displays.

USAGE:
 Type Y for yes, and TK will insert page
  breaks at the end of the printed page.  In a
  table display, the title and column headings
  repeat at the top of each new page.
 Type N for no, and output will be a single
  continuous sheet.  This ignores page numbers
  and form length settings, and prints titles
  and column headings in vertical tables only
  at the top of the table. (This is the default
  setting).
012
The NUMBER PAGES field
  inserts page numbers in Print command output
  and in plot and table displays.

USAGE:
 Type Y, for yes, to  turn on the pagination.
  The default for this field prints a page
  number at the bottom of each page, preceded
  by the word "Page".  Using page numbers
  subtracts two lines from the bottom of each
  page. (This is the default setting.)
 Type N, for no, to turn off pagination.
013
The FORM LENGTH field
  contains the Form Length that specifies the
  total number of lines per sheet of paper.

USAGE:
 The default number of lines is 66.
 Number of lines can be between 7 and 1000.
 The margin at the top and bottom of the page
  is determined by the form length minus the
  printed page length, with the difference
  divided by two.
 When page breaks are off, the form length
  setting is ignored.
014
The PRINTED PAGE LENGTH field
  specifies the number of printed lines per
  page.

USAGE:
 The default is 60 lines.
 Page Length can be between 6 and 1000.
 If the setting for the printed page length is
  greater than the setting for the form length,
  the latter is ignored and there is no space
  between pages.
 Using page numbers and plot and table titles
  decreases the number of printed lines per
  page by two lines each.
012
The PRINTED PAGE WIDTH
  specifies the number of character positions
  available for printing across the page.

USAGE:
 The default width is 80 characters.
 Page Width can be between 6 and 200.
 The printed page width and left margin
  settings together should be less than or equal
  to the width of the paper, or information on
  the right end of each line will print off the
  page.
013
The LEFT MARGIN field
  specifies the width in character positions of
  the left margin.

USAGE:
 The default width is 0, i.e. printing starts
  at the left edge of the paper.
 Left Margin can be between 0 and 200.
 The left margin and printed page settings
  together should be less than or equal to the
  width of the paper, or information on the
  right end of each line will print off the
  page.
015
A PRINTER SETUP STRING
  is normally defined by a series of printable
  ASCII characters and control code sequences:
^  Indicates the beginning of a control code
   sequence.
^^ Sends the caret character itself (^).
^R Sends a carriage return character.
^L Sends a line feed character.
^C Precedes a Control character.
^E Represents an Escape character.
^H Precedes a two-digit hexadecimal code for
   an ASCII character.

See the manual for your particular printer
for more information on setup strings.
008
The ELEMENTS field
  indicates the element number of the last
  element in a list.

NOTE:
  TK automatically updates this number when
  you enter values on a List Subsheet or a List
  Function Subsheet.
012
The VALUE field:
 Contains the numeric or symbolic value of a
  list element.  A value entry can be any valid
  TK expression.  Symbolic values must be
  prefixed with an apostrophe (').
 If unit conversions are not yet defined, a
  question mark (?) precedes the values.
 TK automatically generates numeric values
  using the List Fill command.
 Any value changes made to a List Subsheet or
  a List Function Subsheet are made on all
  corresponding sheets and subsheets.
011
The TYPE field
  designates the type of the declared user
  function.  Typing L designates a list function
  and creates a List Function Subsheet.  Typing
  R designates a rule function and creates a
  Rule Function Subsheet.  Typing P designates a
  procedure function and creates a Procedure
  Function Subsheet.  Once you select a function
  type, you cannot change it unless you delete
  the entire row and enter the function name
  again.
016
The ARGUMENTS field
  displays the number of arguments for a user
  function.  The defaults for the different
  types of functions are:

  List Function       1;1
  Rule Function       0;0    (Argument;Result)
  Procedure Function  0;0    (Input;Output)

  The Arguments field is automatically updated
  as entries are made in the Argument Variables
  and/or Result Variables fields on a Rule
  Function  Subsheet, or in the Input Variables
  and/or Output Variables fields on a Procedure
  Function Subsheet.  The Arguments field for a
  list function is fixed at 1;1.
012
The MAPPING field
  determines how TK derives range values when
  the domain value is within the bounds of the
  specified list, but falls between two values
  in the list.

OPTIONS:
 T specifies the Table Mapping option (the
    default).
 S specifies the Step Mapping option.
 L specifies the Linear Interpolation option.
 C specifies the Cubic Interpolation option.
006
The DOMAIN field:
 Contains the numeric or symbolic value of
  the domain list element.  An entry can be any
  valid value or expression.
 Values cannot be entered unless a list name
  has been declared in the Domain List field.
007
 You can create a Domain or Range List on the
  List Function Subsheet and fill it manually
  or with the List Fill command.  The lists may
  also be generated by list solving or by
  execution of a procedure function.  Values
  entered on the List Function Subsheet appear
  on the List Subsheet as well.
006
The RANGE field:
 Contains the numeric or symbolic value of
  the range list element.  An entry can be any
  valid value or expression.
 Values cannot be entered unless a list name
  has been placed in the Range List field.
016
The PARAMETER VARIABLES field
  lists variables that the function or macro
  shares with the Variable Sheet.  Up to 20
  variable names may appear (separated by
  commas, no duplicates).  If the Append
  Variable Names field on the Global Sheet
  is set to Yes, new names entered in this
  field are added to the Variable Sheet.

  The values of parameter variables (in
  Calculation units) are directly available from
  the Variable Sheet to the function or macro.
  They cannot be evaluated locally and passed to
  the Variable Sheet.  They provide a shortcut
  compared to function arguments, especially in
  the case of indirect function calls.
013
The ARGUMENT VARIABLES field
  names the local variables in the rule
  function which receive or pass values to and
  from the function arguments in the call or
  reference to the function.

  There can be from 0 to 20 argument variables.

  No duplication of variable names is allowed
  within or between the Argument, Result and
  Parameter Variables fields.  However, since
  argument variable names are local, they can
  duplicate variable names outside the function.
019
The RESULT VARIABLES field
  names the local variables in the rule
  function which receive or pass values to and
  from the function values of the expression
  which refers to the rule function.

  There can be from 0 to 20 result variables if
  the rule function is called, but only 1 or 2
  if it is referred to.

  There can be up to 20 result variables if the
  rule function is called, but only up to 2 if
  it is referred to.

  No duplication of variable names is allowed
  within or between the Result, Argument and
  Parameter Variables fields.  However, since
  result variable names are local, they can
  duplicate variable names outside the function.
014
The INPUT VARIABLES field
  names the local variables in the procedure
  function which receive values from the
  function arguments of the call or reference
  to the procedure function.  Input variables
  cannot return values to function arguments.

  There can be from 0 to 20 input variables.

  No duplication of variable names is allowed
  within or between the Input, Output and
  Parameter Variables fields.  However, since
  input variable names are local, they can
  duplicate variable names outside the function.
015
The OUTPUT VARIABLES field
  names the local variables in the procedure
  function which pass values to the arguments
  following the semicolon (in a call) or to
  the function values (in a reference).

  There can be from 0 to 20 output variables if
  the procedure function is called, but only 1
  or 2 if it is referred to.

  No duplication of variable names is allowed
  within or between the Output, Input and
  Parameter Variables fields.  However, since
  output variable names are local, they can
  duplicate variable names outside the function.
013
The PLOT TYPE field

  The options are Line chart, Bar chart and Pie
  chart.  Select an option by typing its first
  letter.

  Once you select an option in the Plot Type
  field, you cannot change it unless you delete
  the entire row.

  Bar charts and pie charts require a graphics
  display; line charts may be created with a
  text-only display.
008
The TITLE field

  The field's contents will be displayed at the
  top of the chart when F7 is pressed.

  An entry or change made here will be reflected
  in the corresponding field on the associated
  Plot Subsheet, and vice versa.
007
The DISPLAY OPTION field

  specifies the device to be used for displaying
  plots.  To choose a device, type in the index
  of the device appearing in the prompt line.

  The list of display devices shown is actually
005
  selected from a list of supported devices;
  it must be included in the configuration
  file TK2.CFG.  Type /SC to bring up the
  configuration editor if you need to change
  the options.
005
The DISPLAY SCALE field

  Options are Yes (the default) and No to
  select whether or not the axes and scales
  are displayed.
004

NOTE: The Help screens for all Plot Subsheet
      settings are "chained".  Keep pressing
      ÄÙ to browse through one after another.
007
The DISPLAY ZERO AXES field

  Zero axes are dotted lines representing the
  line y=0 (the x-axis) and the line x=0 (the
  y-axis).  The options are None (the default),
  X-axis, Y-axis or Both.  Select an option by
  typing its first letter.
005
The DISPLAY GRID field

  Options are Yes and No (the default) to select
  whether or not grid lines are displayed on the
  plot.
019
The LINE CHART SCALING field

  Plots may use linear or logarithmic scaling on
  either or both axes, or isotropic scaling.
  The options are Linear (default), X-log,
  Y-log, Both X & Y-log, and Isotropic.  Select
  an option by typing its first letter.

  Logarithmic scaling cannot be used to display
  values equal to or less than zero.  If scales
  are displayed, the scale for a logarithmic
  axis will always show a range of at least one
  order of magnitude, even if the plotted
  points fall within a smaller range.

  Isotropic scaling is a special case of linear
  scaling, when the same scale factor is
  applied to both X and Y axes.

016
The SYMBOL COUNT field

  specifies the appearance of symbols on the
  line chart.  A count of 0 (default) means that
  no symbols are displayed (except when Style is
  set to Symbols), a count of 1 displays symbols
  at all points, a count of 2 displays one and
  skips one symbol, a count of 3 displays one
  and skips two symbols, and so on.

  Choose a larger value of Symbol Count if you
  think the symbols appear too close to each
  other on the plot.

  The Symbol Count can take a maximum value of
  32000.
007
The X-AXIS LABEL field

  specifies an optional label which is displayed
  below the x-axis.  The label may consist of
  any typeable characters including blanks, and
  it is centered (or left-justified and
  truncated if it is longer than the x-axis).
007
The Y-AXIS LABEL field

  specifies an optional label which is displayed
  vertically alongside the y-axis.  The label
  may consist of any typeable characters
  including blanks.  It is centered, and
  truncated if it is longer than the y-axis.
007
The X-AXIS MINIMUM, MAXIMUM field

  The default of a blank field causes the plot
  interval in the horizontal direction to be
  determined by the entire x-axis list.

  A different range of horizontal coordinates
011
The Y-AXIS MINIMUM, MAXIMUM field

  The default blank field causes the
  largest and smallest values for the
  vertical coordinates to be determined
  by the largest and smallest values
  from all the lists named in the Y-Axis
  field.  The largest and smallest values
  do not have to come from the same list.

  A different range of vertical coordinates
010
AXIS MAXIMUM MUST BE SPECIFIED AND GREATER THAN
MINIMUM

  The X-Axis Minimum, Maximum and Y-Axis
  Minimum, Maximum fields control the range of
  the x-axis and y-axis values displayed.  The
  default of a blank field causes all values
  present in lists to be displayed.

  On each axis, a different range of coordinates
012
  can be specified by entering two numbers in
  this field, separated by a comma.  The first
  value represents the minimum value to appear
  on the axis, the second the maximum.  Both
  numbers must be given, and the second must be
  larger than the first.

NOTES:
 If Display Scale is set to Yes, the actual
  range displayed may be slightly larger.
 You can type a space between the numbers and
  TK will translate it to a comma upon entry.
010
The X-AXIS LIST field

  Only one list name must be entered here to
  specify the list which will provide the
  values for the horizontal coordinates along
  the x-axis of the plot.  These values are
  also referred to as "abscissa values."

  You can dive into the List Subsheet for the
  specified list by typing >.
026
The Y-AXIS field

  specifies the list or lists which will provide
  the values for the vertical coordinates along
  the y-axis of the plot.  These values are also
  referred to as "ordinate values."

  At least one list name must be specified in
  order to display a plot.  Multiple list names
  are entered in a column under the field name.
  You can dive into the List Subsheet for any of
  the specified lists by placing the cursor on
  the desired list name and typing >.

  When you enter a list name in this field, TK
  automatically places default settings Lines in
  the Style field, * in the Character field, and
  values 0 in the Symbol Count field, 1 in the
  First field and blank in the Last field.

  If the length of a y-axis list is equal to or
  shorter than the length of the x-axis list,
  all values of the y-axis list will be plotted.
  If the y-axis list is longer than the x-axis
  list, only values of elements corresponding to
  elements in the x-axis list will be plotted.
006
The FIRST field
  specifies the element number of the first
  element in the y-axis list to be plotted.
  The setting can be any integer from 1 (the
  default) to 32000.  This field cannot be
  blanked.
008
The LAST field
  specifies the element number of the last
  element in the y-axis list to be plotted.  The
  default setting of a blank field will cause
  the last element in the list to be the last
  element plotted.  The setting can be blank, or
  any integer from 1 to 32000 which is larger
  than the setting for the First field.
023
The STYLE field

  A setting of Lines (the default), Curves, or
  Symbols is selected by typing the first letter
  of the desired option.

 The Lines option (default) will cause a plot
  to consist of straight lines connecting data
  points.
 The Curves option will cause a plot to consist
  of pieces of cubic polynomials connecting data
  points.  It usually produces visually smooth
  lines although it does not secure the conti-
  nuity of slope at these points.
 The Symbol option will cause a plot to consist
  of data points represented by the character
  specified in the Character field, with no
  connecting lines.

Note: Graphs consisting of both symbols and
  lines (or curves) may be produced if STYLE is
  set to Lines (or Curves) and Symbol Count is
  set to a value greater than 0.
021
The CHARACTER field

  The default setting, *, can be changed to any
  typeable character.  You must press F5 before
  entering any of the command characters / = :
  ; < > !.  This field may also be blanked to
  display nothing; you may prefer this to
  deleting those y-axis lists the display of
  which you want to suppress.

  The y-axis lists are plotted in the order in
  which they are listed in the Y-Axis field;
  consequently, if two list have overlapping
  plot points, the character from the last list
  plotted will be displayed.

NOTE:
  Almost all the ASCII characters can be used in
  this field.  For example, hold down the Alt
  key while typing 001 on the numeric keypad,
  and a smiling face should appear.
006
The X-AXIS LIST field

  The list specified here will supply the
  symbolic or numeric values used for the x-axis
  scale.  If the field is left blank, TK will
  provide scale numbers automatically.
007
The Y-AXIS LIST(S) field

  From one to five list names can be entered
  here, multiple names being separated by
  commas.  If only one list name is entered, you
  can dive from this field into the List
  Subsheet.
005
The BAR CHART TYPE field

  The options of Multiple bar (the default) or
  Stacked bar are specified by typing the first
  letter of the desired option.
003
The FILL TYPE field

  The options are None, Solid or Pattern.
007
The DATA LIST field

  A data list must be specified.  The elements
  of the data list provide the numeric values
  used to generate the pie chart.  Negative
  values are not allowed.  (Elements with
  symbolic values will be ignored.)
006
The LABEL LIST field

  The Label list supplies the symbolic or
  numeric values used to label the slices of
  the pie.  Specification of a label list is
  optional.
005
The DISPLAY PERCENT field

  The options of Yes and No control the display
  of percentages along with the labels for the
  slices.
008
The SCREEN OR PRINTER field

  Type the first letter of the desired option to
  direct the table output to the screen or to
  both the printer and screen.

  See related topic Table Sheet for complete
  information on printing a table.
004
The TITLE field

  The title, which is optional, is displayed at
  the top of the table.
005
The VERTICAL OR HORIZONTAL field

  Default is V for Vertical.  Type V or H to
  determine whether the lists will be displayed
  column-by-column or row-by-row, respectively.
010
The ROW SEPARATOR field

  The optional row separator, which can be any
  string of printable characters, is displayed
  between rows to provide definition to the
  table.  The row separator is repeated in order
  to extend the width of the table.  (Holding
  down the Alt key while typing 196 on the
  numeric keypad will produce a dash which will
  make a solid line when repeated.)
011
The COLUMN SEPARATOR field

  The optional column separator, which can be
  any string of printable characters, is
  displayed between columns to provide
  definition to the table.  The column separator
  is repeated in order to extend the height of
  the table.  (Holding down the Alt key while
  typing 179 on the numeric keypad will produce
  a vertical line which will make a solid line
  when repeated.)
006
The FIRST ELEMENT field

  The default is 1.  The number specified in
  this field determines the element number at
  which to start displaying list elements in the
  table.
007
The LAST ELEMENT field

  The default is a blank field, which causes TK
  to exhaust each list when displaying list
  elements in the table.  The number specified
  in this field determines the last list
  elements to be displayed in the table.
021
The LIST field

  Lists specified in this field are displayed in
  the table.  Multiple list names are entered in
  a column under the field name.

  Since diving from anywhere in a Table Subsheet
  will bring up the corresponding Interactive
  Table Subsheet, you cannot dive directly from
  a list named in this field into a List
  Subsheet.  (You could dive again from the
  Interactive Table Subsheet into a List
  Subsheet, but there would be rare occasion to
  do so because you can manipulate list elements
  directly from the Interactive Table Subsheet.
  Such an occasion might be to change units, or
  to insert or delete rows in only one list.)

  When a list name is entered in this field, a
  default value of 10 is entered on the same row
  in the Width field.
004
The WIDTH field

  Each list can have its own width specified in
  this field.  The default width is 10 spaces.
009
The HEADING field

  The heading for a column (Vertical table) or
  row (Horizontal table) can consist of any
  printable characters.  If no entry is made,
  the name of the list will be used as a
  default.  Enter a space to avoid displaying
  a heading or, more precisely, to display a
  blank as a heading.
014
VALUE fields in an Interactive Table Subsheet

  allow entering, blanking, deleting or editing
  element values in any list displayed in the
  table.

NOTES:
 The values may only be entered in a column or
  row with a defined list name.
 Only complete rows may be deleted.
 The changes in list elements may be observed
  in the value fields during solution if the
  Display Intermediate Values field in the
  Global Sheet is set to Yes.
018
PERCENT SYMBOL, %

  In the Status field of the Rule Sheet or Rule
  Function Subsheet or Procedure Function
  Subsheet: see the appropriate related topic.

  In the List Sheet:  When you load ASCII, DIF
  or WKS files with listnames which are dupli-
  cates of those which are in the List Sheet
  already, TK will prefix the names of loaded
  lists with  % .  You can then change any of
  the listnames on the List Sheet as convenient.

NOTE:
  % is also one of the special characters which
  may be used anywhere in TK names (i.e.
  variable, list, function, plot or table names,
  or symbolic values).
020
POUND SYMBOL, #
  is inserted as a delimiter in listnames when
  TK automatically generates master lists and
  subordinate lists.

EXAMPLE:
  The statement 'ab[2][4]:= 678  will automati-
  cally create a master list  ab  with the value
  of the 2nd element equal to  'ab#2 ,  and
  a subordinate list  ab#2  with the 4th value
  equal to  678  (provided a list  ab  had not
  existed before).

NOTES:
 # is one of the special characters which may
  be used anywhere in TK names (i.e. variable,
  list, function, plot or table names, or
  symbolic values).
 The  #  option of the Storage command (/S)
  selects the DIF Storage command.
022
APOSTROPHE, '
  is used as a prefix in symbolic values and
  constants, for instance 'dog, 'Marty, 'OK .

  It has to precede names of lists when they are
  used in references to list elements, e.g.
                'abc[i][j] ,
  or in names of variables, lists and functions
  when the latter are used as function
  arguments, e.g.
    given('uv)  listcopy('A,'B)  apply('sin,x)
  - the last expression means simply sin(x).

  When the apostrophe is omitted, as in q[i][j],
    given(xy), listcopy(R,S)  and  apply(Fn,x),
  TK will interpret q, xy, R, S and Fn as
  variable names.  The variables in this case
  must have symbolic values to serve as list
  names or function arguments.  The last four
  expressions would therefore have the same
  meaning as the four expressions preceding them
  if  q='abc, xy='uv, R='A, S='B  and  Fn='sin.
001
SPECIAL SLASH COMMANDS
037
TK MENUING SYSTEM

TK Solver Release 2.0 has a built-in capability
to load annotated model selection menus and
alternate freely between the TK program and
the chosen menu.

LOADING a TK menu:
 From the DOS prompt, type  TK2 filename ÄÙ
  where  filename.TKM  is a file defining the
  menu.  (You don't need to type the extension
  .TKM).  The menu will appear on the screen.
 If TK is already running, type   Shift-F3
  or  F11  to bring up a prompt for the menu
  filename.  Type in a new menu filename, edit
  the default filename, or type Space ÄÙ to
  access the Disk Index.

USAGE:
 Move from the menu to TK by completing the
  model selection (to load the selected models
  into TK) or by pressing Esc (to avoid loading
  any models).
 Move from TK to the menu by pressing  Shift-F3
  or  F11  to load the menu as described above.

NOTES:
 Running under a menu does not affect the
  functionality of TK Solver; you can select
  and load models directly using F3 or the /SL
  command, as well as by using the menu.
 Menu screens have their own Help window with a
  summary of commands (it is invoked by pressing
  F1 when the menu is on screen).
 The menuing system uses an index to the menu
  script file on disk, so running a menu takes
  only 1K or less from memory available to TK.
031
The TK LIBRARY
  contains over 100 models in 15 subdirectories:

 ROOTS    Roots of Equations
 DIFFINT  Differentiation and Integration
 DIFFEQ   Differential Equations
 SPECIAL  Special Functions
 COMPLEX  Complex Variables
 OPTIM    Optimization
 MATRIX   Matrix Manipulation
 BIGINT   Arbitrary Length Integers
 TABLES   Interactive Tables
 GRAPH    Graphics
 STAT     Statistics
 ENG      Engineering and Science
 FINANCE  Finance
 UTIL     Utilities
 TUTORIAL Tutorials for Introduction Manual
These models often contain example user-defined
functions and/or keystroke macros that you can
adapt to your own needs with little, if any,
modification.  After becoming familiar with a
model, you can delete documentation and/or
supporting rules and variables, then save the
model under a new filename.  The new model can
then be merged or included with other TK models.

ACCESSING the TK LIBRARY:

  The recommended installation process puts the
  Library directories under the TK directory.
003
  You can also access the Library by pressing F3
  and using the Disk Index to find and load a
  model of interest.
033
MATHLOOK
  diplays rules and statements in a Field Box in
  a format that is easy to scan and read, .e.g.

      Fij=(1+wj/wi-sqrt(1+(wj/wi)^2))/2

  is displayed as
                        ÚÄÄÄÄÄÄÄÄÄÄÄÄ
                        ³          2
                        ³    Ú    ¿
                  wj    ³    ³ wj ³
             1 + ÄÄÄÄ - ³1 + ³ÄÄÄÄ³
                  wi    ³    ³ wi ³
                        õ    À    Ù
      Fij = ÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄÄ
                        2


Where and How You Can Use MathLook:
 Mathlook is available only when you are in
  the Presentation View and the block cue is
  in a Field Box linked to the Rule Sheet or
  to a Rule Function, Procedure Function, or
  Keystroke Macro Subsheet.
 Press the Enter key to toggle back and forth
  between the normal format of one line per
  equation or statement and the MathLook format.
  - The cursor (highlight) in MathLook
    automatically adjusts its height to
    cover one rule or statement.
  - If you begin editing a rule or statement
    in MathLook, the format reverts to the
    normal format.
!help
!!N
/unknown
!!?
c-1
#`?
c-2
$V'
c-6
$!(
c-7
$GK
c100
%:^
c1000
!HB
c102
%:^
c103
$!H
c120
$YN
c123
%)!
c124
$\+
c125
%!"
c126
%'&
c127
%"Y
c146
$#"
c151
%>2
c152
%>2
c153
%@'
c154
%=6
c155
%=6
c158
%;T
c178
%*\
c186
%?(
c187
%@'
c198
%?(
c199
$UX
c200
$UX
c209
$QJ
c210
$RN
c211
$O`
c216
%!"
c217
%!"
c218
%'&
c219
%'&
c220
%"Y
c221
%"Y
c222
%)!
c223
%)!
c224
%*\
c225
%*\
c226
%,W
c227
%,W
c233
%F&
c234
%F&
c235
%GJ
c236
%GJ
c237
%GJ
c238
%FX
c239
%J.
c245
'M#
c246
%I2
c259
'M#
c262
%$T
c263
%$T
c264
%$T
c265
$4V
c274
$7<
c279
$YN
c285
%.R
c286
%.R
c287
%.R
c293
$$N
c294
$$]
c295
$$]
c296
$$]
c312
$TT
c313
$TT
c38
%,W
c401
$%,
c402
$%,
c404
$%,
c405
$%,
c406
$%,
c407
$%,
c408
$%,
c409
$%,
c411
$%,
c412
$%,
c414
$%,
c415
$%,
c416
$%,
c417
$(!
c418
$(R
c419
$)2
c420
$*7
c421
$+]
c422
$C$
c423
$D!
c425
$D<
c426
$GK
c428
$HO
c429
$K'
c430
$K;
c431
$L@
c432
$%,
c434
$O:
c435
$O`
c436
$PF
c437
$QJ
c438
$RN
c440
%A%
c442
$V_
c443
$SR
c445
$UX
c446
$U:
c447
$V'
c448
$V_
c449
$YN
c451
$\+
c452
$]O
c454
%3F
c455
%5/
c456
%6X
c458
%8[
c460
%9T
c461
%:9
c463
%:^
c464
$]O
c465
%!"
c466
%"Y
c467
%$T
c468
%'&
c469
%*\
c470
%,W
c471
%)!
c472
%.R
c473
%8[
c474
%8[
c475
%8[
c477
$U:
c478
$U:
c479
$U:
c480
$U:
c481
$UX
c482
$UX
c483
$UX
c484
$UX
c487
%5/
c488
%F&
c489
%FX
c490
%6X
c491
%GJ
c492
%J.
c493
%3F
c494
$&=
c495
$'/
c496
%;T
c497
%=6
c498
%>2
c499
%?(
c50
$SR
c500
%@'
c506
%9T
c507
%9T
c508
%9T
c516
%K:
c517
$O:
c518
%MH
c519
%P!
c520
%R#
c521
%T:
c522
%V6
c523
%X6
c525
%A%
c527
$V_
c528
$TT
c530
$UX
c531
$UI
c532
$V'
c533
$UI
c534
$UI
c535
$UI
c536
$UI
c539
%A%
c540
%C@
c541
%D;
c543
%E"
c544
%ED
c545
$TT
c546
$TT
c547
$TT
c548
$TT
c59
%D;
c60
%D;
c61
%ED
c62
%E"
c72
$U:
c74
$U:
c76
$RN
c77
$PF
c80
$O:
c84
$O`
c86
$U:
c90
%F&
c92
$YN
c93
$&=
c94
$&=
c95
$'/
c99
$\+
charstr
":B
cursorfn
!FD
e1
"=N
e10
!L)
e101
"X*
e104
%]R
e105
%;T
e106
%?(
e107
"X*
e108
%;T
e109
%=6
e11
!M.
e110
&_4
e111
&U3
e112
&W$
e113
&Q5
e114
&]F
e115
"Y5
e116
&;&
e117
"YD
e118
"Z%
e119
"Z@
e12
!M.
e121
"[!
e122
"[C
e128
',(
e129
%]R
e13
"DA
e130
'%Z
e131
'"X
e132
'!Y
e133
')<
e134
'#P
e135
'-*
e136
&`V
e137
"\&
e138
"\5
e139
!&^
e14
"E!
e140
"\D
e141
!L)
e142
"\S
e143
"]"
e144
"]1
e145
"]@
e147
"]O
e148
%@'
e149
%>2
e15
"E_
e156
!L)
e157
!L)
e159
"^6
e16
"G/
e160
"^E
e165
"_%
e166
#!+
e167
##1
e169
&D)
e17
"H-
e170
"9-
e171
"9-
e176
"9-
e177
"9-
e179
##Q
e18
"I+
e180
'.-
e181
#%/
e182
#&M
e183
#&\
e184
%]R
e185
&+*
e188
#'+
e189
%[B
e19
"IG
e193
&;F
e194
#'S
e195
%ZL
e196
%]R
e197
&H/
e2
"=]
e20
!($
e21
"JE
e212
#(5
e213
#(D
e22
"K%
e229
&F3
e23
!M.
e230
&OQ
e231
&PA
e232
#))
e24
"KM
e240
#)I
e241
!)4
e242
%;T
e248
#*'
e249
#*6
e25
"L-
e250
#*E
e251
#*T
e252
#+#
e253
&?-
e254
&;F
e256
#+2
e26
"M'
e260
#+A
e261
#+P
e27
"ML
e272
'/3
e278
%]R
e28
"N2
e283
$1.
e284
$1.
e289
#+_
e29
!)4
e291
#,M
e297
%]R
e298
%]R
e299
#-0
e3
"?&
e30
"N[
e31
"OA
e310
#-?
e32
"P7
e33
!L)
e34
"PW
e35
&+M
e36
&,5
e37
"Q&
e39
&(6
e4
"@/
e40
&;&
e41
'41
e42
'5+
e43
'6J
e44
'=$
e45
'<'
e46
'>U
e47
'=`
e48
"SE
e5
"A2
e51
"Q_
e52
"RR
e53
%A%
e54
"RR
e55
"ST
e56
%B;
e57
%B;
e6
"AW
e601
#.(
e602
#.H
e604
#/(
e605
#/H
e606
#/W
e607
#07
e608
#0K
e609
#0Z
e610
#1)
e613
#18
e63
"T7
e64
"TW
e67
&=A
e68
%;T
e69
%B;
e7
"B<
e701
#1W
e702
#1W
e703
#2+
e704
#2?
e708
#2N
e712
#2]
e713
#3Q
e714
#4A
e715
#4U
e716
#5G
e717
#69
e718
#7(
e719
#7W
e720
#8I
e721
#99
e722
#:)
e723
#;%
e724
#<!
e725
#<]
e726
#=Y
e727
#>U
e728
#?Q
e729
#@M
e730
#AI
e731
#A]
e732
#B1
e733
#C#
e734
#CR
e735
#DA
e736
#E0
e737
#E`
e738
#FG
e739
#G7
e740
#H+
e741
#H_
e742
#I[
e743
#JK
e744
#K;
e745
#L7
e746
#M.
e747
#N%
e748
#N9
e749
#O5
e750
#O`
e751
#P/
e752
#P>
e753
#Q!
e754
#QG
e755
#R-
e756
#RS
e757
#S9
e758
#S_
e759
#U#
e760
#UX
e761
#V@
e762
#W>
e763
#X?
e764
#XN
e765
#X]
e766
#Y^
e767
#ZI
e768
#1W
e769
#[G
e770
#]-
e771
#]<
e772
#]K
e773
#]Z
e774
#,M
e775
#^V
e776
#_%
e777
#_4
e78
$PF
e8
"C"
e800
#_C
e801
#_C
e81
"U7
e83
"U]
e85
"V@
e88
"W&
e89
"WH
e9
"CH
e91
$'/
pb
'@X
pbbf
&PA
pbbh
&LE
pbbp
&OQ
pbbs
&J[
pbbt
&KR
pbbv
&M8
pbbx
&N*
pbby
&N\
pbd
'F_
pbi
'C&
pbl
'C&
pbp
'E.
pbt
'HO
pccf
&TC
pccl
&R+
pccp
&S!
pccs
&Q5
pcct
&SS
pcl
'05
pcn
%]R
pcs
'05
perc
'K&
pfa
&)0
pfc
&#^
pffc
&#^
pffd
&+M
pfff
&+M
pffm
&+*
pffr
&,5
pfft
&,5
pfn
%]R
pft
&(6
pg
"]@
pga
'3+
pgb
':<
pgc
'41
pgf
'<'
pgg
'8@
pgga
&B9
pggb
&C2
pggc
&G<
pgge
&J'
pggf
&I4
pggg
&>4
pggh
&@R
pggl
&?-
pggm
&H/
pggp
&F3
pggs
&<D
pggt
&?_
pggv
&AE
pggx
&DN
pggy
&EA
pggz
&=A
pgh
'21
pgi
'6J
pgl
'=$
pgm
'>U
pgn
';2
pgr
'18
pgs
'?S
pgt
'5+
pgv
'9W
pgw
'=`
piia
&_4
piib
&_4
piic
&_4
piid
&_4
piie
&_4
piif
&_4
piig
&_4
piih
&_4
piii
&_4
piij
&_4
piik
&_4
piil
&_4
piim
&_4
piin
&_4
piio
&_4
piip
&_4
piiq
&_4
piir
&_4
piis
&_4
piit
&V&
pkc
&#^
pkh
$1.
pkkc
&#^
pkkh
$1.
pkkp
&)U
pkks
%\"
pkkt
$:'
pkn
$1.
plc
&#^
ple
&8`
pllc
&#^
plld
%`^
plln
&9@
pllu
&"@
pllv
&:F
pln
%]R
plu
%_$
pnc
&#^
pnn
%]R
pnn+
')<
pnn.
'%Z
pnnb
'*3
pnnc
&_U
pnnd
'"X
pnne
'+*
pnng
'&`
pnni
'$U
pnnj
',(
pnnl
'-*
pnnn
&`V
pnnp
'#P
pnnr
'.-
pnns
'!Y
pnny
'/3
pnnz
'(?
pound
'LE
ppi
&?_
ppj
&;F
ppn
%]R
pppc
&#^
pppi
&4)
pppo
&6E
pppp
&,]
ppps
%\"
pppt
!)4
ppt
&;&
prr
&''
prra
&/5
prrc
&#^
prrp
&,]
prrr
&''
prrs
%[B
prrv
&1N
prs
%[B
pssbc
$F?
psslc
$EW
psspc
$G'
ptn
%]R
ptt
&V&
pttc
&XQ
ptte
&Z:
pttf
&YF
ptth
&^;
pttl
&[,
pttn
&[_
pttr
&WY
ptts
&U3
pttt
&V&
pttv
&W$
pttw
&]F
pua
"!?
puc
&#^
puf
"!?
pum
"!?
put
"!?
pvc
&#^
pvi
%\O
pvn
%]R
pvo
%^@
pvs
%ZL
pvu
%_$
pvva
&%,
pvvc
&#^
pvvd
%`^
pvvf
&$A
pvvi
%\O
pvvn
&&!
pvvo
%^@
pvvs
%ZL
pvvu
&"@
pxxs
'05
pxxt
'05
